kind = "web"
  previewPath = "/"
  title = "LazyTopper"
  version = "1.0.0"
  id = "artifacts/lazytopper-app"

  [[services]]
  name = "web"
  paths = ["/"]
  localPort = 25246

  [services.development]
  run = "pnpm --filter @workspace/lazytopper-app run dev"

  [services.env]
  PORT = "25246"
