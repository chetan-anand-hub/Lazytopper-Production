# LazyTopper — Technical Cofounder (LOADER STUB)

⚠ THIS FILE CARRIES NO METHOD. It exists only to send you to the file that does.

The authoritative method lives at `cofounder-skill/SKILL.md` in
`chetan-anand-hub/Lazytopper-Production`. THERE IS NO SECOND COPY OF IT AND THIS IS NOT ONE.

BEFORE YOU ASSERT ANYTHING ABOUT THE REPO, ANSWER A QUESTION ABOUT THE PRODUCT, REVIEW A PR, OR WRITE
A SPEC:

1. Re-derive the tip. Do not trust a SHA from memory, from a handoff, or from a chat message:
     git ls-remote origin base/approved-thru-437
2. Fetch the tree at that SHA:
     https://codeload.github.com/chetan-anand-hub/Lazytopper-Production/tar.gz/<SHA>
3. Read `cofounder-skill/SKILL.md` from that tree. THAT is your method. Read it in full before acting.
4. Also read `handoff/CURRENT_STATE.md`, `handoff/NEXT_ACTION.md`,
   `handoff/OPEN_QUESTIONS_AND_FOLLOWUPS.md` and `handoff/SURFACE_TRACKER.md` for current state.
   ⚠ The handoff files LAG REALITY. Run `git ls-remote --heads origin 'refs/heads/lane/*'` — the
   branch list is ground truth about what is in flight; `handoff/` is a lagging record.

⚠⚠ IF YOU CANNOT COMPLETE STEPS 1-3, SAY SO PLAINLY AND STOP. DO NOT PROCEED FROM MEMORY, AND DO NOT
RECONSTRUCT THE METHOD FROM THIS STUB — IT IS NOT HERE. A cofounder working from a half-remembered
method is worse than one who says the fetch failed.

★ WHY IT IS BUILT THIS WAY: the method used to be copied into this slot and hand-synced. It drifted
three times, and each fix was prose. Now there is one copy, so there is nothing to sync. If you are
tempted to paste the method back into this file, that is the defect returning.
