Good gap analysis. Directionally, I agree with it.

Here are the product decisions so Phase 2 can start without ambiguity:

## Settled decisions

### 1. Theme polarity

Implement the **locked light baseline**.
The current dark theme is **not** an approved delta for this parity task.

### 2. Typography

Align to the locked baseline:

* **Inter** for body
* **Fraunces** for headings

Current Space Grotesk is not an approved parity exception.

### 3. Route strategy

Use `/app/*` as the **primary baseline route structure**.

Add baseline-faithful routes such as:

* `/app`
* `/app/intent`
* `/app/practice`
* `/app/practice/worksheets`
* `/app/check-improve`
* `/app/exam-trends`
* `/app/topic-hub`
* `/app/me`

Keep current parameterized routes temporarily as compatibility/deep-link routes where necessary.
Do not break working engine logic if it can be adapted behind the baseline route layer.

### 4. Home vs dashboard

Do **not** simplify `/dashboard` in place as the first move.
Create a new baseline-faithful **Home at `/app`**.
Existing `/dashboard` can remain temporarily if needed.

## Phase 2 instruction

Proceed with a **controlled baseline-parity implementation**.

Important rules:

* do not redesign
* do not modernize
* do not simplify outside the locked baseline
* do not mix gateway/login/payment work
* do not mix solution-tightening/final-answer/duplicate-ID work
* do not do opportunistic cleanup

## Implementation approach

Build additively where possible.

1. Theme + typography foundation
2. New desktop AppShell matching baseline
3. `/app/*` route structure
4. New `/app` Home
5. New `/app/intent`
6. New `/app/practice` mode-selector landing
7. New `/app/practice/worksheets`
8. New `/app/check-improve`
9. Parity polish for Trends / Topic Hub / Me
10. Responsive mobile parity pass against locked mobile baseline

## Important clarification

When you say “AppShell needs to be built from scratch,” treat that as:

* build a **new additive baseline-faithful shell**
* not a destructive rewrite of the whole app

## Required output for Phase 2

Return:

1. exact files changed
2. exact routes added/changed
3. exact components added/changed
4. exact screens affected
5. desktop parity summary
6. mobile parity summary
7. explicit list of what remained intentionally unchanged
8. explicit list of any unavoidable mismatches
9. before/after screenshots for every affected desktop screen
10. before/after screenshots for every affected mobile screen
11. explicit confirmation that no drift was introduced outside approved parity work

Proceed with this as the approved implementation direction.
