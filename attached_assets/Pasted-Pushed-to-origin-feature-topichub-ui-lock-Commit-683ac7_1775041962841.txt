Pushed to `origin/feature/topichub-ui-lock`.

Commit: `683ac76`  
Message: `fix: wire DiagramBlock, fix markNodeLearning signature, Gemini smoke test`

`npm run build` passed. The code changes are in [TeachFlow.tsx](/c:/Projects/lazytopper/lazytopper_MAIN_WORKTREE/src/components/tutor/TeachFlow.tsx), [TopicHub.tsx](/c:/Projects/lazytopper/lazytopper_MAIN_WORKTREE/src/pages/TopicHub.tsx), and the smoke log is in [gemini-smoke.log](/c:/Projects/lazytopper/lazytopper_MAIN_WORKTREE/src/__tests__/gemini-smoke.log).

Curl summary:
- The exact flat `/api/mentor` curl body did not reach Gemini. It returned `{"ok":false,"error":"Invalid image: Only JPG and PNG images are allowed."}`.
- Root cause: `normalizeMentorRequest()` in [server/index.cjs](/c:/Projects/lazytopper/lazytopper_MAIN_WORKTREE/server/index.cjs) always adds `imageBase64`, `imageMimeType`, and `imageName` keys for flat requests, and `validateMentorImagePayload()` in [mentorImageSupport.cjs](/c:/Projects/lazytopper/lazytopper_MAIN_WORKTREE/server/mentorImageSupport.cjs) treats those keys as an image payload.
- I then verified the preferred nested `payload` shape against the same endpoint. That returned real `learn_teach` content, and `/api/health` reported `provider:"gemini"` and `stub:false`. So the Gemini path is live; the flat curl shape is blocked by the image-validation bug.

`markNodeLearning` signature found:
- `markNodeLearning(snapshot: TopicHubMasterySnapshot, nodeId: string): TopicHubMasterySnapshot` in [topicHubMastery.ts](/c:/Projects/lazytopper/lazytopper_MAIN_WORKTREE/src/services/topicHubMastery.ts)
- The `TopicHub.tsx` call is now type-safe: it uses `updateTopicMastery((prev) => markNodeLearning(prev, topicKey))` with no cast.

`DiagramBlock` props found:
- Named export `DiagramBlock` from [DiagramBlock.tsx](/c:/Projects/lazytopper/lazytopper_MAIN_WORKTREE/src/components/DiagramBlock.tsx)
- Props: `diagram`, `diagramType`, `diagramLabels`, `diagramSpec`, `title`, `note`
- `TeachFlow.tsx` now uses `diagramType={diagram.type}` and `note={diagram.altText || "Concept diagram"}` because `DiagramBlock` does not expose an `altText` prop.