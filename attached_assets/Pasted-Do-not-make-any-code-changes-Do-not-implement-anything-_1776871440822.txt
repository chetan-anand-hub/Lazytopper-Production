Do not make any code changes. Do not implement anything. Do not refactor anything.

I need a **precise audit-support handoff** for LazyTopper so another reviewer can continue seamlessly.

Project context:

* Repo: `chetan-anand-hub/Lazytopper-Production`
* Current audit scope: **solution-tightening work only**
* Explicitly exclude gateway work
* Gateway work has **not** started yet
* Compare range under audit: `1342d57...89c4fbf`
* Main anchor commit: `80fa1da97440c5764da34a826958a31902f2f589`

Locked Lovable baselines:

* Desktop: `https://id-preview--e49fe255-d9b4-4d48-b708-31252b7f230a.lovable.app/`
* Mobile: `https://id-preview--18842e8f-6c4b-4505-9eeb-0747e3a33c5a.lovable.app/`

What the audit is checking:

* step-by-step solution tightening
* final answer tightening
* earlier issue: truncated science `finalAnswer`
* earlier issue: duplicate-ID closure may have been only partial
* strict no-drift from locked Lovable baselines

I need you to answer in the exact structure below with **concrete details only**. Do not give generic commentary.

## 1) EXACT FILES TO REVIEW

List the exact file paths touched in this solution-tightening work, especially around:

* science final answers
* solutionSteps cleanup
* duplicate-ID fixes
* fill/validation scripts
* any frontend/UI files touched in this compare range

For each file, give:

* exact path
* why it matters in 1-2 lines
* whether it was directly changed in `80fa1da...`
* whether it affects content quality, ID safety, script safety, or UI drift risk

## 2) EXACT SCIENCE QUESTIONS / IDS / ENTRIES TO SPOT-CHECK

Give the exact science entries that should be checked for prior truncation or completeness issues.

For each one, provide:

* question ID
* chapter/topic
* short question text
* exact file path
* old problematic `finalAnswer` text if known
* new `finalAnswer` text if known
* why this case matters

If you do not know old vs new exactly, say so clearly, but still provide the exact question IDs and locations.

## 3) DUPLICATE-ID VALIDATION DETAILS

Tell me exactly how duplicate-ID safety should be checked in this repo.

Include:

* whether there is already a script/command to detect duplicate IDs
* the exact command to run, if one exists
* if no dedicated script exists, the exact files/banks where duplicate IDs are most likely
* whether the fix in this range is local-only or broader
* any exact duplicate IDs or duplicate patterns previously found

Be specific. I need repo-usable validation guidance, not general advice.

## 4) COMMITS THAT MATTER MOST IN THIS RANGE

List the exact important commits inside `1342d57...89c4fbf`.

For each one, provide:

* commit hash
* one-line purpose
* whether it matters for:

  * science finalAnswer completeness
  * solutionSteps quality
  * duplicate-ID closure
  * script safety
  * UI drift risk
* whether it looks complete, partial, or follow-up only

## 5) WHAT `80fa1da...` ACTUALLY DID

Describe exactly what commit `80fa1da97440c5764da34a826958a31902f2f589` changed.

I need:

* exact files changed
* exact categories of changes
* whether it directly rewrote science `finalAnswer`
* whether it directly addressed duplicate IDs
* whether it touched scripts
* whether it touched UI/frontend files
* whether it appears to fully close the earlier science truncation concern, partially close it, or just move it forward

Do not be vague.

## 6) ANY EXISTING VALIDATION / TEST / CHECK COMMANDS

List any commands, scripts, or manual checks that should be used to verify this work.

Especially include:

* build/type checks
* duplicate-ID checks
* content validation checks
* any script(s) used to fill or patch predicted question solutions
* any manual smoke-check steps for step-by-step solutions or final answers

Give exact commands if available.

## 7) UI DRIFT RISK

If any UI/frontend files were changed in this compare range, list them exactly.

For each one, say:

* what changed
* whether the change was intentional for this scope
* whether it risks drift from locked Lovable baseline
* what screen/flow it affects

If no UI files were changed, say exactly: `No UI/frontend files relevant to locked-baseline drift were changed in this range.`

## 8) MUST-CHECK CASES BEFORE APPROVAL

Give a short reviewer checklist of the exact must-check items before this range can be accepted.

I need:

* must-check content items
* must-check ID safety items
* must-check script safety items
* must-check UI drift items

Keep this actionable and concrete.

## 9) UNCERTAINTIES

List anything you are not fully sure about.

Be explicit if:

* a file path is uncertain
* an exact question ID is uncertain
* a duplicate-ID check is inferred rather than confirmed
* `80fa1da...` is assumed central but not proven
* a UI drift risk is only theoretical

## 10) FINAL HANDOFF SUMMARY

End with:

* what is definitely known
* what is likely but not fully proven
* what another auditor should inspect first
* whether the range currently looks fixed, partially fixed, or still open

Important instructions:

* Do not edit code
* Do not implement anything
* Do not generalize
* Prefer exact paths, exact IDs, exact commit hashes, exact commands
* If something is uncertain, label it clearly instead of guessing
* Optimize your response for a reviewer who needs to audit the range quickly and accurately
