I want you to implement a full “Mistake Intelligence” layer for LazyTopper using the existing answer-checking and mistake logging foundation already in the repo.

Context from current repo state:
- The examiner-style checker already exists and returns annotated steps, marks awarded, mistakeSummary, correctedWorking, and teacherNote.
- The SolutionChecker UI already exists and logs mistakes through logMistakes().
- mistakeLogService.ts already persists mistake logs to localStorage and Firestore.
- But the product does NOT yet turn these logs into a real student-facing improvement system.
- Current weak-area/profile logic is still driven mostly by practice/mastery aggregates, not by the exact checker mistake categories.
- I want to fix this.

Goal:
Turn “Check My Answers” from a one-time copy-checking feature into a true improvement system that:
1. tracks recurring mistake patterns,
2. shows whether the student is improving,
3. identifies where marks are most often lost,
4. recommends next actions.

IMPORTANT PRODUCT RULES:
- Use the existing exact checker categories as the source of truth:
  - conceptual
  - calculation
  - silly
  - presentation
- Do NOT replace them with vague labels.
- Keep current checker behavior intact.
- Add insight and improvement layers on top.
- Do not break current Practice, Topic Hub, or Profile flows.
- Prefer additive, clean implementation.

What I want built:

1) New mistake insights service
Create a new service, something like:
- src/services/mistakeInsightsService.ts

It should build on top of:
- getMistakeLogs(uid, days)
from the existing mistakeLogService.

This new service should expose functions like:
- getMistakeInsights(uid, days)
- getMistakeTrend(uid, currentDays, previousDays)
- getTopicMistakeHotspots(uid, days)
- getRecommendedNextActions(uid, days)

The output should include:
- total checked answers
- total marks lost
- mistakes by type:
  - conceptual
  - calculation
  - silly
  - presentation
- top recurring mistake type
- top topic where marks are lost
- trend vs previous period
- improvement or worsening signal
- recommendation list

2) Trend logic
Implement actual trend comparison:
- compare last 7 days vs previous 7 days
- compare last 30 days vs previous 30 days
- optionally last 90 vs previous 90

Trend should measure:
- number of mistakes by type
- marks lost by type
- overall marks lost
- frequency of repeated mistakes in same topic

Example output ideas:
- “Presentation mistakes down 28% from previous 7 days”
- “Conceptual mistakes in Triangles still recurring”
- “Calculation mistakes improving, but silly mistakes unchanged”

3) Topic-based mistake hotspots
Create topic/chapter hotspot summaries:
- which topic loses the most marks
- which mistake type dominates within that topic
- count + marks lost
- last seen timestamp

This should let us say things like:
- “Most marks lost in Trigonometry due to presentation mistakes”
- “Life Processes has repeated conceptual mistakes”

4) Actionable recommendations
Based on the student’s recent mistake pattern, generate deterministic next-action recommendations, not generic motivational text.

Examples:
- if presentation mistakes dominate:
  - “Retry 3 written questions and focus on units, labels, and final answer presentation”
- if conceptual mistakes dominate:
  - “Learn this topic first before more practice”
- if calculation mistakes dominate:
  - “Do untimed accuracy practice before timed tests”
- if silly mistakes dominate:
  - “Switch to slower timed drills and review final line before submitting”

Implement this as a rules-based first version, not LLM-dependent.

5) New UI on Profile / Me page
Add a compact new section on the Profile / Me page called something like:
- “Mistake Patterns”
or
- “Where You Lose Marks”

This section should be based on real mistake logs, not weakAreaAggregator approximations.

It should show:
- top mistake type
- whether it is improving or worsening
- top topic hotspot
- total marks lost in selected time window
- one primary recommended action

Keep it compact and visually clean.

Do NOT overload the page.

6) Checker result follow-up card
In SolutionChecker, after a result is shown, add a small “Improvement Insight” block below the existing annotated result.

This block should:
- use recent mistake history + current result
- show whether this mistake type is recurring
- show whether student is improving or repeating the same issue
- suggest one next action

Examples:
- “You often lose marks on presentation in Maths. This is your 4th presentation error in the last 7 days.”
- “Your calculation mistakes are lower than last week — good progress.”
- CTA examples:
  - Practice this topic
  - Learn this concept
  - Retry with timed practice

7) Keep exact checker categories consistent everywhere
Audit any place where the product is using surrogate categories like:
- Concept Gap
- Speed Issue
- Accuracy
and make sure the new mistake intelligence system does NOT mix those with the checker’s real categories.

If weak areas remain separate, that is fine.
But mistake intelligence must use the exact four checker categories consistently.

8) Data model expectations
Use the existing MistakeLogEntry shape as base input.
If needed, extend carefully without breaking old logs.
Make sure old logs still work.
Be defensive about missing fields.

9) Edge cases
Handle:
- localStorage-only users
- Firestore available/unavailable
- users with very little history
- no mistakes yet
- malformed/older entries

10) Acceptance criteria
I should be able to:
- check an answer
- have the result logged
- see recurring mistake pattern insights later
- see whether mistakes are improving or worsening
- get actionable recommendations from those trends
- view a compact mistake insights section on Profile / Me

11) Keep the implementation scoped and production-minded
I do NOT want a big architecture rewrite.
I want a focused implementation that adds real product value fast.

Please do the following in your response:
1. Inspect the current files first
2. State the exact files you will modify/create
3. Implement the feature
4. Explain any tradeoffs briefly
5. Do not remove existing working behavior