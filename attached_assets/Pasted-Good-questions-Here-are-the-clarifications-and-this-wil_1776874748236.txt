Good questions. Here are the clarifications, and this will now become a 2-phase task.

## 1. What the actual gap is

This is **not**:

* a full rebuild from scratch
* a blank-slate redesign
* a new product architecture pass

This **is**:

* a **baseline-parity correction pass** against the locked Lovable desktop and mobile baselines

So the answer to Gap 1 is mainly **(b)**:
**a specific set of pages/components have drifted and need to be corrected to match the locked baseline.**

It is not “rebuild the entire app from zero,” because the codebase already contains most of the product capabilities:

* Practice
* Exam Trends
* Check & Improve / Solution Checker
* Topic Hub
* Worksheet flow
* Mistake Intelligence
* Me/Profile
* full routing and navigation

The problem is not “missing product depth.”
The problem is **UI/UX drift from the locked baseline**:

* composition drift
* nav/sidebar drift
* screen-emphasis drift
* copy drift
* grouping/hierarchy drift
* flow drift

So your first job is **not implementation yet**.
Your first job is a **screen-by-screen gap analysis** of current codebase vs locked baseline.

## 2. What “mobile” means

For this task, “mobile” means:

**(a) the responsive mobile view of the existing web app**

It does **not** mean:

* a new Expo app
* a React Native build
* a separate mobile codebase in `artifacts/lazytopper-mobile`

So do all work inside the existing web app / current product codebase.
Do not create a separate mobile app.

## 3. Screenshot requirement

You are correct that you cannot safely match the Lovable baselines without visual reference.

So yes:

* baseline screenshots are required for exact parity work

I will provide screenshot packs for both locked baselines.

Required baseline screenshots:

* desktop landing / start
* desktop sidebar + main shell
* desktop Practice
* desktop Exam Trends
* desktop Check & Improve
* desktop Topic Hub
* desktop Worksheet flow
* desktop Me/Profile
* mobile landing / start
* mobile intent selection
* mobile Practice
* mobile Exam Trends
* mobile Check & Improve
* mobile Topic Hub
* mobile Worksheet flow
* mobile Me/Profile

Until screenshot pack is available, do **not** start baseline-parity implementation.

## 4. Current app status vs baseline

The current app is **functionally rich** but **not yet baseline-faithful**.

So this is neither:

* a tiny polish patch
  nor
* a total rewrite

Treat it as:

* **mid-sized UI/UX parity correction work**
* built on top of already-existing product features

Meaning:

* existing features mostly exist
* but the shell, composition, grouping, copy, and emphasis may not match the locked baseline yet

## 5. Approved delta list

As of now, there is **no special approved delta list** saying certain locked-baseline features were intentionally cut from implementation.

Default rule:

* the locked Lovable baselines are the target
* unless I explicitly approve an exception later

Also:

* do **not** mix gateway/login/payment work into this task
* do **not** mix solution-tightening / final-answer / duplicate-ID work into this task

Those are separate lanes.

## 6. Revised implementation approach

This is now a 2-phase task.

### Phase 1 — Gap analysis only

Do **not** implement yet.

Audit the current codebase against the locked baseline and return a precise report:

1. exact routes/pages currently corresponding to:

   * start / home
   * Practice
   * Exam Trends
   * Check & Improve
   * Topic Hub
   * Worksheet flow
   * Me/Profile

2. desktop shell assessment:

   * current sidebar composition
   * current workspace composition
   * where it differs from locked baseline

3. mobile assessment:

   * current responsive mobile flow
   * current screen grouping/hierarchy
   * where it differs from locked baseline

4. drift inventory:

   * copy drift
   * CTA order drift
   * feature visibility drift
   * grouping drift
   * section order drift
   * visual emphasis drift
   * nav/sidebar drift

5. implementation scope estimate:

   * pages/components that need correction
   * whether each correction is small / medium / large

### Phase 2 — Implementation

Only after I approve the gap-analysis report and provide screenshot pack.

## 7. Minimum-change rule clarification

The minimum-change rule still stands, but it applies to implementation method, not to ignoring real baseline gaps.

Meaning:

* do not touch unrelated files
* do not refactor opportunistically
* do not “clean up” adjacent UI
* but do fix every actual baseline-parity gap once approved

So:

* if gap is small, change few files
* if gap is broad, change more files
* but only where baseline-parity work truly requires it

## 8. What I want from you now

Do **Phase 1 only**.

Return:

1. exact files/pages/routes/components to inspect for baseline parity
2. current desktop shell/sidebar mapping
3. current mobile-responsive mapping
4. list of likely drift points vs locked baseline
5. list of screenshots still needed from me
6. a recommended implementation sequence after gap analysis

Do not start implementation yet.
Do not change files yet.
Do not create a new mobile app.
Do not mix in gateway or solution-tightening work.
