Good — this mobile analysis is directionally useful.

You have now confirmed the mobile baseline shell, nav shape, and light-theme family.

A few decisions are already settled, so remove them from the open-question list:

1. **Theme polarity is settled**

   * Implement the locked **light baseline**
   * Current dark theme is **not** an approved delta

2. **Typography is settled**

   * Align to the locked baseline typography family
   * Do not treat current typography as an approved exception

3. **Mobile means responsive web**

   * Do not build a separate native app
   * Mobile parity target is the responsive mobile view of the existing web implementation

Now I need the mobile gap analysis completed to execution-ready level.

## Complete the mobile analysis with these additions

For each of the 10 baseline mobile screens, return:

1. baseline screen name
2. current equivalent route/component(s), if any
3. whether current equivalent is:

   * exact
   * partial
   * overloaded
   * missing
4. drift type:

   * structural
   * copy
   * composition/layout
   * hierarchy/grouping
   * CTA order
   * feature visibility
   * navigation
5. files likely to be touched
6. implementation size:

   * small
   * medium
   * large

## The 10 mobile baseline screens to map fully

* landing
* intent
* practice
* worksheet
* worksheetReady
* checkImprove
* graded
* examTrends
* topicHub
* me

## Also answer these specifically

1. Which mobile baseline screens already come naturally from the planned `/app/*` desktop parity work?
2. Which mobile screens require dedicated mobile-only composition work?
3. Which screens need new page-level UI rather than responsive tweaks?
4. Which existing current components can be reused safely without causing drift?

## Important rule

Do not start implementation yet.
Do not change files yet.

I want a final combined parity build plan:

* desktop
* mobile
* shared foundation
* route strategy
* shell strategy
* page creation sequence

After that, implementation can begin.
