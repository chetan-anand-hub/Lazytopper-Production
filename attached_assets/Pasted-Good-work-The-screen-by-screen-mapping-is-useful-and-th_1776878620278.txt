Good work. The screen-by-screen mapping is useful and the reuse strategy is sensible.

I am approving the analysis direction, but **not approving the implementation plan as written**, because it overreaches beyond the mobile-only task.

## What is approved

Approved for this mobile baseline task:

* mobile parity inside the existing web app
* reuse of existing logic/services where safe
* new mobile-facing UI where baseline requires it
* correcting mobile bottom nav to:

  * Practice
  * Exam Trends
  * Me
* removing Learn from top-level mobile nav
* keeping Check & Improve primary but not as bottom-nav item
* building mobile versions of:

  * landing
  * intent
  * practice mode selector
  * worksheet
  * worksheetReady
  * checkImprove
  * graded
* responsive/composition corrections for:

  * examTrends
  * topicHub
  * me

## What is NOT approved in this task

Do not include the following in this mobile-only Phase 2 task:

* desktop AppShell work
* desktop sidebar work
* desktop shell strategy
* desktop workspace composition work
* new desktop Home implementation
* broader combined desktop+mobile parity program
* any implementation framed as “Final Combined Parity Build Plan”

## Important correction

This task remains **mobile baseline only**.

That means:

* no desktop parity implementation in this task
* no combined foundation plan unless a shared change is absolutely required for mobile parity
* if a shared file must be touched, keep it minimal and explicitly report any desktop side effect

## Scope correction for routes/pages

Use the mobile baseline inventory as the scope anchor:

* landing
* intent
* practice
* worksheet
* worksheetReady
* checkImprove
* graded
* examTrends
* topicHub
* me

Do not add extra baseline pages outside that set in this task.

Specifically:

* do not include `/app` Home work in this mobile-only implementation pass unless I explicitly approve it later

## Shared foundation rule

You may introduce only the **minimum shared plumbing required** for mobile parity.

Examples that may be acceptable if truly necessary:

* bottom-nav correction
* light-theme token additions needed by mobile screens
* typography loading needed by mobile screens
* minimal `/app/*` routing support for mobile-accessible flows

But:

* do not build the desktop shell now
* do not build desktop sidebar now
* do not build a full combined foundation layer beyond what mobile strictly needs

## Revised implementation plan required

Please revise the implementation plan into a **mobile-only execution plan** with these sections:

1. shared plumbing strictly required for mobile parity
2. new mobile pages/components to build
3. existing pages/components to adjust responsively
4. exact files to touch
5. exact routes to add/change
6. any unavoidable desktop side effects from shared changes

## Explicitly keep untouched

Continue to keep untouched:

* AI/engine files
* data service files
* auth/payment/admin systems
* PracticePage Q&A engine logic
* unrelated architecture cleanup

Return the revised mobile-only implementation plan before coding begins.
