You are implementing against a LOCKED UI/UX BASELINE for LazyTopper.

This is not a redesign task.
This is not a modernization task.
This is not a simplification task.
This is not a brainstorming task.

Your role is to implement the approved LazyTopper UI/UX exactly as defined by the locked Lovable baselines, with no visual or functional drift unless I explicitly approve a delta.

SOURCE OF TRUTH

Locked desktop baseline:
https://id-preview--e49fe255-d9b4-4d48-b708-31252b7f230a.lovable.app/

Locked mobile baseline:
https://id-preview--18842e8f-6c4b-4505-9eeb-0747e3a33c5a.lovable.app/

Treat both baselines as frozen source-of-truth references for cycle 1.

WHAT IS LOCKED

You must preserve the approved baseline in:
- structure
- flow
- copy/text
- hierarchy
- layout/composition
- section grouping
- navigation
- sidebar composition
- CTA order
- feature visibility
- visual emphasis
- overall density and presentation

This includes preserving:
- page/screen order
- left sidebar composition on desktop
- workspace composition on desktop
- mobile screen order and grouping
- headings
- labels
- card titles
- button text
- helper text
- section order
- product architecture

PRODUCT GUARDRAILS THAT MUST STAY INTACT

- LazyTopper is choice-first, not plan-first.
- Top-level starting intents must remain:
  - Practice
  - Exam Trends
  - Check & Improve
- Learn is not a top-level destination.
- Learn must remain inside Topic Hub.
- Mistake Intelligence is a major product pillar and must remain visible wherever it exists in the locked baseline.
- Worksheet flow must remain visible and intact.
- Me/Profile must remain present and must not become a dead-end.

NON-NEGOTIABLE DO-NOT-CHANGE RULES

Do not:
- redesign anything
- modernize anything
- simplify anything
- improve UX on your own
- rename copy
- move sections
- change screen order
- change CTA order
- change navigation structure
- change sidebar composition
- change feature visibility
- alter visual emphasis
- hide Mistake Intelligence
- move Learn out of Topic Hub
- weaken worksheet flow
- change the product architecture
- invent a new desktop shell
- compress or reinterpret mobile screens

DESKTOP ENFORCEMENT

Desktop implementation must:
- match the locked desktop baseline’s left sidebar composition
- match the locked desktop baseline’s workspace composition
- match the locked desktop baseline’s section grouping
- match the locked desktop baseline’s hierarchy and emphasis
- preserve the same perceived desktop shell and information architecture

Do not introduce a new desktop layout system, alternate sidebar logic, or “cleaned up” shell.

MOBILE ENFORCEMENT

Mobile implementation must:
- match the locked mobile baseline’s screen order
- match the locked mobile baseline’s hierarchy
- match the locked mobile baseline’s grouping
- match the locked mobile baseline’s composition
- preserve the same perceived flow and emphasis

Do not compress, merge, reinterpret, or “optimize” mobile screens beyond narrow responsive fitting.

ALLOWED TOLERANCE

Only the following narrow tolerance is allowed:
- responsive fitting
- tiny CSS/rendering differences
- technical substitutions that preserve the same perceived experience

Any meaningful deviation in structure, wording, grouping, emphasis, flow, or feature visibility counts as drift.

DRIFT DEFINITION

The following count as drift unless explicitly approved:
- renamed headings, labels, buttons, cards, or helper text
- changed CTA order
- hidden or merged features
- moved sections
- altered hierarchy
- changed nav/sidebar composition
- changed visual emphasis
- layout changes that materially change meaning
- helpful simplification
- modernization
- self-directed UX improvement

IMPLEMENTATION RULE

When there is any conflict between implementation convenience and baseline fidelity, baseline fidelity wins.

SEPARATE LANES — DO NOT MIX

Do not mix this task with:
- gateway/login/payment work
- solution-tightening audits
- final-answer audits
- duplicate-ID audits
- unrelated architecture cleanup

Only implement the locked UI/UX baseline.

REQUIRED OUTPUT FORMAT

When you finish, return all of the following:

1. Exact files changed
2. Exact routes touched
3. Exact components touched
4. Exact screens affected
5. Desktop parity summary:
   - what was matched
   - how sidebar/workspace/grouping were preserved
6. Mobile parity summary:
   - what was matched
   - how screen order/grouping/hierarchy were preserved
7. Explicit list of what was intentionally unchanged
8. Explicit statement confirming:
   - no silent redesign was introduced
   - no drift was introduced outside approved delta
   - copy parity was preserved or list every approved exception
   - nav/sidebar parity was preserved or list every approved exception
   - CTA order parity was preserved or list every approved exception
   - feature visibility parity was preserved or list every approved exception

If anything cannot match the locked baseline exactly, do not improvise. Flag the mismatch explicitly and stop treating it as an invisible implementation choice.