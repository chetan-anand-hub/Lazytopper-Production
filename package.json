{
  "packageManager": "pnpm@10.32.1",
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm --filter @workspace/scripts run syllabusGuard && pnpm --filter @workspace/scripts run validateQuestionBanks && pnpm --filter @workspace/api-server --filter lazytopper run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter @workspace/api-server --filter \"./scripts\" --if-present run typecheck",
    "validate:banks": "pnpm --filter @workspace/scripts run validate:banks",
    "check:coverage": "node scripts/checkQuestionBankCoverage.mjs",
    "deploy:firestore-rules": "node scripts/deploy-firestore-rules.mjs",
    "test:firestore-rules": "firebase emulators:exec --only firestore --project demo-lazytopper-rules \"node --test firestore-rules-tests/subscriptions.rules.test.mjs\""
  },
  "private": true,
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase": "^12.17.1",
    "firebase-tools": "^15.27.0",
    "prettier": "^3.9.6",
    "typescript": "~5.9.2"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.117.1"
  }
}
