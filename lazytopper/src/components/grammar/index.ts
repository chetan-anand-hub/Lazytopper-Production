/**
 * Shared responsive grammar primitives. Reusable building blocks that carry the
 * LazyTopper desktop design grammar and a consistent desktop→mobile reflow, so
 * page reflows (PR B+) cannot drift. Wired into no page yet.
 */
export { Card } from "./Card";
export { TileRow } from "./TileRow";
export { Pill } from "./Pill";
export type { PillTone } from "./Pill";
export { SectionHeader } from "./SectionHeader";
