API_KEY=YOUR_GEMINI_KEY_HERE
PORT=3001
# C&I PR-3 (Gate 2b): comma-separated Firebase uid allowlist for the admin
# solution-cache endpoints (/api/admin/solution-cache/evict|regenerate).
# Unset = those endpoints are disabled (fail-closed).
ADMIN_FIREBASE_UIDS=
