# LazyTopper — SURFACE TRACKER (the road to soft launch)
**Home:** this file lives at `handoff/SURFACE_TRACKER.md` in the repo (version-controlled, beside CURRENT_STATE). **Trunk at last sync:** `203fb370` (**#601-#604 - WAVE 5C, four PRs / four lanes under a controller + subagent model**: #601 replaced the `App.tsx` blanket ban with targeted routing tests in BOTH overlay gates, where the lane's OWN first-draft guard was a SILENT NO-OP - `not.toThrow()` passed under a nested `<MemoryRouter>` because `App.tsx` wraps `<Routes>` in an `<ErrorBoundary>`, so the app ERROR-PAGES instead of crashing and a throw-assertion cannot tell the difference, caught only by mutation - and which also found QP-OVL's GUARD 3 was a COMMENT, not a check, leaving the propless-`/practice` invariant with ZERO executable coverage repo-wide; #602 shipped the VISIBLE paywall layer (the locked CTA that OPENS THE SHEET rather than doing nothing), disproving its spec's parent list - the four named parents are `useSubscription` CONSUMERS, not `SolutionChecker` render sites, of which there are TWO and none of the four is one - and DECLINING to build the `entitled` prop because a prop CREATES the mount-not-live risk it was meant to solve, since a third render site added later ships un-gated by default; #603 cut 40 defective Section A questions after refuting its own brief WITH A CONTROL - the packs were ALREADY LIVE, so the defective solutions were reaching students, not latent - and then overturned the owner's own remedy by adjudicating all 79 questions to show 48 sound and the defect 100% confined to Section A, separable by a MECHANICAL `section` filter, root cause a solution pool MIS-PAIRED against a question pool (`ME-E07`/`ME-E14` swapped); #604 split the CI gate without trimming it, retaining the full bar deliberately for the wave-closing handoff because a product PR's CI runs against ITS OWN BASE and that docs run is the project's ONLY integration check, and proving `scope:guard` CANNOT go in a CI fast path because it reads the WORKING TREE, so a clean CI checkout measures `inspected=0` and passes forever. THE WAVE'S SUBJECT: EVERY LANE DISPROVED PART OF ITS OWN SPEC, and the three spec errors were the cofounder's, not the lanes'. CONTROLLER ERRORS RECORDED: a non-existent doctrine contradiction reported and retracted; BANK-1 traffic MISROUTED TO CI-DOCS TWICE, the second time carrying an instruction to delete files CI-DOCS was forbidden to touch - IT REFUSED AND PROVED THE MISROUTE, and only the receiving lane stood between that and a destructive cross-lane write; and `[FU-GATE3-SIGNED-OUT-GRADING-FAILS-OPEN]` amplified as a live product hole when it is LATENT and curl-only.) Previous sync: `59ba4da2` (**#579–#582 — WAVE 5A, four PRs / five lanes under a controller + subagent model**: #580 closed the 🛑 P0 by enforcing entitlement on the SERVER for the paid AI endpoints — where ★★ **the spec's §1 was incomplete in a way that would have left the P0 OPEN, because the effective tier is `applyExpiry(repairInterruptedTrial(...))` and NOT the stored `tier` field**, so reading the raw field would have served **every expired trial** *and* **locked out every mid-trial student** at once, and would have re-opened Wave 4's #574 P0 on the server side; ★ its 402 was proven by a **real HTTP POST against the real `index.cjs`** with firebase-admin swapped before `require`, so **no test seam entered production code**, and **denials are never cached** so an upgrade is honoured on the next call; its GATE-1b wiring commit then showed ★★ **43 passing tests that CI had never executed — a grep returning ZERO, with a control on an already-wired sibling in the same before-log**, which is the difference between *"the count changed"* and *"the count changed BECAUSE OF MY EDIT"*; #582 rebuilt the auth door as **one door with a blocking email verification**, ★★ **declining its own spec's §6 because "reduce `SignUpPage` to a thin render" would have DELETED the product's only name capture**, forced deleting 7 guard tests and re-opened PR-B2's one-way-door defect — `App.tsx` diff **ZERO**, **0 of 25 `vi.mock` factories touched**, and ★★ **16 screenshots found two PRE-EXISTING contrast defects invisible to every assertion in the repo** (white-on-white input text; the primary Google button measured `rgb(248,250,252)` on `rgb(255,255,255)`); #581 replaced the `SolutionChecker` blanket ban with targeted tests, finding ★★ **the ban was the ENTIRE protection — nothing anywhere asserted one line of that component's behaviour** — a **strict increase**, not a trade, with 19 mutations ALL RED and ★★★ **M2b proving that renaming a CSS class made a negative assertion go VACUOUS and still pass, caught only by the POSITIVE CONTROL**; and #579 added Dependabot + CodeQL, which ★★ **proved its own spec wrong in BOTH directions** — secret scanning and push protection were **already enabled**, while **Dependabot ALERTS and SECURITY UPDATES** are the ones actually off ⇒ ⚠⚠ **`dependabot.yml` configures VERSION updates only, so #579 delivers routine bumps and NO vulnerability response at all**, and **must not be recorded as closing the supply-chain item.** 🛑🛑 **THE WAVE'S SUBJECT: `MERGED` IS NOT `DEPLOYED`.** GATE-1 merged, **Vercel never built it and Railway did**, and for ~2 hours the server enforced a rule the client had no code to explain — a free student saw the raw string `premium_required` in red, **the exact defect the merged, green, CORRECT §3D was written to prevent.** Caught by **fetching the live bundle and grepping it** (absent from all 65 chunks). ⇒ **two deploy targets build independently from one trunk; a merge confirms neither; confirm by asking the running system, not a dashboard** — ⚠ **Vercel "Redeploy" rebuilds the ORIGINAL commit, not the branch tip**, and branch protection makes **a Deploy Hook the only manual trigger.** ★★ **Its generalisation: every lane proved "a green CI run is evidence only about what it EXECUTED"; this proves the sequel — a merge is evidence only about the REPOSITORY.** ✅ **BOTH owner live-verifies are DONE** — the paywall by a three-way production discrimination with an account-specific `trialEndedAt` proving the gate really read Firestore, and the door end-to-end including that a wrong password creates **no second account**. 🛑 **TWO LAUNCH-BLOCKING items are OPEN and neither was visible to any gate** — the upgrade modal **sells four RETIRED surfaces to a student being asked ₹599** (fix belongs in GATE-2's spec, not a separate lane), and the verification mail **lands in Spam on a BLOCKING gate** (owner/DNS work, not an agent lane).) Previous sync: `fcdbfa65` (**#566–#575 — WAVE 4, ten PRs under a controller + subagent model**: #566 stated the founding offer on the sign-in surface — where ★ **the load-bearing part was the MOBILE MIRROR**, because the live `Login.tsx` hides the entire brand panel below 1024px while the prototype's split collapses to one column, so the spec's fallback **would have been invisible to every phone student**, and ★★ *the prototype is not the product — it is authoritative when a lane INVENTS visual language and wrong when a lane must MATCH it*; #567 wrote the `MONTHLY_INLINE` hard gate into `NEXT_ACTION.md`, finding ★ **a second, independent way the trap fires — `pricing.ts`'s own doc comment on that constant claims "this one line is the entire switch" and it is FALSE**; #568 restored GUARD-2, wiring two acceptance suites that CI had **never run** and fixing a boundary auditor that kept a **stale private copy of `classifyFile` missing four lanes** — ★ *the blind spot did not merely hide the stale copy, it hid its magnitude: 158 tracked files unclassified* — and its log now **names its subject**, `tracked=1715 outside_anchor=334`; #569 gave a zero-attempt student a first-session start card on both Home pages with ★ **TRI-STATE hydration, never boolean**, so the card can appear only after a completed read returning zero rows, and ★★ *a test proves the code works; a CHUNK proves it ships* — the CI log carried both the suite line and the emitted `FirstSession` asset; #570 cleared **616 legacy mojibake lines in `handoff/` down to 8**, and ★★ **its first blanket pass destroyed 8 DELIBERATE SPECIMENS quoted inside lessons about mojibake — the lesson was destroyed by the repair — and the same agent caught and reversed it before pushing**; #571 swept the cwd-frame blind spot out of every ops gate and made `check:mojibake` **SCOPED**, where ★★★ **it was told to write an ENFORCE-list and wrote a one-entry EXEMPT list instead, so a new top-level tree defaults to ENFORCED — it fails safe** — under the ruling that ★★ *mojibake is a DEFECT in product text and a legitimate SUBJECT in documentation about mojibake, and a gate that cannot tell those apart is not detecting a bug, it is banning a character*; #572 corrected `CLAUDE.md`'s stale suite count in **exactly one line**, where ★★ **the file already contradicted itself fifteen lines apart and the correct line was documenting its own past staleness**; #573 merged the duplicate Home cards, removed a redundant mobile "Signed in" pill and deleted a search placeholder that **lied about what it returns**, and ★★★ **caught its own predecessor faking evidence — the prior session's "with-attempts" screenshots were the same state photographed twice**; #574 fixed a 🛑 **launch-blocking P0 in which every new student's trial downgraded to free during its own activation** — `serverTimestamp()` is a sentinel and `snap.data()` materialised it as `null`, so ★★ *the defect was the CLIENT evaluating a correct rule before the proof could exist, and no rules test could ever have caught it*; and #575 fixed a 🛑 **live mobile crash (React #310)** caused by a `useState` below an early return, where ★★ **the real variable was PERSISTED SESSION, not email vs phone and not viewport** — *"a student with cleared site data never crossed it … that is why clearing site data 'fixed' it and why 1082 green tests never saw it: every one of them starts from clean state."* ★★ **THE WAVE'S SUBJECT IS ONE FINDING WITH FOUR INSTANCES:** `check:mojibake` blind to `handoff/`, two acceptance suites CI never ran, `react-hooks/rules-of-hooks` reporting "0 violations" while running nothing, and a GitHub branch ruleset that was Active with "Block force pushes" enabled and **exempted the only person who could trigger it** ⇒ **a check that cannot be shown to have looked, and to be capable of failing, is not coverage — it is the APPEARANCE of coverage, which is worse, because it stops anyone looking**, and **a protection with a bypass is only as strong as its bypass list.** ⚠ **A force push from the shared checkout dropped #564 and #565 mid-wave**; #565 re-landed as #568 and **#564's content was restored in full by #566's squash, verified whole on trunk.** ⚠ **TWO owner live-verifies are OWED**, and for #575 only one path proves it — mobile `/browse` as a returning student **without clearing site data**.) Previous sync: `25e995a7` (**#557–#563 — WAVE 3, four lanes under a controller + subagent model**: #557 put a one-time phone-linking nudge on both Home pages — ★★ after proving the returning-session signal the spec assumed existed **does not exist**, by enumerating ~170 `lazytopper.*` keys and tracing every candidate to its **writer**; #558 replaced the grader's blanket FORBIDDEN ban with 32 targeted tests in **both** gates (`checkSolution.cjs` byte-identical to trunk), because ★ *a lift verified in only one gate leaves the other silently protecting nothing*, and ★★ its one mutation that ran GREEN turned out to be **the mutation's bug, not the test's** — the anchor exists twice at different indentation and patched the wrong near-duplicate grader; #559 constrained grader output with `responseSchema` — **THREE parsers ⇒ THREE schemas**, with Schema C deliberately not requiring `annotatedSteps` because reusing Schema A **would have forced the model to fabricate steps for an unreadable answer**, and `mistakeType` **nullable with no enum** because a forced value would have made **Mistake Intelligence learn from noise** — owner LIVE-VERIFIED, including the null path; #560 made `scope:guard` see the whole repo — it ran with `cwd=lazytopper`, so an entire **new top-level directory was invisible**, and ★ *a self-check asserted against its own input is a tautology*; its new zero-match meta-assertion immediately caught a ceiling check **passing with count = 0**; #561 + #563 closed **all three** client-side routes to free premium — Firestore, localStorage and a **forged trial** nobody had counted — under one principle: ★★ **entitlement must derive from data the client cannot forge**, so the trial's START is a server timestamp the rules pin and its END is **derived, never stored**; #562 deleted the retired `pages/Home.tsx` and the dead AI learning path, where ★★ *a deadness analysis that enumerates only the fixtures you expected is not an enumeration* — a pricing guard asserted the price walk **reaches** that file and would have gone CI-red. ★★ **THE WAVE'S LESSON SITS BELOW EVERY GATE THIS PROJECT OWNS:** merging #561 did **not** deploy it — a `firestore:rules` deploy from the shared checkout re-shipped **six-commit-stale** rules and printed `Deploy complete!`; **the two-word difference between `skipping upload` and `uploading rules` is the whole signal**, and a lane whose outcome depends on a deploy now **closes on the DEPLOYED state, never the MERGED state**. Rules are deployed and **read back from the Firebase Console**. ⚠ **The honest limit:** the API server checks **rate limits, not plan**, so paid features are protected **IN THE UI** — which is not the claim “paid features are protected.” **ONE owner live-verify remains OWED** — `hasPhoneLinked` against a real phone-linked account.) Previous sync: `e8b15735` (**#546–#552 - WAVE 2, LANES A + B**: #546 closed the api-server front door — a bare `app.use(cors())` in front of both entry points on the service Railway actually runs — with a **missing Origin allowed unconditionally**, because the Vercel rewrite is server-side and real production traffic arrives without one; ★★ **a CORS refusal is header OMISSION, not a block**, so `cb(null,false)` still returns 200 and it is `cb(new Error())` that 500s every caller; ★★ **an unobservable rule is untestable** — `cb(null,true)` with no origin to reflect emits no header at all, byte-identical to a refusal, so the no-Origin case answers `"*"`; helmet with CSP off (it governs nothing where no document is served) and **HSTS off**, measured at a full year with `includeSubDomains` and pinnable onto the apex domain through the rewrite; ★ **it also added the first CI gates `artifacts/api-server` has ever had** — before it, the only thing catching a type error in the production front door was the Railway deploy. #547 restored a grader protection that had **never been in force**: both C&I gates listed `checkSolution.cjs` without the `lazytopper/` prefix against an exact-membership check fed repo-root-relative paths, ★★ **proven by CONTROL CASE** — a commit that really did modify the grader passed both gates printing `ok FORBIDDEN: ... shows zero changes` at 91/91 and 31/31, so the gate did not fail to notice, it *asserted the file was untouched*; ★★ **`FORBIDDEN(wired)` proves MEMBERSHIP, not MATCHABILITY**, and the new filesystem-only `FORBIDDEN(path)` loop cannot skip. #549 gave the token telemetry a reader — `snapshot()` and `getTokenTelemetry()` had **no caller anywhere in the repo**, so #540 measured into a void from the day it merged; the endpoint reports the rate-limit counters **including `anonKey`** because a diagnostic with no reader is not a diagnostic, and ★★ **its own test caught a real bug**: reusing the Gemini call-class list for the limiter aggregation silently dropped `rate_limit.hard_block.anonymous`, the signed-out lockout counter, i.e. exactly the number the diagnostic exists to surface. #548/#550/#551 published founding+list pricing from one constant, made the sign-up name REQUIRED (the one-way door), and made phone sign-up reachable — where ★★ **a fix that cannot be shown to change behaviour is not a fix**: the brief's "use a distinct container id" would have done nothing, because `initPhoneRecaptcha` early-returned and ignored the container argument entirely. #552 was a **★ LAUNCH BLOCKER** — the client never sent `X-Lazytopper-Uid`, so every signed-in student was rate-limited as anonymous at 3/day, sharing one bucket with everyone behind the same IP; ★★ **it defeated four PRs of rate limiting that were each individually green, because the gap was BETWEEN two correct halves**, and ★★ **it was a RECURRENCE** of `[FU-XUSERID-PROXY-STRIP]`, fixed in `dbSyncService` only and closed. **★ `[FU-TELEMETRY-NO-READ-PATH]` is CLOSED and the efficiency tier is UNBLOCKED.** **FOUR owner live-verifies are OWED** — see `NEXT_ACTION.md`; A3 and B4 are settled by a single curl.)

---

## ⚠ 2026-08-06 — #623 `NAME-2`. THIS FILE'S `Trunk at last sync` LINE IS STILL STALE, AND IS STILL DELIBERATELY NOT UPDATED.

**Re-derived: trunk is `2ca9a3d0`. The header at the top of this file still reads `203fb370`
(Wave 5C) — now several handoffs behind.**

★ **Left as it stands, for the same reason as last time.** Advancing a sync header over content
nobody has re-reviewed asserts that everything below has been reconciled against the current trunk,
and it has not. The header would then be evidence of a review that never happened. **The lag is
recorded here instead**, where a reader meets it before trusting the matrix. Reconciling this file is
a job of its own; it is not a line-item in a docs PR.

**#623: NO CORE-MATRIX COMPLETION CELL CHANGES, and NO Scope cell changes.** Stated explicitly per
§10 — **silence is not an answer.** The lane moved one surface that is already ✅ Built / Verified, so
no cell moves. What changed, recorded for shape rather than state:

- **Login / Auth (`#623`)** — **the phone step now captures a name.** The same self-declared
  segmented control the email step got at `#616`, reusing `.lt-login-seg` rather than a second rule
  set (`role="group"`, "I'm new here" pre-selected, `data-testid="lt-phone-mode"`), a required
  `Your name` field on the create branch only, and per-branch copy replacing the retired *"phone
  works the same either way"* line — which stopped being true the moment the step began asking.
  **Owner LIVE-VERIFIED on a real handset**, including a **second sign-in on the same number** proving
  the no-overwrite guard fires against a live Firebase record.
  ★ **The surface's nameless-account defect is now closed on ALL THREE methods** — Google supplies a
  name, `#616` closed email, `#623` closes phone. **`[FU-AUTH-PHONE-DISPLAYNAME-NEVER-SET]` is
  CLOSED for new accounts.**
  ⚠ **The surface is STILL not complete, and the remaining gaps are unchanged by this lane:**
  **nothing backfills accounts already created without a name** (`[FU-AUTH-DISPLAYNAME-NO-BACKFILL]`
  — ★ a sequencing constraint on the ~50-student QA pass), and **a phone-first student still cannot
  add an email**, so one who later signs in with Google gets a second account
  (`[FU-AUTH-EMAIL-LINK-DIRECTION]`, `AUTH-1`'s, runs alone, **split accounts unrecoverable by
  design**). Recorded as honest limits on what shipped, not as regressions.

**No Scope cell changes:** Login is `Locked`, and `#623` added **no new necessary piece for its own
definition-of-done** — it delivered a piece already named and queued as `NAME-2` in the previous
handoff. **No scope was discovered by this lane.**

---

## ⚠ 2026-08-06 — #615-#616. THIS FILE'S `Trunk at last sync` LINE IS STALE, AND IS DELIBERATELY NOT UPDATED.

**Re-derived: trunk is `1b477e5f`. The header above still reads `203fb370` (Wave 5C).**

★ **It is left as it stands on purpose.** Advancing a sync header over content nobody has
re-reviewed would assert that everything below has been reconciled against the current trunk, and it
has not — the header would then be evidence of a review that never happened. **The lag is recorded
here instead**, where a reader meets it before trusting the matrix. Reconciling this file is a job of
its own; it is not a line-item in a docs PR.

**#615-#616: NO CORE-MATRIX COMPLETION CELL CHANGES, and NO Scope cell changes.** Stated explicitly
per §10 — silence is not an answer. **Both lanes moved surfaces that are already ✅ Built / Verified,
so no cell moves.** What changed, recorded for shape rather than state:

- **Login / Auth (`#616`)** — the door now runs on a **self-declared branch**: a segmented control on
  the email step (`role="group"`, "I'm new here" pre-selected), so the page never has to infer who is
  new and each branch makes **one** call reporting on the **first** submit. The **offer block merged
  and moved into the brand column** (with a price-free compact mirror below 1024px), and **legal sits
  in the auth column at every width** — a desktop brand-column mount was tried and **reversed on
  live-verify because it fell below the fold**. Forgot-password is confined to the returning branch.
  **Owner LIVE-VERIFIED on production, desktop and mobile.**
  ⚠ **The surface is NOT complete for phone:** `§9` was not started, so **phone-first students still
  land nameless** (`[FU-AUTH-PHONE-DISPLAYNAME-NEVER-SET]`), and **nothing backfills accounts already
  created without a name** (`[FU-AUTH-DISPLAYNAME-NO-BACKFILL]`). Recorded as honest limits on what
  shipped, not as regressions.

- **SEO / crawler surface (`#615`)** — **the sitemap now lists exactly three URLs**: `/app/`,
  `/app/exam-trends`, `/app/pricing`. The four-URL version advertised `/topic-hub`,
  `/highly-probable/10/Maths` and `/practice/10/Maths`, all auth- or premium-gated and **all 404 at
  the root**. Canonical moved to `https://www.lazytopper.com/app/`, **the only URL that resolves** —
  measured `308 → 307 → 200`. ⚠ **The domain still has no homepage of its own**
  (`[FU-SEO-ROOT-IS-A-REDIRECT]`); that is a shape decision this lane did not make.

---

**#579–#582: NO CORE-MATRIX COMPLETION CELL CHANGES, and NO Scope cell changes.** Stated explicitly per §10 — silence is not an answer, and this is a verified statement rather than an omission. **Surfaces this wave MOVED, none of them in build state:** **Login / Auth** was rebuilt into **one door with a blocking email verification** (#582) — a behaviour and flow change on a surface already ✅ Built/Redesigned/Desktop/Mobile/Verified, so **no cell moves**; **owner LIVE-VERIFIED end to end** (Google and phone show no gate; a fresh email gets the gate, the mail and entry; a wrong password shows the right copy and creates **no second account**). ⚠ Two pre-existing contrast defects on this surface were **found by screenshot and fixed in the same PR** — they were invisible to every assertion in the repo, which is a fact about the surface's coverage, not about its build state. ⚠ **The verification gate is PAGE-SCOPED, not app-wide** — a restored session bypasses it (`[FU-AUTH-VERIFY-NOT-APP-WIDE]`), and `/login` can still create nameless accounts (`[FU-AUTH-NAME-PROMPT]`); **both are recorded as honest limits on what shipped, not as regressions.** **Check & Improve** (#580) now shows a **proper gate rather than an error** for a student without entitlement — a defect repair inside a shipped surface, **owner-confirmed on production**; Practice, Exam Trends, the bank and Progress were verified **untouched**, and "Show steps" is still free. **Pricing / the paywall** — ⚠⚠ **this is where scope was DISCOVERED, and it is launch-blocking**: the "Choose a Plan" modal sells **Smart Study Planner, Daily Focus Mix, Full Analytics Dashboard** and Chapter Hub, and **three of those are RETIRED SURFACES** ⇒ **the paywall is selling four things the product no longer ships, to a student being asked ₹599.** It also offers **only "Choose Plan" and no "Keep using Basic"**, and renders correct copy in an **error-red box** — *a locked feature is not a mistake the student made.* **Ops gates, supply chain and CI wiring** (#579, #581, GATE-1b) — **tooling and infrastructure, not a student surface.** **MI cell unmoved on every row** — nothing this wave changed what MI is or says, and ⚠ **the pre-existing `CLAUDE.md` §5 conflict remains open and is NOT introduced by this wave.** **No Scope cell changes:** every surface the discovered scope touches (**Pricing**, **Check & Improve**, **Login**) is either **already `Settling`** or, in Login's case, `Locked` with **no new necessary piece for its own definition-of-done** — the paywall copy belongs to **Pricing**, which is already `Settling`. See §2a.


**#566–#575: NO CORE-MATRIX COMPLETION CELL CHANGES, and NO Scope cell changes.** Stated explicitly per §10 — silence is not an answer, and this is a verified statement rather than an omission. **Surfaces this wave MOVED, none of them in build state:** **Auth / Login** gained the founding-offer strip plus an after-trial line (#566), rendered on desktop **and** mirrored on mobile from one shared constant — *behaviour on a surface already ✅ Built/Verified*, so no cell moves; **owner LIVE-VERIFIED at desktop and phone.** ⚠ `/app/sign-up` renders a **different component** (`SignUpPage`), so the strip is **absent there until the AUTH-3 one-door lane lands** — expected, and recorded here so nobody reads it as a regression. **Home — Desktop and Mobile** gained a zero-attempt `FirstSession` start card (#569, **owner LIVE-VERIFIED**: fresh account → card appeared → graded one answer → card vanished), then #573 **merged it into one slot with the MI card**, deleted a redundant mobile "Signed in" pill and removed a search placeholder reading *"Search topics, chapters, questions…"* that **could return none of those three**. ★ **The card count is pinned at exactly one by four tests and its POSITION by two more**, so *"replaced the empty state"* is proven rather than *"one card exists."* All of this is behaviour on surfaces already ✅ Built/Redesigned/Verified. **Mobile account menu** (#575) — a hook-order fix; **student-visible only in that `/app/browse` stops crashing for returning students**, which is a defect repair inside a shipped surface. **Premium / trial gating** (#574) — a returning-to-correct fix: **the 7-day trial now actually activates**, which is the behaviour the surface always claimed. **Infrastructure and security, not a student surface**, and invisible to a student for whom it already worked. **MI cell unmoved on every row** — #573 changed **where** the MI card is mounted (into the shared `FirstSession` slot via a `fallback` prop, **zero new state**) and **not what MI is or says**; the doctrine that **MI is navy-sidebar chrome only** is untouched, and ⚠ **the pre-existing conflict remains open and is NOT introduced by this wave: `CLAUDE.md` §5 says MI is sidebar chrome ONLY, while trunk renders an MI card in BOTH Home page bodies.** Reported, not resolved — **owner's call.** **Scope WAS discovered** — the retention answer (`[FU-RETENTION-ALREADY-MINIMAL]`, and the **PDF-at-grading-time** gap that runs the other way), the DPDP deletion/export question, and the BATCH-1 blast-radius correction (**four live surfaces on the batch path, including C&I — not three on a path being left alone**). **No Scope cell changes:** every surface that scope touches (**Check & Improve**, **Home**, **Pricing**) is **already `Settling`**, and the remainder (ops-gate wiring, git/branch protection, cost modelling, legal) is **tooling, infrastructure and commercial mechanism — not a student surface's definition-of-done.**

**#557–#563: NO CORE-MATRIX COMPLETION CELL CHANGES, and NO Scope cell changes.** Stated explicitly per §10 — silence is not an answer. **Surfaces this wave MOVED, none of them in build state:** **Home — Desktop (`pages/desktop/DesktopHome.tsx`) and Mobile (`pages/app/MobileHome.tsx`)** now render `<LinkPhoneNudge />` (#557), and a per-browser returning-visit counter exists for the first time; EV-1's screenshots at 390/768/1024/1440 show **no layout defect and no residual gap when dismissed**, and ★ `spaced` is a **prop, not a wrapper div**, because an always-rendered wrapper would push every student's layout down even when the nudge renders null. This is *behaviour on a surface already ✅ Built/Redesigned/Verified*, so no cell moves. **Auth — sign-in method linking** gained a component and 14 tests; the modal's lifetime is deliberately **not** tied to the nudge's visibility, or a successful link would unmount the confirmation mid-success. **Check & Improve — grading server:** the protection **changed form** (#558: blanket ban → 32 targeted tests + asserted replacements in both gates; convergence 97→100/100, overlay 34→36/36) and the model's output is now **schema-constrained** (#559). The student-visible surface is unchanged; the grader file itself was byte-identical to trunk at #558. **Premium / trial gating** is now enforced by Firestore rules rather than by client trust (#561, #563) — **infrastructure and security, not a student surface**, and deliberately invisible to a legitimate student: a real 7-day trial behaves exactly as before, which is what the fresh-student live-verify confirmed. **Login** — #562 corrected two stale reCAPTCHA comments; **comments only, zero behaviour**. **The retired `pages/Home.tsx` was deleted** (#562) — an unrouted file that was never a matrix surface; the live Home surfaces are `DesktopHome`/`MobileHome` and are untouched by that deletion. **MI cell unmoved on every row** — nothing in the wave touched Mistake Intelligence, and #559's nullable-`mistakeType` decision exists precisely to keep MI from being fed invented values. **Scope WAS discovered** — five items, logged in `DECISION_LOG.md` and in §2a below. **No Scope cell changes, and that is a verified statement rather than an omission:** every surface the discovered scope touches (**Check & Improve**, **Home**, **Pricing**) is **already `Settling`**, and the remaining discoveries (ops-gate wiring, the deploy layer) are **tooling and infrastructure, not a student surface's definition-of-done**.

**#546–#552: NO CORE-MATRIX COMPLETION CELL CHANGES, and NO Scope cell changes.** Stated explicitly per §10. The wave was edge hardening, gate repair, instrumentation read-out, pricing copy, and two auth-surface reachability fixes — not surface build state. Surface-by-surface: **api-server edge** — #546 is infrastructure, not a student surface. **CI gates** — #547 repaired a guard; no student-visible behaviour. **Admin/ops** — #549 added an owner-only endpoint deliberately absent from the browser CORS preflight list; not a student surface. **Pricing page** — #548 changed published figures and one FAQ sentence; the surface was already Built/Verified and does not move. **Auth / Sign-up** — #550 adds a required name field and #551 makes an already-live phone path reachable; both are *behaviour* on a surface already Built, so no completion cell moves, but they are the wave's only student-visible changes and are recorded here for discoverability. **Rate limiting** — #552 corrects caller identity; invisible until a student hits a cap, at which point it is the difference between 3/day and their real allowance. **MI cell unmoved on every row** — nothing in the wave touched Mistake Intelligence. **Scope discovered** (`[FU-PRICING-FOUNDING-COHORT]` — a published promise the product cannot enforce; nothing counts subscribers or closes the offer at 200) is logged in `OPEN_QUESTIONS_AND_FOLLOWUPS.md` + `NEXT_ACTION.md`; it does not move a Scope cell, because it concerns a commercial mechanism rather than a student surface's definition-of-done.

Previous sync: `1013daa7` (**#538–#540 - LANE H**: #538 forgot-password recovery, **enumeration-safe by ALLOWLIST** — `RESET_SURFACEABLE_ERRORS` admits only request-shaped failures so every other code, *including ones Firebase has not shipped yet*, falls through to one neutral notice; it fails SAFE where a denylist would not, and it is now the repo's reference implementation. **Owner LIVE-VERIFIED end to end.** #539 published ₹599/month + ₹4,999/board year from a single `src/config/pricing.ts` — the real finding was that the price lived in **five places across four files, two of them live and route-mounted** (`PracticeLimitGate:90`, `MockViewGate:189`), so a student would have seen ₹599 on `/pricing` and ₹149 at the moment of upgrade intent; the no-literal guard checks **two shapes**, because the most dangerous instance (`price: "149"` in JSON-LD) carries no rupee sign at all. #540 instrumented Gemini token/thinking usage **per handler function, not per source file** — `checkSolution.cjs` holds three endpoints across two billing classes, so a file-level map would have corrupted the very cost numbers the lane exists to produce. **★ The efficiency tier is gated behind `[FU-TELEMETRY-NO-READ-PATH]`** — #540 measures but nothing reads it. See `handoff/LazyTopper_Cost_Pricing_Analysis_v1_1.md`, committed with this update.) Previous sync: `7185c5f` (**#531–#535 - LAUNCH-BLOCKER WAVE + DEAD-PAGE SWEEP**: #531 `/sign-up` post-auth redirect guarded (`isSafeInternalPath`, defence-in-depth not a live exploit); #532 legal reachable (Lane C); #533 `Welcome.tsx:1867` "Last 5 years pattern" → "Board paper pattern" (text-only; the caption labels a *decorative* 5-row grid, so the number was dropped rather than raised to ten); #534 12 unrouted page files deleted (Lane E; 8 `scripts/ops/**` content-fixture pages spared); #535 both trial auto-activate write sites removed (`useSubscription.ts` + the unconditional `AuthContext.tsx:233`) and the real trial CTA wired into `RequirePremium` — owner LIVE-VERIFIED on production. **★★ The wave surfaced a cost-exposure tier that now outranks the empty code-side launch-blocker tier** — `/check-improve` and worksheets are ungated/unmetered, no server entitlement, no rate limit; see `OPEN_QUESTIONS_AND_FOLLOWUPS.md`.) Previous sync: `7998ee4a` (**#528 - PR-B2: THE RAIL TUTOR ENTRY + THE HEADER STACKING FIX**: a **Tutor entry at rank 2** of the `DesktopShell` rail that **opens the shared `TutorPickerModal` and does not navigate** - a discriminated-union `NavItem` narrowed by `"action" in n`, so the five existing entries stay byte-unchanged and no placeholder `to` ships an anchor into a dead route; plus `position: relative; zIndex: 35` on the header, resolving **`[FU-HUB-DROPDOWN-ZINDEX]`**. 2 files, +428/-24. Owner byte-reviewed and **LIVE-VERIFIED**. **This completes the original Home spec end-to-end** - PR-B was its last unbuilt half. ★★ The ceiling was **50, not 9998**: `.command-palette-backdrop` (`styles.css:6494`), a sibling of the shell opened by the header's OWN search box - and the FU's recorded `zIndex: 55` was **above** it, both of its bounds having rotted (TrendsPage retired, the two drawers deleted in #516). ★★ Mobile and desktop needed **different SHAPES of fix**: mobile's invariant is "no trap ancestor", desktop's is "the trap must outrank page content", because the header's blur is intended design. ★★ The picker mounts at the **shell root** - inside the header its z-index 60 would have been trapped under the new 35, fixing the bug while shipping a fresh instance of it.) Previous sync: `2865432` (**#520 + #522 - THE HOME REDESIGN ARC**: the duplicated Worksheets hero retired (it and Practice carried the identical destination string), the tutor surfaced via a SHARED firebase-free `TutorPickerModal`, MI rebuilt, the mobile carousel, and the §3 login/return contract with the chapter surviving the round-trip [#520, 5 files +1563/-1059]; then the signed-out SAMPLE MI panel RESTORED to that conversion surface, the practice-hub "side and edge" card colour treatment - which fixed a spine that had NEVER rendered - and `MobileAccountMenu` extracted so mobile Home can carry the account avatar without adopting MobileShell [#522, 5 files +877/-258]. Owner byte-reviewed both, LIVE-VERIFIED #522. **PR-B (the DesktopShell rail Tutor entry) is the remaining piece and is now UNBLOCKED.**) Previous sync: `ee5cd640` (**#521 - EXAM TRENDS DESIGN UPLIFT**: presentation-only restyle of `ExamTrendsRanked.tsx`, 2 files +1150/-574; one copy change `Open`->`Learn`; owner-signed tier data proven byte-frozen twice over and re-verified independently by the owner. **#519, #520 (PR-A Home) and #522 (PR-A2 Home fixes) are NOT reflected here - the Home lane's combined handoff covers #520 + #522.**) Previous sync: `a86feda` (**#516 - THE TUTOR RETIREMENT IS COMPLETE**: PR-2 of 2 deleted the dead old-tutor cluster, the unreachable `MentorSolveDrawer`, the `/api/mentor` endpoint and the persona/test-bot cluster - **62 files, +34/-15,239**, the largest deletion this repo has taken. Owner byte-reviewed the full deletion set and merged. **NO completion cell moves and NO surface changes for a student** - dead-code removal beneath surfaces that already stopped using it at #512. **`[FU-TUTOR-LEGACY-RETIRE]` CLOSED end-to-end.** ★★ **A FALSE CLAIM IN THIS FILE IS CORRECTED**: the post-#512 sync recorded `/api/mentor` + `MentorSolveDrawer` as **"LIVE in PracticePage" and must-keep**. Both were **dead** - the drawer was MOUNTED but unreachable (trigger prop threaded three levels down into an unused `_onOpenMentorBoard` binding; its `?journeyMentor=` auto-open had no producer), and the endpoint had no product caller once the old tutor died. **Mount is not liveness.** `types/mentor.ts` DOES survive, but for a different reason than recorded - a shared TYPE module feeding `src/ai/aiClient.ts` (33 importers) and the live grading stack. **`topicHubMastery` UNTOUCHED** (owner still holding; that lane is now unblocked). `App.tsx` byte-frozen. Scope discovered ⇒ §2a; no Scope cell moves. Previous sync: `a40fa75` (**#515 - WAVE-3: TESTS ARE TYPECHECKED · 26 GARBLED BANK ROWS RECOVERED FROM SOURCE · "REFRESH SET" ACTUALLY REFRESHES**: one PR, three file-disjoint subagent lanes, three independently revertable commit-sections; 17 files, zero forbidden; owner byte-reviewed the pushed diff against CBSE maths + the source citations and merged; CI `quality-gate` PASS + `lane-overlap` PASS.

**#531–#535: NO CORE-MATRIX COMPLETION CELL CHANGES, and NO Scope cell changes.** Stated explicitly per §10. The wave was hardening / text / deletion / gate-chrome, not surface build state. Surface-by-surface: **Auth / Sign-up** — #531 hardened the post-auth redirect (behaviour, not a completion cell). **Legal** — #532 made policies reachable (reachability, not a matrix surface). **Welcome (Exam Trends preview caption)** — #533 changed one caption string; the surface was already Built/Verified and does not move. **Dead pages** — #534 deleted 12 unrouted files that were never in the matrix (no student surface). **Premium gate / `RequirePremium`** — #535 added the trial CTA: this is the only *new rendered behaviour* on a chrome surface (the paywall), but it is a gate, not a core student surface, so no matrix row moves; recorded here so the CTA is discoverable. **MI cell unmoved on every row** — nothing in the wave touched Mistake Intelligence. **Scope discovered (cost-exposure tier) is logged in `OPEN_QUESTIONS_AND_FOLLOWUPS.md` + `NEXT_ACTION.md`; it does not move a Scope cell** (it concerns server gating/metering, not a student surface's definition-of-done).

**#528: NO COMPLETION CELL CHANGES, and NO Scope cell changes.** Stated explicitly per §10. **Tutor / Learn** was already ✅ Built/Redesigned/Desktop/Mobile/Verified (MI 🟡); #528 adds a **new entry point** to it from app-wide shell chrome, which is reachability, not build state — the surface itself is untouched. **Home** is unchanged: the header fix is `DesktopShell` chrome that renders on *every* shell route, not a Home-surface change, even though Home's cards are where the trapped dropdown was most visible. No surface gained scope: `[FU-HUB-DROPDOWN-ZINDEX]` was a **known, already-logged defect being closed**, and the z-index ceiling correction is a technical constraint discovery inside chrome, not product scope. **MI cell unmoved on every row** — the rail entry surfaces the tutor, it does not surface MI, and the doctrine that **MI is navy-sidebar chrome only** is untouched.

(previous sync, #521) **NO COMPLETION CELL CHANGES, and NO Scope cell changes.** Stated explicitly per §10. Exam Trends was already Built/Redesigned/Desktop/Mobile/Verified all ✅; #521 is a **presentation-only** restyle to the current card grammar with **zero functionality change**, so it moves no cell - it deepens a surface that was already complete. **No scope was discovered on any surface.** The one new product-facing follow-up (`[FU-EVIDENCE-BASE-CLAIM-INCONSISTENT]`, inverted) is **copy scope on Welcome/config, not Exam Trends scope**, and both its targets are globally forbidden files. Notes cell updated below.

(previous sync, #515) **NO COMPLETION CELL CHANGES, and NO Scope cell changes.** Stated explicitly per §10. Lane by lane: **L1** is build/CI configuration with **zero product `src/`** — it touches no surface at all. **L2** is bank CONTENT quality (26 PYQ rows recovered from source, 1 corrupt duplicate retired) — it improves what Quick Practice / Worksheet / Chapter Test serve, but moves no surface’s build state; **Quick Practice** remains ✅ and the recovered rows now sit in the **student-QA queue**, the owner-ruled final content gate. **L3** fixes a live **Quick Practice** defect — the built-set toolbar’s “Refresh set” (and `Alt+R`) returned the identical questions — but that surface is already ✅ Redesigned/Built, so a defect repair inside a shipped surface flips nothing. **`PracticeControls.tsx` was not touched at all** (pure presenter; the defect was in the handler passed to it).

**Scope discovered — logged, but it does NOT move a Scope cell.** The garbled-bank damage class is **89 rows across 141 files**, not the briefed ~15 (~61 remain → `[FU-BANK-GARBLED-EXPANDED-SCOPE]`), and **`check:mojibake` is blind to the PUA codepoints that constitute it** (→ `[FU-MOJIBAKE-GATE-MISSES-PUA]`). This is **content-lane and tooling scope, not surface scope** — no product surface’s remit widened, so no cell moves to Settling.

**★ Three questions were UNSOLVABLE AS PRINTED and now are** — `PYQ-M-STAT-008`, `PYQ-M-2024-STAT-003`, `PYQ-M-2026-PROB-002`. A real student-facing quality gain on an already-✅ surface, which is exactly what this tracker does *not* capture; recorded in `CURRENT_STATE` §Section 2. Previous sync: `e19b2d1` (**#512 - THE OLD "TEACH ME" TUTOR DRAWER IS RETIRED FROM THE LIVE PRODUCT + ONBOARDING→HOME**: PR-1 of 2 of the tutor/onboarding retirement — **behaviour only, 7 files, +102/−110, ZERO file deletions**; owner byte-reviewed the pushed diff and **LIVE-VERIFIED all five checks**, incl. the referral credits-once path. **ONE completion cell moves: Tutor / Learn `Redesigned` 🟡→✅** — that 🟡 was held *explicitly because the old tutor was still live on Topic Hub*; it no longer is, so the surface is now genuinely the redesigned one. (`MI` stays 🟡 — a separate Stage-3 gap, untouched.) **Topic Hub** concept rows lose the "Teach me" button and keep their `/tutor` entry — no cell changes; **Login** gains the `/onboarding`→`/` re-point across all four live inbounds, already ✅ and live-verified. **This was a PRODUCT REMOVAL, not a cleanup** — "Teach me" was an *unconditional* button on every concept row sitting beside the new "Stuck? Ask", so students saw both. **The old-tutor FILES remain on disk** (now imported only by each other); deleting them plus sweeping the ~27 coupled ops scripts is **PR-2, NOT STARTED**, so `[FU-TUTOR-LEGACY-RETIRE]` is **HALF resolved**. **`topicHubMastery` UNTOUCHED — owner is holding that decision.** Scope discovered ⇒ §2a; no Scope cell moves (Topic Hub and Tutor were already Settling). Previous sync: `856d556` (**#511 - BANK MIS-BANDING CLASS (b) COMPLETE**: 223 genuine Section-D/E rows — **correctly banded all along**, unlike Class (a) — now carry CBSE `[N mark]`-prefixed `solutionSteps` summing exactly to their marks (validator: thin 220→**0**, bad-sum 3→**0**, compliant 1,137→**1,360**). 109 of them had held their whole solution in ONE run-on step. **NO product SURFACE moved and NO completion cell changes** — this is bank-content quality BENEATH every surface that serves D/E questions (CT / FM / QP / Worksheet / Topic Hub all read the same canonical bank, so each gets better marking schemes with zero UI change). Bank count unchanged at 8,584; every question field byte-identical (proven by a field-by-field deep-compare of the ASSEMBLED EXPORT across all 8,584 rows, not a diff grep). **`[FU-BANK-SCARCE-BAND-MISBANDING]` is now CLOSED end-to-end** (Class a = #504, Class b = #511). Opens `[FU-BANK-GARBLED-ANSWER-CLASS]` — the QUESTION-side twin (~15 rows with OCR-garbled `answer` fields + `PYQ-M-2026-CG-002`’s welded `questionText`), correctly a separate lane because #511 authors SOLUTIONS, not QUESTIONS. **No scope was discovered; no surface Scope cell moves to Settling.** Previous sync: `41277c1` (**#509 - FRESH-SET FIXED + THE VITEST GATE IS FULLY STRICT**: the Quick Practice scorecard's "Build a fresh set" now returns genuinely different questions (owner LIVE-VERIFIED), and all four red vitest suites are repaired with **every** `--exclude` deleted - CI now runs 60 files / 789 tests with no exclusions, so a red suite fails the build. Previous sync: `b810055` - **#505 - MOCKBUILDER FULLY DELETED**: the whole mock-builder feature is removed - HPQ "Mock basket" + StudyPlanPage "Quick mock" button + `buildMockBuilderUrl` + the command-palette "Build a Mock Paper" entry - plus A-1 the orphaned `mock_builder` paywall gate (UpgradeModal was advertising a deleted feature), A-2 the dead `utils/mockBuilder.ts` + its `syllabusGuard.ts` allowlist entry, A-3 the stale `sitemap.xml` URL; the `/mock-builder` -> `/practice-hub` redirect + `:356` nav-check KEPT. **HPQ's Mock-basket sub-feature was removed but NO product SURFACE moved** - HPQ stays Locked/live; the basket was defunct cruft that only fed the dead builder. Resolves [FU-MOCKBUILDER-FULL-DELETE]; opens [FU-APP-TSX-DEADCASE-+-OVERLAY-FREEZE] + [FU-HPQ-PREDICTED-MOCK]. Prior sync: `b810055` (**#504 - BANK MIS-BANDING (Class a) FIXED**: 44 bank rows relabelled off a bogus 5-mark Section-D to their true CBSE value — **37 MCQ/AR → A/1** (format unchanged ⇒ stay objective/binary), **7 short VSA → B/2** (PYQ-grounded, `solutionSteps` re-authored to the 2-mark scheme). Data-only + one shrink-only guard (`PACK_5MK_SHORT_BACKLOG` emptied). The count was 44, not the spec's ~76 — short-*prompt* genuine-`Long` proofs correctly excluded. **The question bank's CT/FM/QP/Worksheet Section-A/B/D banding is corrected; NO UI surface completion cell moves** (see §2a under Chapter Test). **CLASS (b) (~178 under-stepped genuine-5-mark rows) STILL OPEN.** Owner LIVE-VERIFIED; [FU-BANK-SCARCE-BAND-MISBANDING] Class (a) + [FU-AITIER-MARKS-MISMATCH] resolved. ⚠ trunk tip `b810055` also carries #505 (MockBuilder-delete, separate lane); the one-commit lag from #504's merge `b799952` is expected. Prior sync: `579822e` (**#503 - CI-VITEST GATE**: vitest (`src/**/*.test.{ts,tsx}`) now runs as a required step on the linux CI runner; 4 pre-existing red suites `--exclude`'d + booked as FUs. **NO product surface moved - this is CI/infra only, so no cell below changes.** Resolves [FU-CI-GATE-VITEST]. Prior sync: `7979a89` (**#501 - QP SCORECARD DENOMINATOR FIXED**: the scorecard "of N" now counts the DISPLAYED set (`filteredQuestions.length`), not the over-fetched pool (`questions.length`) - a full-page 5-MCQ set read "5 of 75", now "5 of 5"; feed-only, forbidden `ResultsScorecard.tsx`/`scorecardVariants.ts` untouched; owner LIVE-VERIFIED; [FU-QP-SCORECARD-ATTEMPTS-WIPED] resolved, [FU-QP-WRITTEN-BINARY-CHECK] logged. Only Quick Practice moved. Prior sync: `273cfe8` (**#490 → #491 → #493 → #495 — ★★ THE QP OVERLAY ON THE TUTOR IS LIVE** — the tutor⇄QP arc COMPLETE, owner LIVE-VERIFIED. Quick Practice is now hosted as an in-tutor overlay (the C&I twin): "Practise this" opens the real `PracticePage` over the dimmed tutor, filtered to the microconcept, scorecard in the panel, closing returns and the tutor quotes the score. ⚠ The arc shipped a production break — #490's nested `<MemoryRouter>` inside the app's `<BrowserRouter>` threw for every student; reverted (#491, byte-identical to healthy `0b22ee7`) then fixed (#493: `UNSAFE_RouteContext` reset + `<Routes location>`, no nested Router; nav contained via a `NavigationContext` override); #495 added the named return. `overlay === undefined` ⇒ BYTE-IDENTICAL; C&I unregressed; engine/persist/grader/fetch-filter zero-diff. Only Quick Practice + Tutor moved. Prior sync: `9edb939` (**#488 — ★★ QP MULTI-TOPIC PRESETS (shape 3c)** — a `≥2 topics` hub selection now produces a pooled-and-shuffled mixed set spanning all chosen topics [per-topic fan-out + merge + competency floor; owner LIVE-VERIFIED both Maths & Science]; single-topic BYTE-IDENTICAL [ternary-wrapped, gated `≥2 topics`], ZERO engine-service edits, `sessionRecords.ts` byte-identical [forbidden-file touch caught + reverted; `topicKeys` moved to the QP service]; only Quick Practice (PracticePage) moved. Prior sync: **#483 → #486 — ★★ THE QP A1 ENTRY REDESIGN IS REACHABLE + NAVIGABLE** — #481's preset entry is now reachable from the hub [#483 — `deriveArrivedTargeted` re-keyed on `source`: `source=practice`→presets, `source=tutor`/`targeted=1`→auto-build; the agent correctly deviated from the spec's literal `source==="tutor"` rule after mapping ~21 topic-bearing entrypoints it would have regressed] AND navigable both ways — browser-back [#485, a real `built=1` search-param history entry] and the breadcrumb "Back" CTA [#486] both return the built set to the preset chooser. ★ The honest two-step: #484 shipped a state-only same-URL navigate = a replace/no-op (RR #5362) that created no poppable entry and passed all gates + CI + 14/14 because the test never mounted a router — owner live-verify caught it, #485 fixed it with the search param + a `createMemoryRouter` test. Engine/presets/persistence/hub byte-identical throughout; only Quick Practice (PracticePage) moved. Prior sync: **#481 — ★★ QUICK PRACTICE A1: PROGRESSIVE-DISCLOSURE ENTRY + OPTIONAL TIMER LIVE — presentation-only [a preset = a bundle of the existing setCommitted* setters; the reshuffle/rotation engine, the filter wiring and persistQuickPracticeSession are byte-identical]; ⚠ the presets shipped correct-but-UNREACHABLE [gated on a topic-less direct visit that production never produces, since every route carries topic= → auto-build → owner live-verify caught it, not the green gates; the Piece-1 hub-reachable fix (re-gate on source) is IN FLIGHT]; only Quick Practice (PracticePage) moved this PR. Prior sync: #478 + #479 — ★★ THE TUTOR READS THE GRADED WORK**: on C&I overlay return the tutor now names the question AND the actual lost step — the question + graded per-step digest reach the model as one-shot `returnedWork` context [#478 seams; #479 flipped `RETURNED_WORK_DIGEST_ENABLED` ON after a live 28-call rubric-2 eval — additive, flag-on]. Only **Tutor** [return-opener carries question+digest] and **Check & Improve** [overlay `onClose` gained the graded-response arg] moved; no other surface did. Prior sync was `cca0a5d` — **#476 — ★★ THE TUTOR ⇄ C&I OVERLAY IS LIVE**: the tutor's "Get my attempt marked" CTA opens the real `DesktopCheckImprovePage` as an in-tree panel over the dimmed light tutor [Option A, responsive — right-slide `min(88%,920px)` desktop/tablet, full-screen `100dvh` mobile]; the student grades and the record is handed back via the existing `composeReturnOpener` — no navigate/poll/banner; grader/`SessionRecord`/`ResultsScorecard`/MI-52-anchor/`tutorRoundTrip.ts` byte-identical, overlay gate 24/24, convergence 92/92. **Only Tutor + Check & Improve moved this PR.** **Prior:** **#472 — ★★ C&I QUESTION-SIDE PARITY COMPLETE**: the question uploader gained the answer uploader's hands — `<EquationInput>`, a QR handoff in a new `"question"` mode, camera/files, and paste; a frozen five-site map was caught one **server** allow-list site short. Also #473 = cofounder skill v2.1. **Prior:** **#466 → #470 — ★★ THE CHECK & IMPROVE CONVERGENCE ARC IS COMPLETE.** Owner LIVE-VERIFIED at 360 / 768 / 820 / 1024 / 1440. C&I was two components [`DesktopCheckImprovePage.tsx` 2,734L + `pages/app/CheckImprove.tsx` 1,656L] switched by a route ternary on `isDesktop`; it is now **ONE fluid responsive component at every width**, and the mobile twin is **deleted**. **#466 `f895306`** the convergence [inverted mobile order killed, 5 purples → `PRIMARY_GREEN`, false "PNG or JPG" → canonical `UPLOAD_LIMIT_SENTENCE`, **13 `isNarrow`/`matchMedia` layout sites removed**, `CARD_BASIS = 340`] · **#468 `fd00377`** the twin deleted [re-land of the orphaned #467] · **#469 `7786966`** the gate that never ran [`checkout@v5` default depth 1 ⇒ MI + FORBIDDEN checks silently skipped every run; fixed with `fetch-depth: 0`, MI anchored to the fixed SHA `e8f75af`, and **a missing ref now HARD-FAILS in CI**] · **#470 `2c59dd2`** the mobile header [`flex: 1` = basis 0% ⇒ the `flexWrap` could never fire ⇒ 5-line title; `HEADER_TITLE_BASIS = 320`; title `clamp(22px, 6vw, 30px)`; lede deleted both widths; How-it-works recoloured + chevron; D4 completed]. **★ WHY — the thread, not a cleanup:** the tutor is to host **QP + C&I as in-tutor OVERLAYS**; C&I could not live in an 820px overlay because `useIsDesktop()` measures the **window, not the container** ⇒ the convergence was a **PREREQUISITE**. **★★ THE GATE NOW RUNS** — `MI: all 52 survivors are still present BYTE-IDENTICAL (permanent moat guard)` executes on trunk [63/63], first time in this repo’s history. **NO COMPLETION CELL REGRESSES OR NEWLY-COMPLETES** — C&I was already ✅ across the board [owner live-verify at five widths re-confirms Mobile ✅ + Verified ✅]; this is a **restructure + §2a DEPTH** on an already-built surface, so the row’s DESCRIPTION is rewritten, not its marks. **SCOPE DISCOVERED, logged in §2a + `DECISION_LOG.md`, C&I Scope stays `Settling`:** [1] the **13 `matchMedia` layout sites** in the "desktop" twin — the convergence was structurally larger than "delete a twin"; [2] **the CI gate that never ran** — a whole class of green-proves-nothing. **★ ONLY CHECK & IMPROVE MOVED** — Tutor, Quick Practice, Worksheet, HPQ, Full Test, Topic Hub, Home, Me/Progress, Landing, Login are all untouched this arc. **★★ THREE LESSONS [full text in SESSION_LOG]:** the stacked-PR **orphan trap** fired three times [squash-merge + a live stacked base = a merge into a branch nobody sees; only `git merge-base --is-ancestor` catches it]; **an honest skip in CI is still a green build** [if a check matters, its ABSENCE must fail — the 2nd gate in this repo to silently never run]; **naming a failure mode does not immunise you** [the spec was wrong nine times, every correction from running a command]. ⚠ **[FU-CI-RETARGET-NO-GATE]:** `gh pr edit --base` fires `pull_request:edited`, which `quality-gate.yml` ignores ⇒ a retargeted PR keeps its GREEN check from the old base. **Branch hygiene this session: five stale branches deleted [local + remote] under owner authorization; owner asked to enable auto-delete-head-branches.**) _Prior sync `50783e7`:_ (**#464 — ★★ THE TUTOR SAYS THE REAL NCERT PAGE IS THERE; the NCERT arc #457 → #459 → #464 is COMPLETE. Owner BYTE-REVIEWED, then LIVE-VERIFIED all FOUR probes: marked concept → page mentioned UNPROMPTED on the same turn as the signal · unmarked concept → total silence · NO invented page number/chapter/link · a topic with ZERO pages → no NCERT reference of any kind (the `anyNcertPage` gate, confirmed in the LIVE model, not just a static render).** **1 file, `tutorSystemPrompt.cjs`, +56/−7 — PROMPT TEXT ONLY.** #457 filled the data (65 rows carry `ncertPage`) → #459 let the page **win** the panel → **#464 makes the tutor SAY it.** ★ ***An affordance nobody knows about is not an affordance*** — the page sat behind a button a 15-year-old had no reason to look for, on a concept they had no reason to think had one. `hasNcertPage` had reached the prompt layer since #457 and **nothing read it** (the code said so itself at `routes/tutor.cjs:101`); `figurePanelBlock` now marks each carrying option `[real NCERT page available]` and tells the tutor to say so in plain words. **★★ RAIL 1 — MENTION ONLY ALONGSIDE THE `[[figure:<key>]]` SIGNAL:** the page renders **inside** the explanation panel (`ExplanationPanel:182` — its button, or the panel body itself) and `TutorPage:185` gates that panel on **`showPanel = panelOpen && !!resolvedVisual`** ← the model's own signal ⇒ **a mention without the signal describes a button that is NOT on the student's screen — a FAKE AFFORDANCE**, the species this codebase refuses everywhere. *Coupling is the only way the mention is true 100% of the time — and it was **NOT in the dispatch**; it came from tracing the affordance to the UI.* **★★ RAIL 2 — NEVER A PAGE NUMBER, CHAPTER OR LINK (the more important one):** the model is told only **THAT** a real page exists, never **WHICH** (`normalizeFigures` whitelists exactly `{key,label,hasNcertPage}`; `resolveConceptVisual` alone decides `kind:"ncert"`) — ★ **telling a model a page exists is an open invitation to guess its number, and a plausible-but-wrong number is WORSE than no mention: it sends a student hunting through their REAL textbook for something that is not there.** **Do NOT pass the page data through "to be more helpful" — that inverts a deliberate architectural split.** **Wording holds whichever body wins** (`resolveConceptVisual` attaches `ncertPage` **regardless of body**, so the page is reachable as the panel's body *or* its button, and the model **cannot know which**) ⇒ *"right there in the panel"*, never *"there's ALSO a page"*. **Plus an honesty fix one line up:** *"have a curated, NCERT-aligned diagram"* was **FALSE for page-only rows** (`catalogueFiguresForTopic` admits a page-and-no-figure row) → *"a diagram, the actual page, or both"*. **★★ THE HARNESS CAUGHT A BUG A RENDERED CASE COULD NOT:** the directive was first emitted **UNCONDITIONALLY** ⇒ on a topic with **no** pages the prompt would describe a marker **appearing nowhere in its own list** — **the fake-affordance bug ONE LAYER DOWN, built in while congratulating ourselves for catching the first one**; gated on `list.some(f => f.hasNcertPage === true)`. ★ **Reading the block would never have caught it — the MIXED case rendered perfectly** ⇒ ***one rendered example is not coverage: assert the ABSENT cases.*** **★★ EVERY GATE WOULD PASS IDENTICALLY ON TERRIBLE WORDING — they do not read English** (rendered+read · 14/14 structural harness · tsc · 190/190 · ops 73 rows · mojibake · `scope:guard lanes=product` · `diff --check`); ⚠ **a first tsc run printed a FALSE GREEN (`TSC EXIT: 0`) because `$?` after a pipe reports `tail`'s status — run tsc BARE** (*the 4th green-that-proved-nothing this session; self-inflicted*). **NO COMPLETION CELL MOVES** — the Tutor row was already ✅ Built; §2a **DEPTH**. **★ The Tutor's `Verified` cell is NOT re-claimed** — this pass covered **the NCERT mention**, not the Tutor's whole live checklist; *the standing #444 precedent, held to the last PR of the night.* **⚠ LEFT OPEN — [FU-TUTOR-CJS-STALE-PLUMBING-COMMENT]:** `routes/tutor.cjs:101` still reads *"plumbing only today: `figurePanelBlock()` does not read it yet… Using it is the tutor-round-trip lane's sequenced task"* — **#464 made that FALSE**, so it is now **a SPENT INSTRUCTION LEFT IN PLACE, and it is the very comment that dispatched #464**: grep `hasNcertPage`, land there, conclude the work is undone, redo it. ★★ **The #451/#454 ruling verbatim — STRIKE A SPENT CHECK IN PLACE; a stale instruction is worse than none because it LOOKS LIKE DILIGENCE.** Comment-only; **PRODUCT file ⇒ owner-approved product PR** (could not ride the docs-only PR that logged it, §8). *Found by re-verifying #464 on trunk before writing these docs — **not** by review. **Third instance tonight, in a third medium**: #451/#454 a product instruction → #462/#463 a cross-lane docs claim → #464 a code comment. **The disease does not care which file it lives in.*** **★ NEXT SESSION (specced, deliberately not tonight's): QP + C&I as IN-TUTOR OVERLAYS reusing the real pages VERBATIM** ⇒ **[FU-TUTOR-ROUNDTRIP-COUNT-5] + [FU-TUTOR-WAITING-BANNER] REMAIN HELD** (that architecture makes the round-trip **banner/count-link mechanism secondary**; building either now risks throwing it away — *a HOLD with a reason*). **Branch cleanup: the owner does ONE sweep across everything accumulated tonight — no agent action (§3).**) _Prior sync `7be651d`:_ (**#460 — A STUDENT WHO ASKS TO PRACTISE GETS THE HAND-OFF. Owner BYTE-REVIEWED, then LIVE-VERIFIED all THREE probes: direct ask → CTA · _"Can you give me an example?"_ → taught, NO CTA (the boundary held) · offer→agree → CTA unregressed.** **1 file, `tutorSystemPrompt.cjs`, +13/−2 — PROMPT TEXT ONLY.** `[[offer:practice]]` fired on exactly ONE turn — the tutor's own offer — so a student who said *"I want to try a few questions"* got a reply and **no button**; the app could route them only if the **tutor** thought of it first. ★ **The ask is stronger evidence of readiness than the offer ever was.** Now: **(a)** the offer turn **or (b)** the turn answering a direct ask. **★★ THE DISPATCH POINTED AT A GATE THAT DOES NOT EXIST** — it named `useTutorSession.ts`, which only **ATTACHES** the tag (`:345`); the real gate is **`TutorPage.tsx:356`**, firing on **tag PRESENCE** with **no agreement-state condition anywhere** ⇒ the restriction lived **entirely in the prompt's English** ⇒ prompt-only. ★ **Editing the named file would have INVENTED a client-side coupling the design deliberately kept server-side** — *carry the question, not the expected answer; the owner verified both claims in the code before approving.* **★★ THE BOUNDARY — #442: QP practises, the Tutor teaches.** Loose widening **inverts it** the first time a student asks *"what would a question on this look like?"* and is pushed to Quick Practice instead of answered ⇒ (b) drawn **narrowly**, teaching phrasings **enumerated by example**, **cross-referencing** the existing WORKED EXAMPLES block so the two **cannot drift apart**, and **★ ambiguity FAILS CLOSED** (*"treat it as a teaching request and emit NO tag"*) — **a missing CTA costs one more sentence; a wrong CTA pushes someone who wanted an explanation into a practice set** (*#456's honest-or-silent, applied to a prompt rule*). **★★ EVERY GATE WOULD HAVE PASSED IF THE WORDING WERE TERRIBLE** — tsc/matrices/mojibake **do not read English**; the only check with power was **rendering the block through `buildTutorSystemPrompt` and reading it**, which proves it *renders*, not that it *works* ⇒ **a prompt is only verified by a model reading it**, so prompt changes need live-verify MORE than code changes. *(Third instance tonight of a green gate proving less than it appears — with the others: an unrun suite, and `scope:guard` returning `no changes` on a clean tree. Contrast: here it returned `lanes=product` and DID inspect a real diff — same gate, opposite evidentiary value.)* **NO COMPLETION CELL MOVES** — the Tutor row was already ✅ Built; §2a **DEPTH**. **★ The Tutor's `Verified` cell is NOT re-claimed** — this pass covered **the CTA trigger**, not the Tutor's whole live checklist (#448's Stage-3 items remain pending on that row); *the standing #444 precedent, held again.* **✅ #457 + #459 ARE DOCUMENTED — by #461 (the `27e6ec2` sync, nested directly below). CORRECTED BY #463:** this sync originally claimed they had **NO handoff docs and that the catalogue lane owed them** — **true when written, FALSE within the hour** (#461 merged mid-write and paid that exact debt). ★★ **The correction was written but NEVER PUSHED** — it sat on an unpushed local commit behind a `--force-with-lease` approval that was **requested and not granted**, while the conflict was resolved **in the GitHub UI from the pre-correction commit** and merged ⇒ **the false claim went live on trunk in SIX files.** ***A correction that is not pushed does not exist*** — **if a claim you ALREADY know to be false is sitting in an open PR, SAY SO; that outranks waiting politely for approval on the mechanism that would fix it.** ★ **The 13th stale-base catch and the FIRST DOCS-vs-DOCS one — a NEW COLLISION CLASS** (two docs lanes prepending to the same six files; expect it whenever two lanes finish within the hour). ★★ **That same UI merge ALSO DUPLICATED this file's `**Home:**` header** — two competing "Trunk at last sync" lines (`be200cb` + `27e6ec2`), each carrying a **full parallel history**, against this file's **one-Home-plus-nested-`_Prior sync`** convention; **#463 collapsed it** (the two tails were verified **byte-identical** before chaining). ⇒ *a UI merge of two prepend-style docs lanes does not merely pick the wrong text — **it can break the file's structure, and no gate reads these files.*** **Re-verify every cross-lane claim AT REBASE.** **NEXT — [FU-TUTOR-NCERT-PROACTIVE-MENTION] UNBLOCKED:** ★ **`hasNcertPage` VERIFIED IN CODE, not inferred from "#457 landed"** (`conceptVisualCatalogue.ts:226`/`:253` → `tutorClient.ts:50` → `tutor.cjs:111`, coerced `=== true`); ★★ **the code names its own seam at `tutor.cjs:101` — _"plumbing only today: `figurePanelBlock()` does not read it yet"_** ⇒ the work is `figurePanelBlock()` (`tutorSystemPrompt.cjs:226`); ⚠ **`normalizeFigures` REBUILDS each option at the trust boundary — an un-whitelisted field is silently dropped**; ★ **never make the student ask for the page — a 15-year-old will not know the trigger phrase.** **★★ HELD, do NOT build:** `count: 5` + the "tutor is waiting" banner / scorecard-return-row, pending the **overlay-architecture investigation** (QP + C&I may become **in-tutor overlays reusing the real pages verbatim** ⇒ the mechanism becomes **secondary**).) _Prior sync `27e6ec2`:_ (**#457 + #459 — ★★ THE NCERT PAGE ARC: DORMANT → LIVE → WINNABLE.** `084442b` = #457's squash. **This sync PAYS #457's docs debt, which #458 recorded as owed by that lane.** Owner BYTE-REVIEWED both pushed diffs, then **LIVE-VERIFIED ×3** — maths **p.11** (polynomials) · science **p.132** (heredity) · **electricity · Ohm's law** (the inline `ncert` body). *Different subjects, Storage paths and offset entries ⇒ the pipe is reachable **generally**, not for one lucky chapter — and the pages landing on correct CONTENT validated the verification method behind all 65, not just the two clicked.* **#457** = pure data: the affordance was **BUILT AND DORMANT, not missing** (`ExplanationPanel` already rendered the button whenever a row carried `ncertPage`; **zero of 54 rows had one**) ⇒ **54→73 rows**, **65** with a page, coverage **13→22 topics**; `data.ts` **`306 added / 0 deleted`** — git *proving* the 54 originals untouched. **#459** removed the ceiling `resolveConceptVisual`'s own comment stated (*"offered ALONGSIDE the body ... NOT as the body itself"*) — a **whole-chapter interactive outranked an exact page at ANY fit**; new priority **in-play exam figure → real figure → NCERT page → interactive → gap** (**15 of 73 rows change**), fail-closed to the honest gap when Storage doesn't answer. **NO COMPLETION CELL MOVES** — the Tutor row was already ✅ Built; this is §2a **DEPTH**, exactly like #456/#454/#451/#447/#445. **★ The Tutor's `Verified` cell is NOT re-claimed** — the owner verified **these three paths**, not the Tutor's whole checklist, and Stage 3's own pending items stay pending. *The standing #444 precedent, held again.* **★★ THE CATALOGUE IS TWO FILES AND ONLY ONE SHIPS** — `handoff/curation/…curated.ts` is imported by **NOBODY** (app tsconfig includes only `src`); the wired copy is `conceptVisualCatalogue.data.ts`. **They had ALREADY drifted** (#448's gap-fill rows + the label fixes lived only in `data.ts`) ⇒ always re-sync **curated ← data**; nothing enforces it ⇒ **[FU-CATALOGUE-SYNC-GUARD]** — *it rotted in ONE PR cycle.* **★★ THE REPO'S OWN NCERT PAGE DATA IS UNTRUSTWORTHY — open the PDF:** a spec's `page_pdf` is **0-based in some specs and 1-based in others** (25/62 off by one; **Light's is plain wrong**), the **`source_ledger` misplaces electricity** (series 192→**182**, parallel 186→**185**, Ohm's law 176→**175**), and the field is named **three ways**. **★★ PHOTOSYNTHESIS IS A VOCABULARY GAP, NOT A COVERAGE GAP** — no such `boardEssentials` row exists and CI hard-fails a non-live label ⇒ **the gate did its job by REFUSING**; same shape as the human brain ⇒ [FU-TUTOR-VOCAB-BRAIN-ROW]. **39 concepts checked and REFUSED** (the trophic pyramid shows *feeding levels*, not "ecosystem components" — the tempting stretch). **★★ [FU-CI-GATE-VITEST]'s CEILING WAS WRONG — CHEAP, not hard:** vitest **runs on Windows** (drop the stripped `@rollup/rollup-win32-x64-msvc` into rollup's own `node_modules`; **92/92**, nothing committed) ⇒ **the real blocker is that nobody wired vitest into the linux CI job that already exists and already works**; the new cases were **mutation-tested** because a `if (!row) return` guard makes a test pass while asserting nothing. **★ `scope:guard` FAILED benignly on #457** — every lane in `repo_boundary_policy.json` is relative to `lazytopper/` so repo-root `handoff/` is `[unclassified]`; the near-miss that hides it is **`_handoff/`** (leading underscore) ⇒ [FU-SCOPE-GUARD-HANDOFF-LANE]. **NEXT:** the NCERT proactive-mention prompt fix — **UNBLOCKED, the field is CONFIRMED to arrive** (`hasNcertPage` survives `normalizeFigures`, which REBUILDS `{key,label}` and would otherwise drop it by construction); **prompt-only — the UI alone gates the button; do NOT feed the page to the model.**) _Prior sync `084442b`:_ (**#456 — THE TUTOR READS QUICK PRACTICE'S GRADED WORKING; squash `dfe3144`, live trunk `084442b` because #457 landed on top mid-docs-write — the 12th stale-base catch, the second in two hours; #457's docs are owed by THAT lane. Owner BYTE-REVIEWED the pushed diff, then LIVE-VERIFIED: _"it does correctly identify the mistakes I made."_** The practice return-opener now names **where** a mark went, not just how many — reading QP's own durable session record (#436), whose payload carries the **same grader's** per-step detail because **QP's written-working path runs the same grader as C&I**. *The detail already existed; nothing was reading it.* **NO COMPLETION CELL MOVES** — the Tutor row was already ✅ Built; this is §2a **DEPTH** on an existing surface, exactly like #454/#451/#447/#445. **★ The Tutor's `Verified` cell is NOT re-claimed** — the owner verified **this round-trip leg**, not the Tutor's whole live checklist (and #448's Stage-3 verify items remain pending on that row). *The standing #444 precedent, held again.* **★★ TWO VOCABULARIES:** the pending marker says `"practice"`, the record says `"quick-practice"` ⇒ the existing `matchReturningRecord` could **never** match a QP record — silently, forever, **with every static gate green**. Mapped via a documented constant in a separate matcher; **the marker is NEVER renamed — it is live persisted state** (`tutorSessions/{uid}/topics/{topicKey}`) and renaming strands in-flight round-trips (*same species as #448's `conceptKey` rule*). **★★ THE RECORD IS NOT ALWAYS WRITTEN** — QP writes it **only at the scorecard** (`sessionFinished || allDone`; **no unmount hook, by owner ruling**) ⇒ a student who does 2 of 5 and taps back has **none**, while `practiceInsights` attempts exist for **every** answer ⇒ **record = richer, attempts = further reach, neither subsumes the other** ⇒ the chain is **ADDITIVE (record → attempts → nothing)** and `composePracticeReturnOpener` is **byte-unchanged as the honest floor**. **★ A step's marks are NEVER quoted** — the #445 objective clamp zeroes them **by design** (the mark lives at answer level), so quoting one reads as *"you scored nothing"*; keep the annotation, drop the mark — **exactly what the C&I/worksheet views already do**. **★★ OWNER RULING, general:** *any composed opener that has to reach for something to say about incomplete data is manufacturing insight the data doesn't support — **honest-or-silent beats a softened fabrication***; the composer returns `null` on every thin-data path (MCQ-only / no quotable step / no marks / no payload / unreadable). **★★ TWO GATES THAT READ PASS WHILE PROVING NOTHING — ONE SPECIES, owner-ruled into the [FU-CI-GATE-VITEST] case, not a footnote:** **vitest ran NOWHERE** (win32 can't — rollup's linux-pinned binary — **and it is not CI-gated**) ⇒ the 18 added cases execute nowhere, indefinitely — *the danger is not that tests break, it is that **a test file borrows the authority of a passing suite without ever being run**, in every diff, every review, every handoff doc*; **`scope:guard` returned `SCOPE_GUARD_OK (mode=product, no changes)`** post-rebase — **a green string from a gate that inspected nothing** (it reads the *working-tree* diff; a committed tree is clean) — *a gate that returns green without inspecting the thing you think it inspected doesn't merely fail to prove, **it pays out the feeling of proof***. **The workaround (`import type`-only ⇒ `tsc` emits standalone ⇒ driven from plain node, 24/24 against the REAL compiled logic) was LUCK, not a strategy** — most of this repo's logic has real runtime deps and no such hatch ⇒ **ships on argument, not execution.** **★ TWO-DOT `git diff` vs a MOVED trunk reports other lanes' merged work as YOUR deletion** (QR's `DesktopCheckImprovePage.tsx` at −47) ⇒ **always three-dot for own-scope review**; trunk moved **three times** across this one lane. **★ The rebase force-push was ASKED FOR, NEVER INFERRED** — §3 marks `push --force` *never auto-approve*, and *"rebase and open the PR"* does **not** authorise it by implication; the owner verified the remote state, then approved. **NEXT:** widen `[[offer:practice]]` so a **direct student ask** fires the same sentinel (dispatched) → the NCERT proactive-mention prompt fix (**blocked on `hasNcertPage` reaching `catalogueFiguresForTopic`; #457 landed — CONFIRM the field in code before starting**). **★★ HELD — do NOT build:** the hardcoded `count: 5` and the "tutor is waiting" banner / scorecard-return-row, both pending a **separate overlay-architecture investigation** (QP + C&I may become **in-tutor overlays reusing the real pages verbatim** ⇒ the banner/count-link mechanism becomes **secondary**); *a HOLD with a reason — do not "just fix" them because they look like one-liners.*) _Prior sync `a8be752`:_ (**#454 — ★★ THE QR LANE IS COMPLETE. The last wire (Check & Improve) is LIVE; owner BYTE-REVIEWED the pushed diff, then LIVE-VERIFIED.** **THE ARC — do NOT redo any of it:** **#441** (`9ebb87c`, the channel; **ONE wire lit THREE surfaces** — CT in-test, CT result, Full Mock — because `ChapterTestUploadPanel` is shared: *wire the shared component, not each page*) → **#443** (`5aaaeec`, hardening + the **live pre-existing** unspendable "5 MB" ⇒ `uploadLimits.ts` @ 3.5 MB) → **#447** (`d99c14d`, QP + HPQ + TopicHub via the shared `SolutionChecker`) → **#451** (`c132f27`, **the C&I guard that never existed** — not a wrong ceiling, **NO ceiling**) → **#454** (`a8be752`, C&I, 1 file +47/−0, **desktop-only**). **NO COMPLETION CELL MOVES** — C&I / QP / HPQ / TopicHub / CT / FM / Worksheet were all already ✅ Built; the whole arc is cross-surface **DEPTH** on existing surfaces (§2a), exactly like #353/#344/#420. **★ NO `Verified` CELL IS FLIPPED OR RE-CLAIMED ANYWHERE IN THIS ARC** — the owner verified **the QR** (and #451's guard) on those screens, **never any surface's OWN live-verify checklist**; flipping a surface's Verified on someone else's pass would be over-claiming. **This is the standing #444 precedent (Full Mock), held consistently across #447, #451 and #454.** **#454's shape: `mode={isMultiQuestion ? "document" : "photo"}`** — ★★ **the FU's own `mode="photo"` was WRONG-SHAPED: C&I is BIMODAL** (multi-question = the answers to a WHOLE PAPER, one multi-page PDF per the page's own `:1765` comment ⇒ `"photo"` there is exactly the *shoot-page-1-and-believe-you-are-done* failure `mode` exists to prevent). `isMultiQuestion` (`:756`) settles on the QUESTION upload, **before** the answer upload is reachable ⇒ no race. Owner live-verified the **multi-question PDF-leading copy**. **Mobile got NO QR — correct:** `QrAnswerHandoff` → `useIsDesktop()` → null <1024px; a QR on a phone is meaningless. **★★ THE TWO LESSONS THIS ARC EARNED (apply to every future FU): (1) CARRY THE QUESTION FORWARD, NEVER THE EXPECTED ANSWER** — two-for-two here (*"a THIRD 5 MB constant"* that did not exist because the bug's SHAPE was *no guard at all*; *`mode="photo"`* on a bimodal host). **Neither was careless — both were true when written and had ROTTED. A stale instruction is worse than none: it looks like diligence** ⇒ **strike a spent check IN PLACE.** **(2) CHECK WHAT THE ACTUAL HOST RENDERS, NOT THE SHAPE OF THE LAST WIRE YOU BUILT** — #447 needed **MORE** state than a naive copy (a QR PDF would render broken in an `!isPdf`-gated `<img>`); #454 needed **LESS** (no preview, 3 fields). Both wrong directions, one cure. **Lane CLOSED.** Not in its hands: [FU-QR-STORAGE-LIFECYCLE] (owner infra) · [FU-GRADER-5MB-COPY] + new **[FU-GRADER-COULDNOTREAD-REASON]** (the "Couldn't read" chip is one generic message because the grader returns only a boolean — both are the FORBIDDEN grader ⇒ ONE batched owner-approved pass; owner-found live-verifying #454, **not a regression**) · [FU-UPLOAD-GUARD-CONVERGE] · [FU-QR-CI-QUESTION-PHOTO] · [FU-CI-DROPZONE-PDF-COPY]. **Branch cleanup: the owner does ONE sweep** (6 branches + 4 worktrees) when ready — never auto-approved (§3).) _Prior sync `c132f27`:_ (**#451 — CHECK & IMPROVE: THE UPLOAD GUARD THAT NEVER EXISTED. Owner BYTE-REVIEWED the pushed diff, then LIVE-VERIFIED.** **PR 1 of 2** (3 files: `uploadLimits.ts` +70 · `DesktopCheckImprovePage.tsx` +51/−6 · `CheckImprove.tsx` +54/−6). **C&I had NO client-side upload guard AT ALL — not a wrong ceiling, NO ceiling:** neither page read `file.size`, neither imported `uploadLimits`, all four inputs went straight to FileReader→base64→grader — **live on real students, desktop AND mobile, independent of QR**. **SIZE:** a 10 MB PDF hit `readJson`'s 5 MB body cap. **TYPE:** `accept="image/*,application/pdf"` vs the server's exact `{jpeg,png,pdf}` (`mentorImageSupport.cjs:3`) ⇒ a WEBP/GIF/BMP passed the picker and died server-side. Both = the forbidden **"uploaded, then dead"**; both now refused **at the picker**, with the copy+constants of the panels #443 fixed. **★ `accept` is a HINT, not a guard** (every OS dialog has an "All files" escape) ⇒ **no `accept` change, no server change, no new number**. **ALL FOUR sites** — the question photo rides the SAME request to the SAME body cap. **NO COMPLETION CELL MOVES** — C&I was already ✅ across the board; this is **§2a bug-fix DEPTH** on an existing surface (like #348/#420). **★ The C&I `Verified` ✅ is NOT re-claimed on this pass** — the owner verified **the upload guard**, not C&I's whole live checklist; the row's own text still carries its pending items. *(Same #444 precedent that held for QP/HPQ/TopicHub under #447 — never flip a surface's Verified on someone else's pass.)* **★★ THE LESSON, worth more than the fix — CARRY THE QUESTION FORWARD, NEVER THE EXPECTED ANSWER:** the carried instruction was *"look for a THIRD copy of the 5 MB constant"* — **there was none, and that was the CORRECT result of a REAL bug**, because the bug had a different SHAPE (no guard at all). Run it literally ⇒ find nothing ⇒ report *"C&I is clean"* ⇒ **close a live bug as a pass.** The question that worked: *"what does C&I ENFORCE?"* **★ Shared `checkUploadFile()` in `uploadLimits.ts`, not 4 more inline copies** (that file's mandate is literally "THE ONE PLACE … so the number and the words a student reads can never drift apart"); behaviour+copy verbatim, duplication not ⇒ new **[FU-UPLOAD-GUARD-CONVERGE]** (CT/Worksheet keep their inline copies; behaviour-neutral, own PR). **★ `subject` REQUIRED with no default** (mirrors `QrAnswerHandoff`'s `mode`) — 2 of 4 sites are QUESTION inputs, so verbatim *"…photo of your answers."* would have told a student to photograph the wrong thing. **★★ The refusal does NOT reuse the grade-failure channel** — desktop's `errorMessage` hard-codes *"Press Retry to call the grader again."*, a lie for a file that never reached the grader ⇒ dedicated per-input error states. **★ [FU-SOLUTIONCHECKER-STALE-ANSWERTAB]'s C&I question ANSWERED: C&I is IMMUNE** (both grade paths are plain `async function`, not `useCallback`) ⇒ that FU is **SolutionChecker-specific, not a family**; C&I is safe **because it never reached for memoization**. **NEXT: [FU-QR-CI-WIRE] (PR 2 of 2) — the last wire, DESKTOP-ONLY** (mobile gets the guard, no QR — `QrAnswerHandoff` returns null <1024px); ⚠ do NOT re-run the spent 5 MB-constant check. **10th stale-base catch** (docs #450 mid-build ⇒ rebased onto `13fc1b0`, gates **re-run**). ⚠ **#452 MERGED while this docs PR was being written (the 11th catch, an hour after the 10th)** — product-only, zero overlap; rebased onto **`a9798e7`**. **The live trunk is `a9798e7`+, not `c132f27` (that is only #451's squash).** **Branch cleanup ON HOLD** (§3).) _Prior sync `0e42e16`:_ (**#448 — TUTOR STAGE 3: THE EXPLANATION PANEL is LIVE. D-TUT-13 closed; the fresh tutor's 3-stage build is DONE (Stage 1 + 2 + 3 all live).** The tutor could teach trigonometry without ever showing a triangle; now it shows the right diagram — **or honestly shows none**. Panel splits on desktop / overlays on mobile (Stage 1's scaffold already encoded both); resolves a concept **exact-match-or-nothing (D-TUT-15)** — unknown concept → nothing, **never a wrong figure**, never `concepts[0]`. The model signals a diagram via the **`[[figure:<conceptKey>]]` sentinel** (mirrors `[[offer:…]]`; server-stripped + validated against the topic's **closed curated set** ⇒ a hallucinated key yields **no panel**). **Interactives are an OFFER, never auto-embedded** (19/54 rows' best asset is a whole-chapter interactive). **5 of 7 hard gaps filled as committed `.svg`** — **NCERT-provenance-traced, 3 traced / 4 original, each labelled** (*"NCERT drew it" and "we drew it" are different claims*). **★★ D-TUT-16's AI-diagram cache DELIBERATELY NOT BUILT** — it mirrors `stepSolution.cjs`, which **no-ops in prod** ⇒ as specified it would **pass every gate while regenerating an unreviewed diagram per student**; gaps offline-authored instead. **★★ The curated catalogue's own header LIED** (`conceptKey = slugified conceptLabel` is false for **46/54** rows) — slugifying the live label would have **silently blanked 85% of concepts** ⇒ lookup on the EXACT label / the STORED key, never a re-derived slug. `buildTutorPath` gained an **optional** `questionId` seam (omitted ⇒ byte-identical). **NO COMPLETION CELL MOVES beyond the Tutor row's text** — this is the Tutor surface's own last major piece (§2a depth elsewhere: none). **★ `Redesigned` stays 🟡 deliberately** — the fresh engine is complete but **the OLD tutor is still live on Topic Hub** (`ConceptTeachDrawer` mounted by `ConceptSpine.tsx:692` + `TopicHub.tsx:1789`), so [FU-TUTOR-LEGACY-RETIRE] must close first; its "dead files" premise was **verified WRONG** — only `TutorDrawerV2` is dead. **★ `Verified` ✅ is carried from Stage 1+2's own pass and is now PARTIALLY STALE — Stage 3's live-verify (atmospheric refraction + the reactions scheme) is PENDING**; not flipped either way, per the standing rule that a Verified cell moves only on that surface's own pass. **★ NEW STRUCTURAL CONFLICT CLASS:** two lanes each adding an ops check **always** collide on `package.json`'s script line + `test:matrix:all` chain — **always additive, never a real collision**; do not re-diagnose. _Prior sync `d99c14d`:_ (**#447 — QR PR-2: SCAN-TO-SEND is LIVE on QUICK PRACTICE + HPQ + TOPIC HUB, owner LIVE-VERIFIED on the deployed app (all four checks green).** **ONE file (`SolutionChecker.tsx`, +62/−7), ONE wire, THREE surfaces** — QP + HPQ + TopicHub all render `SolutionChecker`; the #441 channel reused verbatim. **NOT C&I** (it owns its own upload code; `DesktopCheckImprovePage:1714` is a COMMENT — re-verified at `d99c14d`) ⇒ [FU-QR-CI-WIRE] is the last wire. **NO COMPLETION CELL MOVES** — Quick Practice / HPQ / Topic Hub were already ✅ Built; this is cross-surface **DEPTH** on existing surfaces (§2a), exactly like #441/#443, #353, #344 and #420. **★ Their `Verified` cells are DELIBERATELY NOT FLIPPED** — the owner verified **the QR** on those screens, **not each surface's own live-verify checklist**; flipping a surface's Verified on someone else's pass would be over-claiming. **This is the same call #444 made for Full Mock, and it is the standing precedent.** **Seam contract honoured:** `mode="photo"`, attached as a **sibling of the dropzone INSIDE the upload panel — never a third peer**; 360px safe twice over (two `flex:1` peers unchanged AND `QrAnswerHandoff` returns null <1024px). **★★ The CT wire was NOT copy-pasteable** — the phone's picker keeps `accept="...,application/pdf"` in EVERY mode (`capture` only hints, it does not restrict) ⇒ a PDF can arrive in `"photo"` mode, and since `SolutionChecker` has an `imagePreview` branch gated on `!isPdf` it needed the **full FIVE-field tuple** or a QR PDF renders broken in `<img>` (owner verified this exact case). **The QR-session reset needed NO code** — `!hasFile` gates the mount ⇒ delivery unmounts, clearing remounts fresh. **#447 also closed `SolutionChecker`'s OWN 5 MB copy lie** → `uploadLimits.ts` (3.5 MB). ★ **NOT a surface #443 missed** — [FU-QR-SOLUTIONCHECKER-WIRE] was logged **BLOCKED** because the QP lane owned the file when #443 shipped; #443 correctly scoped to the three host panels then free. **SolutionChecker was the one correctly HELD BACK for PR-2 — the seam contract WORKING AS DESIGNED, not a lapse.** New **[FU-SOLUTIONCHECKER-STALE-ANSWERTAB]** (latent, pre-existing #436: `handleCheck` reads `answerTab` at `:343-344` but omits it from its deps at `:404` ⇒ the send can silently disagree with the screen — the exact failure the gate's own comment says it prevents; needs gate-vs-deps reasoning **and a negative test proving it fails pre-fix**). [FU-SOLUTIONCHECKER-TEXT-XOR-IMAGE] decided for QR's purposes (tab-gated ⇒ moot, **no data-loss path shipped**) but stays open on its original terms. **Branch cleanup ON HOLD** at the owner's instruction.) _Prior sync `ad2a9b2`:_ (**#445 — GRADER `objective` FLAG (§2) + ATTEMPT-DEDUP `mode` DROP (§4b), owner LIVE-VERIFIED on the deployed app.** Two bugs, ONE root: **nothing client-side could tell an objective question from a subjective one** — hence both the "+0 marks" chip AND the attempt double-count. **NO COMPLETION CELL MOVES** — Quick Practice / Check & Improve / Worksheet were already ✅ Built; this is a **grader-correctness + display fix across them** (§2a DEPTH), exactly like **#348**, which was the same shape. **§2:** the grader has always (PR-348) zeroed per-step marks on an objective question BY DESIGN — the whole 0-or-full mark lives at **ANSWER level** — but never SAID so, so five sites printed "0 marks / +0" per step under a "Full marks 1/1" header, reading as *the student scored 0 on every step*. One **additive** `objective` field from **BOTH** grader functions (`handleCheckSolution` + `normaliseStructuredResult`, the keep-in-sync pair, **patched together**) → five chip sites **suppress the chip, KEEP the annotation**. **Gated at the VIEW, not grade-time** (stored scorecards froze the old 0s; pre-`ad2a9b2` ones carry no flag → old chip, honestly stale, never rewritten). No score change; **subjective byte-identical**. ★ **The flag had to reach FURTHER than the five chips** — through the TYPES they render (`WorksheetQuestionGrade` `aiClient.ts:407` + `CiGradedQuestion` + four print-props builders); the client parse is a **generic passthrough (`handleJsonResponse<T>`) ⇒ NO grade-service edit** (mirrors the PR-2 `topicSlug` precedent). *Lesson: trace to the TYPE the renderer consumes; never assume the server response object reaches it directly.* The dead 1-mark banner was deleted **with the CORRECTED reason**: it is dead by a **DATA invariant** (all 3,087 `marks===1` bank rows are `section:"A"` → objective → clamped) **plus surface gates**, **NOT** by the clamp (which only runs `if (questionIsObjective)`, so a 1-mark *subjective* question would have fired it). **§4b:** `attemptDedupKey` included `ctx.mode` and **★ the key IS the Firestore doc id** ⇒ dedup is **all-time + cross-device** (the 400-entry local ring is only a fast pre-check); a wrong-click then a graded typed answer on the same question minted **TWO permanent attempt docs** and progress counted it twice forever. Dropping `mode` collapses exactly that pair; **the score stays in the key so 0/1 vs 1/1 never collapse**; `mode` still lives in the attempt DOCUMENT. **Owner live-verified all three:** chip gone + annotation kept · 5-mark subjective still shows per-step marks (**the gate did not leak into the subjective path**) · click-wrong-then-grade = **+1, not +2**. **Verified cells deliberately NOT flipped:** the owner verified *this fix* on those screens, not each surface's own live-verify checklist — flipping on someone else's pass would over-claim (same rule as #441's Full Mock). **★ UNBLOCK: `SolutionChecker.tsx` is VACATED** (#445 was the last lane holding it; CI `lane-overlap` PASS) ⇒ **[FU-QR-SOLUTIONCHECKER-WIRE] (QR PR-2) dispatched to a fresh agent** — ⚠ **its line numbers MOVED; re-derive.** **Surfaced a cross-surface bank defect [FU-BANK-SUBJECTIVE-FORMAT-IN-SECTION-A]** (§2a): **117 one-mark rows carry a SUBJECTIVE format yet sit at `section:"A"`**, so the bare `s === 'A'` classifier grades them **binary 0/full** — the mirror of [FU-BANK-SCARCE-BAND-MISBANDING]; owner/CBSE ruling needed. New [FU-STEPMARKCHIP-EXTRACTION] (the five chips are five INLINE re-implementations — inline chosen deliberately so the diff stayed narrow and did not hold the QR/paywall lanes hostage for a behaviour-neutral refactor). **§7 (paywall) remains UN-DISPATCHED**; its §7.7 test parked at `scratchpad/PR2-paywall-7.7-test-DRAFT.mjs`.) _Prior sync `5aaaeec`:_ (**#441 + #443 — QR DESKTOP→MOBILE ANSWER UPLOAD LIVE, owner live-verified on the Full Mock result screen.** A laptop student who solved on paper no longer photographs → WhatsApps/emails themselves → saves → uploads: the desktop shows a QR, the phone scans + sends, the file lands in the SAME answer box and **grades exactly as today** — the grader was never touched (QR = DELIVERY, not grading). **NO COMPLETION CELL MOVES** — Chapter Test / Full Mock / Worksheet were already ✅ Built; this is cross-surface DEPTH on existing surfaces (§2a), exactly like #353/#344/#420. **Full Mock's `Verified` ⬜ deliberately NOT flipped:** the owner verified the *QR* on that screen, not FM's own live-verify checklist — flipping it on someone else's pass would be over-claiming. **ONE wire lit THREE surfaces** (CT in-test, CT result, Full Mock) because `ChapterTestUploadPanel` is shared by `ChapterTestPage:711` **and** `FullMockPage:1172` — *wire the shared component, not each page*; [FU-FULLMOCK-NO-UPLOAD-PANEL] was WITHDRAWN (a directory listing is not a dependency graph). Channel = Firebase Storage blob + a Firestore coordination doc, both server-side via firebase-admin; **no rules change, no deploy step, no new dep/env var** (admin bypasses both rule sets; no client touches either → the deny-alls are correct and protective). Security: a **two-token split** makes write-only literal (`uploadToken` in the QR writes only; `pickupToken` never leaves the desktop, reads once + destroys); 256-bit, sha256-hashed at rest, single-use, delete-on-pickup, never logged; **per-UID caps, never per-IP** (shared school wifi / carrier NAT would throttle a whole school). **#443** also fixed a **live PRE-EXISTING bug: "PDF up to 5 MB" was never spendable on EITHER path** — base64 ×4/3 ⇒ 6.67 MB vs `readJson`'s 5 MB cap, so a 4-5 MB PDF attached **on the desktop, no QR**, passed the picker and died at the grader; measured ceiling **≈3.68 MB**, limits unified in `src/services/uploadLimits.ts`, guarded by a **negative-tested** cap-arithmetic assertion (`test:qr:channel` 46/46, in `test:matrix:all`). **★★ THE CREDENTIALS TRAP:** `verifyIdToken` working proves NOTHING about service-account credentials (it needs only the project id + Google's PUBLIC certs) — the deploy log read `credentials: explicit` **only because the key was already set for the pre-existing `[share]` feature**, not because auth implied it. **CORRECTED:** `SolutionChecker` does NOT reach C&I (its only importers are QP + HPQ + TopicHub; `DesktopCheckImprovePage:1714` is a COMMENT, not an import) ⇒ [FU-QR-SOLUTIONCHECKER-WIRE] (PR-2, blocked until the QP lane vacates that file) + a separate [FU-QR-CI-WIRE]. New owner-infra [FU-QR-STORAGE-LIFECYCLE]; new [FU-GRADER-5MB-COPY].) _Prior sync `fd57db1`:_ (#435 — **[FU-MATHTEXT-COMMAND-CORRUPTION] CLOSED + owner-live-verified: MathText protect-then-promote.** The shared maths renderer no longer mangles bare LaTeX commands — the tutor renders cos²A/sin²A as real maths and every consumer surface is unchanged. Fixed BY CONSTRUCTION (protected-span model), no lookbehind, so AB^2/CO_2/cm^2 stay byte-identical. Zero consumer edits. NEW ★[FU-CI-GATE-VITEST]: four vitest suites exist and none run in CI. _Prior sync:_ `65fdf85` (#432 — **TUTOR STAGE 2 COMPLETE + owner-live-verified (360px): the round-trip.** #428 shipped the durable session + round-trip; #432 fixed the six owner-verified bugs: (1) Practise routes to Quick Practice concept-filtered (NOT the worksheet builder), returns via `practiceInsights`; (2) the holding banner clears on return/re-engage/dismiss — never stuck; (3) ONE intent-driven CTA via the `[[offer:…]]` sentinel (server-stripped), never a standing pair; (4) demonstrations pull a VERIFIED bank question (`selectBankQuestions`, `solutionSteps`-filtered), self-gen fallback; (5) prompt mandates full equation wrapping; (6) C&I leg intact. Fresh engine, honesty guard structural (reads `practiceInsights`/`sessionRecords`, never grades). **Tutor row updated below: Mobile 🟡→✅ + Verified 🟡→✅.** Stage 3 (explanation-panel visuals) = separate dispatch, not started. Live FUs: [FU-MATHTEXT-COMMAND-CORRUPTION], [FU-QUICK-PRACTICE-DURABLE-SURFACE], [FU-RETURN-TICKET-CONTRACT], [FU-PRACTICE-COUNT-PASSTHROUGH], [FU-TUTOR-INCHAT-QUESTION-UPLOAD].) _Prior sync `d3c7be2`:_ (#425 `2864be9` + #426 — TUTOR STAGE 1 chat shell LIVE, owner live-verified; two Stage-2 carry-ins since resolved by #428/#432 (durable session + mobile-viewport fix).) _Prior sync `a8f36ab`:_ (#423 — **FINAL MOBILE-PARITY SWEEP, owner merged + LIVE-VERIFIED at 360px.** The one-product-one-website rule now holds product-wide: no live user-facing route shows the OLD global brand bar at mobile width. `isMobileSelfChromedRoute` + 4 families mirroring `isDesktopShellRoute` (`/practice/worksheets` · `/topic-hub*` · `/highly-probable*` · the practice-runner regex) + a route-level `<MobileSelfChrome>` applying the shared MobileShell avatar header AROUND the gates (premium-upsell/daily-limit states carry the header). 2 files (App.tsx + its test); pages/gates/DesktopShell byte-untouched. **Cells moved: Exam Trends Verified 🟡→✅ · HPQ Mobile 🟡→✅ + Verified 🟡→✅.** Closes the live subset of [FU-MOBILE-OLD-HEADER-STRAGGLERS] (the other 7 routes verified dead/retiring, deliberately not chromed). New non-gating [FU-LEGAL-FOOTER-LINK] + [FU-MOBILE-SHELL-PADDING-STACK].) _Prior sync `cc84ae5`:_ (#420 — **C&I PR-3: the model-solution CACHE — the Check & Improve surface is COMPLETE.** Scheme-first grading: keyless SUBJECTIVE questions grade against a student-agnostic solution from the EXISTING `step_solutions` Postgres cache (hash → read → generate-from-question-ONLY on miss → Gate-2a quality gate at EVERY write path → write-if-pass) fed into the grader's EXISTING scheme slot; sacred `checkSolution.cjs` diff = +95/−4 deps-injected hooks only (owner byte-review "textbook-clean"); Gate 2b admin evict/regenerate behind fail-closed `ADMIN_FIREBASE_UIDS` ([FU-ADMIN-UIDS-DEPLOY-ENV] = set the env); CACHE_VERSION now prefixes ALL hashes. **[FU-CI-SOLUTION-CACHE] CLOSED; [FU-MODEL-ANSWER-QUALITY] structurally addressed.** No COMPLETION cell moves — server-side DEPTH on the already-✅ C&I surface (§2a); the C&I Verified pass now also covers cache-hit speed / garbled-not-persisted / eviction). _Prior sync `daf403a`:_ (#399 docs on #397 `6db7f1d` — **CT BALANCED PYQ+FRESH MIX** [FU-CT-BALANCED-MIX] CLOSED. Sourcing-only: the Chapter Test now draws each section (A–D) through the SHARED `drawBalancedSet` (`utils/balancedMockDraw.ts`, reused verbatim — the FT helper, byte-unchanged, not forked), mirroring FT's pass-1 per-cell pattern, so a CT paper deliberately mixes real PYQs with fresh authored/extracted questions (~50% PYQ target; honest fallback for thin/zero-PYQ topics). Byte-identical: paper shape/marks/grading/scorecard/numbering + the `MIN_TEST_QUESTIONS` honest gate (confirmed intact after the draw); seed minted in-blueprint (FT's recipe, optional `seed?` test seam) so `ChapterTestPage.tsx` stays byte-unchanged. Exactly 2 files; no forbidden files. New `chapterTestBlueprint.test.ts` proves the seeded wire + zero/thin-PYQ fallback (vitest 22/22). No surface COMPLETION cell moves — DEPTH on the already-built CT surface (§2a). **Surfaced a cross-surface bank defect [FU-BANK-UNRESOLVABLE-MCQ-KEYS]** (34 MCQs whose key matches no option → never scorable in CT/FT Section A; §2a) + [FU-FM-BLUEPRINT-TEST-SEED-LUCK]. Owner live-verify on the balanced mix pending. _Prior sync `e33b9d3`:_ (#395 — **C&I PR-1: Check & Improve is a first-class SessionSurface.** Every graded C&I session now writes a durable `surface:"check-improve"` sessionRecord (idempotent on the durable cross-device `CI-{S}-{TOK}-{NN}` code — the device-local `lt:ci-multi-seq` RETIRED, no shadow path; `couldNotRead`-only sessions write NOTHING; `questionIds: []` — an external upload's concept is unknowable, never fabricated) + `topicSource` provenance tagged onto the EXISTING correction flow (`confirmed`/`inferred`/`mixed`; **`bank-matched` RESERVED-not-emitted** until a real bank-match path exists — [FU-CI-TOPICSOURCE-BANK-MATCHED-RESERVED]; never backfilled) + the 5th `<ResultsScorecard>` variant pair (live + stored; **shell zero-line-diff**; four-type lens only — by-topic is per-question-topic-gated; NO board-readiness/solution key) + the "Your checked papers" overlay panel (locked CT ScoreRing/DotStrip; MIX = plain "Mixed topics" chip, never a guessed topic or fabricated count). Detection/correction/override-log/MI byte-intact; grader/worksheetGradeService/App.tsx/DesktopShell/firestore.rules byte-clean; one forced +4 copy-seam ripple in `SurfaceHistory.tsx` (Record union expansion). Closes [FU-CI-SCORECARD-VARIANT] + [FU-CI-DEVICE-LOCAL-SEQUENCE]; new [FU-CI-TOPICSOURCE-BANK-MATCHED-RESERVED] + [FU-CI-PERQUESTION-TOPIC] (arc PR-2) → then [FU-CI-SOLUTION-CACHE] (arc PR-3/4, owner 3-gate sign-off). **PR-B is now unblocked to aggregate check-improve** — the critical-path successor). _Prior sync `25257c0`:_ (#391 — **FT FINALIZE: Full Test LINKED + cross-device upload-later.** Hub "Full Test" card (locked copy, cta "Open Full Test") + DesktopHome per-subject tiles + MI-panel link all navigate PLAINLY to `/full-mock/:grade/:subject` — MockViewGate the ONLY gate; old-engine entries RETIRED (the /exam-simulation card, the circular un-routed /mock-builder "Practice Paper" card, + a third discovered dead link on Home's mistake-strip — all "weak-area" wording dropped, the board paper has no weak-area targeting). Cross-device upload-later CLOSED: `services/fullMockPaperStore.ts` text-only paper snapshot (paper + frozen objective + §8b focus — never the answer image/typed answers) at `sessionRecords/{uid}/fullMockPapers/{code}` under the EXISTING recursive rule → firestore.rules byte-untouched; fetch + re-seed on pending re-open (also cures localStorage eviction); verbatim honest fallback for true misses. Plus [FU-TOPIC-DISPLAY-TITLECASE] (connectives lowercase in the display fallback) + [FU-SCORECARD-STALE-HEADER-COMMENTS] (comment-only). Executable RTL nav proof `DesktopPracticePage.fullTestNav.test.tsx`. Resolves all four FT FUs; new [FU-RETIRE-EXAM-SIMULATION-LINKS] + [FU-VITEST-PREEXISTING-FAILURES]. Owner live-verify on the stable link pending → the FM Verified cell flips on that pass). _Prior sync `99b1d2a`:_ (#388 — bank-expansion Batch 4 our-environment +80, data-enrichment parallel track: assembled bank → **7,342**, our-environment 169→249; both scarce bands honest-stop; owner byte-review added the **two-direction syllabus boundary** (deleted-chapter + Class-11/12 adjacency) as standard, and the scarce floor is now **≥75 distinct**. NEXT: reproduce/heredity. ~22 topics remain). _Prior sync `f6522d0`:_ (#387 — Full Mock) (#387 — **FULL TEST (Full Mock) BUILT + LIVE**: per-subject 3-hour board mock, 38Q/80mk A–E, DUAL-SOURCE union (canonicalQuestionBank + the live predicted banks) through the new shared `drawBalancedSet` (utils/, seeded, honest PYQ/fresh re-weighting), persisted wall-clock timer + whole-paper resume (§8a), §8b focus aggregates (additive optional `focus?`), scorecard = section lens (exact) + CHAPTER lens (derived; `sectionBreakdown` null), FM-{S}-{NN} records + MI via the SHARED grader. Gated by `MockViewGate`; route LIVE but UNLINKED [FU-FM-HUB-ENTRY]; cross-device upload-later honestly limited [FU-FM-CROSS-DEVICE-UPLOAD]; CT can now reuse the helper [FU-CT-BALANCED-MIX]. Resolves [FU-FM-RESOURCE-PREDICTED]). _Prior sync `ce34b3e`:_ (#384 + #385 — **bank-expansion lane IN PROGRESS**, data-enrichment parallel track: Batch 2 real-numbers corrective +12 (exhaustive re-sweep + D/E exhaustion audit proving the ~24-distinct-method ceiling) and Batch 3 life-processes +136 (first SCIENCE batch — extract-max 75 A/B/C, scarce D→53/E→54 clearing the ≥50 distinct floor). Assembled bank → **7,262**. Exhaustive per-source-sweep discipline confirmed working (75 vs 23 = reservoir depth). New FIGURE-PENDING safeguard doctrine. Manifest `docs/bank-expansion-review-queue.md`; surfaces GATED until student QA. NEXT: another Science topic). _Prior sync `3866a94`:_ (#381 — **bank-expansion lane IN PROGRESS** (data-enrichment parallel track, NOT an architecture-gate surface): Batch 1 real-numbers +30 lands the assembled bank at **7,114** under the owner-set **band-scarcity floor** (A/B/C extract-max; scarce E/D/proofs ≥50 genuinely-distinct + honest-stop). Manifest `docs/bank-expansion-review-queue.md`; surfaces that serve these stay GATED until trusted-student QA. Correction for future batches: A/B/C extract-max must EXHAUSTIVELY sweep all sources with a per-source table [FU-BANK-EXPANSION-SOURCE-SWEEP]). _Prior sync `5bd148c`:_ (#380 — **CT concept-lens + bare full-screen**: the CT full scorecard now derives a by-CONCEPT (subtopic) lens between the A–D section lens and the four-type (derive-don't-persist, `sectionBreakdown` null, honest-unknown, ALL resolved concepts sorted by loss), and `isBareFullScreenRoute` makes CT chrome-less full-screen (legacy header + mobile BottomNav suppressed on `/chapter-test`, both widths; `DesktopShell` byte-unchanged — it was the NON-shell navbar); **[FU-CT-CONCEPT-LENS] + [FU-CT-HEADER-UNIFORMITY] CLOSED** — CT is ready to flip live at `MockViewGate` at the owner's discretion. New open [FU-RETIRE-OLD-GLOBAL-HEADER]). _Prior sync `e54ab8c`:_ #374 — **Chapter Test BUILT to the locked spec**: two-phase grading, native canonical sourcing, Universal `<ResultsScorecard>` chapter-test variant live, `CT-{S}-{TOPIC}-{NN}`, behind `MockViewGate`; scorecard is section+four-type today — CONCEPT lens + header-uniformity are the pending fast-follow before the gate flips). _Prior sync `8fb1ad6`:_ post-**notes-fan-out-COMPLETE** (all 26 chapters specced + independently audited; #365/#368/#370/#371/#372) + **NCERT page-cites LIVE** (#375 offset map + owner-hosted PDFs + public-read rule + CORS) + **coordination automation LIVE** (#366: lane-overlap REQUIRED check + state-board ledger + CODEOWNERS + trunk ruleset). [FU-LEDGER-CLICKABLE-CITES] PR #376 (source-ledger `p.N` clickable) in review. _Prior sync `aa7e778`:_ (#357 — the worksheet builder is now **CONTEXT-AWARE**: it reads scope/subject/topic(s) from the URL (single source of truth; extends the `buildDesktopWorksheetPath` idiom), DELETES the `topics[0]` guess, and redirects ONCE to the hub when no valid topic — **App.tsx untouched** (the hub already passed the params; the builder was ignoring them). **Multi-topic MI now aggregates** across all in-scope weak topics (`rankedInScopeWeakTopics` + `allocateMiCounts`: proportional marks-lost split + FLOOR [a chosen topic is never dropped] + CAP [≈50% at N≥3, 60/40 at 2] + availability gate + per-topic level-2 section skew); `allocateCounts`/`MI_BOOST` byte-unchanged; enrichment count + named topics **drawn-gated**. Sticky bar removed (mobile Preview restored); accessible `role="switch"`; 360px reflow; Home Worksheets card → hub. 8 files, App.tsx + all forbidden untouched; CI quality-gate GREEN; 6-dim review 5 clean + 1 honesty finding fixed. **No COMPLETION cell moved** — entry-flow + MI DEPTH on the existing Worksheet surface (§2a), owner live-verified. Closes [FU-WS-ENTRY-CONTEXT]/[FU-WS-MULTITOPIC-MI-AGGREGATE]/[FU-WS-PREVIEW-BUTTONS]/[FU-WS-MI-SWITCH]; [FU-WS-MI-COPY] HALF-landed; new [FU-WS-SCOPE-DERIVE] (Customise tick never derives scope) + **[FU-TOPICKEY-UNIVERSAL] (P0)** — raw-slug topic matching → 4 Science chapters (chemical-reactions-equations/acids-bases-salts/metals-non-metals/reproduction ≈1,180 Qs) return ZERO; Chapter Test + Full Mock carry the same LATENT defect (see §2a)) (#353 — the worksheet builder's Mistake-Intelligence is now **SCOPE-RELATIVE** (weakness resolved WITHIN the chosen scope via the new pure `worksheetMiSelector.ts` — `scopeHotspot` vs `globalHotspot`; the single locked box SPLIT into its true causes so a student WITH MI data sees the real weak topic NAMED + a one-tap remedy, never the false "grade a worksheet first") and **within-topic section enrichment is LIVE** for single-topic scope (section derived from each mistake's `totalMarks` via the CBSE band proxy — honest-unknown for non-band values, never a fabricated section; additive `orderPoolBySectionBoost` in `worksheetModel.ts`, capped at real availability, gated on the drawable pool so the toggle is never a no-op; cross-topic `MI_BOOST` byte-unchanged; NO schema change / no new writes). Desktop `position:sticky` Preview footer + drawer-foot Preview. 6 files, all `components/worksheet/`, zero forbidden; CI quality-gate GREEN + Vercel PASS; 11-agent review → 5 low findings all fixed + SHIP. **No COMPLETION cell moved** — Worksheet MI was already ✅; this is scope-relative DEPTH on an existing surface (§2a), owner live-verified. Closes [FU-MI-SCOPE-RELATIVE] + [FU-MI-ENRICH-WITHIN-TOPIC] + [FU-BUILDER-PREVIEW-AFFORDANCE]; new FUs [FU-WS-ENTRY-CONTEXT], [FU-WS-MULTITOPIC-MI-AGGREGATE], [FU-WS-PREVIEW-BUTTONS], [FU-WS-MI-SWITCH], [FU-WS-MI-COPY], [FU-MOBILE-VERIFY-GAP]) (#352 — objective **answer keys** repaired: the bank's objective rows are now **DETERMINISTICALLY gradable except 13 rows** pending a real-paper lookup (`docs/objective-answer-key-review-queue.md`). 89 defects re-derived via an AST scanner using the grader's OWN `normaliseOption`/`resolveOptionIndex`/`isObjectiveType` — 76 fixed (61 corrupt MCQ keys set to the exact correct EXISTING option + 15 Assertion-Reason rows given the 4 standard CBSE `options[]`), 13 honestly manifested (corrupted/duplicated options + figure-dependent → grader defers to model). `correctOption` never introduced — the key stays `q.answer`; grader `server/routes/*` byte-untouched; 43 bank files + 1 manifest; only `q.answer` + AR `options[]` changed. **No COMPLETION cell moved** — a bank data-correctness fix completing #348's guarantee. [FU-BANK-CORRUPT-KEYS] CLOSED except the 13; new FUs [FU-BANK-KEY-REVIEW-QUEUE], [FU-SECTION-A-VSA-HALFMARK] (99 Section-A written-answer rows all `marks:1` → 0/1, no live hole), [FU-BANK-GARBLED-DISPLAY-TEXT]) (#349 — the worksheet BUILD view redesigned to the locked "A · Smart default": a smart-default hero (`{mode} · {topic}` + real exact chip line) → primary **Preview worksheet →**; ALL prior controls preserved behind **Customise** (progressive disclosure, nothing removed); MI as ONE honest toggle; a new **Preview step** (real section bar/counts/marks, computed enrichment callout, 3 real samples, honest bank-shortfall) — one `candidate` set feeds hero→preview→generate so the shown count IS the generated count. **History moved OFF the page body to a header-control OVERLAY** (`WorksheetHistoryPanel` reuses SurfaceHistory rows + read-only `<ResultsScorecard>` re-open; `embedded`/`pendingOnly` props only) — the bottom in-page history section is GONE. **Pending uploads surface via `WorksheetPendingBanner`** (1 → `Upload now` deep-links + attaches to the EXISTING record, #338 idempotency, no dup; ≥2 → panel filtered to pending). PDF filenames now carry the unique code. The generated "worksheet is ready" view / `WorksheetGradePanel` / Practice hub / Topic Hub were **NOT touched**. **No COMPLETION cell moved** — Worksheet was already ✅ Built/Redesigned; this is discovered-scope DEPTH on an existing surface (like #344), logged §2a. Closes 2 of the 3 owner-found worksheet bugs post-#344 (PDF filename + history placement; grader MCQ = #348). New follow-ups: [FU-MI-SCOPE-RELATIVE], [FU-MI-ENRICH-WITHIN-TOPIC], [FU-BUILDER-PREVIEW-AFFORDANCE]) (#348 — the grader now enforces the **OBJECTIVE-scoring invariant** uniformly: an MCQ / Assertion-Reason / Section-A question scores **0 or FULL — never fractional, never step-distributed**; working is analysed ONLY to classify the mistake type. One shared `server/routes/objectiveScoring.cjs` (+ parity-pinned client twin) is called by BOTH grader functions → byte-aligned. **TWO paths, and they do NOT carry the same guarantee — be explicit:** **(a) bank-sourced** grading (the Worksheet surface, and bank-sourced Check & Improve, which forward `q.answer` = the option text + `q.options`) is **DETERMINISTIC** — a normalised key compare that OVERRIDES the model; **(b) keyless C&I uploads** (a student's own question, no bank key) are **MODEL-CLASSIFIED-then-CLAMPED** — a detect-step `objective` flag routes to the model's BINARY verdict clamped to 0/full, gated by a **≤1-mark safety rail** so a multi-mark subjective is never clamped on a model guess. Corrupt bank keys / no-`options` AR rows also fall to (b) [FU-BANK-CORRUPT-KEYS]. **No COMPLETION cell moved** — Check & Improve / Quick Practice / Worksheet were already ✅; this is a grader-correctness fix across them, owner live-verified on both paths. `objectiveScoring.cjs` is the single source of truth Chapter Test / Full Mock will consume when their MCQs move server-side. New follow-ups: [FU-OBJECTIVE-COST-SKIP], [FU-BANK-CORRUPT-KEYS], [FU-CI-SCORECARD-VARIANT] (C&I writes no session record / isn't a `SessionSurface` — an OPEN design question, not yet decided-necessary, so C&I stays Locked)) (#344 Progress-Journey ARC · PR-3 — the **per-surface Worksheet HISTORY** section (`components/results/SurfaceHistory.tsx`) on the WorksheetGenerator BUILD view: a "Your worksheets" list (code/title/date/score-chip-or-awaiting-pill/four-type dot-strip + honest empty) with an honest-or-silent `getSubjectProgress` vs-last-time chip, each row re-opening its STORED record as a READ-ONLY `<ResultsScorecard>` (no `perQuestionRef` reconstruction). **CONSUMES the store read-only** (no mutation). The **Worksheet surface now carries its history section**; the arc READ-LAYER (histories reading `sessionRecords` via `progressStore`) is proven on the one LIVE graded surface — CT/FM history are deferred `SURFACE_COPY` seams pending those rebuilds. No COMPLETION cell flips (Worksheet was already ✅ Built/Redesigned; this is discovered-scope depth, logged §2a). PR-2 files edited additively (no live-scorecard regression)) (#341 Progress-Journey ARC · PR-2 — the **Universal `<ResultsScorecard>`** extracted from `WorksheetScorecard`: one shared results SHELL + a typed 4-surface variant interface (`components/results/`). **Worksheet + Quick Practice variants LIVE**; Chapter Test + Full Mock = `deferred:true` config seams pending those surfaces' rebuilds. Presentational — writes nothing (§1a: Quick Practice writes no session record; worksheet write stays in `gradeWorksheetAndRecord`). **No COMPLETION cell moved** — both live surfaces were already ✅ and the worksheet scorecard is byte-identical (the non-regression gate); this consolidates shared chrome the later arc surfaces (histories/Me/CT/FM) will reuse) (#338 Progress-Journey ARC · PR-1 — the session-record **DATA LAYER**: `sessionRecords/{uid}/records/{code}` store + `progressStore` aggregation reader + durable cross-device `#NN` + `perQuestionRef`; the connectivity spine the scorecard/histories/Me/Home arc reads — **no surface COMPLETION cell moved** (data plumbing; the surfaces that read it are later arc PRs); new Firestore collection → rule deployed `dc73360`) (#330 Light extraction — CONTENT-ONLY: question-bank supply enriched 326→767 with bound figures; **no surface cells moved**) (post-#297 Z3 figure-binding — Quick Practice now renders bound source figures as `<img>` for Z3 case-based questions). **Maintainer ritual:** see §3 — it is updated in the SAME post-PR handoff the agent already does, reconciled by the cofounder each session, and the Verified column flipped by the owner.

This is the **glanceable "are we there yet" view** that the per-PR CURRENT_STATE/ROADMAP can't give. CURRENT_STATE answers "what did the last PR do"; THIS answers "how complete is each surface, and what's left to soft launch."

---
## 1 · THE NORTH STAR (definition of soft-launch-ready)
Soft launch = **every CORE student surface is Built + Redesigned (to the new grammar) + responsive on Desktop AND Mobile + MI-connected (where applicable) + owner Live-verified.**
Goal ORDER (owner-set): **(1)** finish & converge the surfaces + make everything responsive on both widths + wire MI through-and-through (the *architecture & plumbing*) → **(2)** then enrich data gradually (extraction, notes authoring). Data enrichment is a PARALLEL track and is NOT part of the architecture gate — a surface can be "done" structurally with a thin-but-honest bank.

**Legend:** ✅ done · 🟡 partial / in progress · ⬜ not started · ⚠️ done-but-owner-live-verify-pending · — not applicable
**Mobile column rule:** ✅ only if the surface renders the CURRENT design on mobile (one responsive component or verified mobile build). 🟡 if it still routes to an OLD/dead mobile surface — that drift is the known launch risk and must reach ✅.

**⚠️ THE SCOPE-IS-NOT-FIXED PRINCIPLE (read before trusting any row).** A surface's definition-of-done is NOT known in full up front. Building a surface *discovers* necessary pieces — the worksheet generator revealed the scorecard, the nomenclature, the durable record, the parent/teacher foundation; none was in the original plan, all proved necessary. This is scope *discovery*, not scope creep. So every row carries a **Scope** status, and the completion cells (✅/🟡) are read **against the surface's CURRENTLY-KNOWN scope** — which can grow.
- **Scope = `Settling`** — still discovering necessary detailing; the known scope is probably not the whole scope. Do NOT read a row of ✅s as "finished" while Scope is Settling.
- **Scope = `Locked`** — the shape has stabilized; nothing obvious remains; ✅s can be trusted as truly done. A `Locked` surface that later sprouts a new necessary piece is a useful signal (the lock was premature) — reopen it to `Settling`, log the discovery (§2a + the repo's DECISION_LOG), and usually spawn a NEW PR rather than swell an open one.

## 2 · THE MATRIX
Cells are read against each surface's CURRENTLY-KNOWN scope (see the Scope column + §4 discovered-scope log).
| Surface | Scope | Built | Redesigned | Desktop | Mobile | MI | Verified | Next action |
|---|---|---|---|---|---|---|---|---|
| **Check & Improve** | Settling | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | **#476 (trunk `cca0a5d`, owner byte-verified) — HOSTED AS AN IN-TUTOR OVERLAY: the page gained an additive, `overlay`-gated host prop (the tutor opens the real page in an in-tree panel); a direct `/check-improve` visit is BYTE-IDENTICAL (overlay gate 24/24, convergence 92/92, grader/`SessionRecord`/`ResultsScorecard` zero-diff). No completion cell regresses. Prior: #472 (trunk `0649e20`, owner byte-verified) — QUESTION-SIDE PARITY COMPLETE: the question uploader now matches the answer side — `<EquationInput>` (math palette, replacing the plain input), a `<QrAnswerHandoff>` in a NEW `"question"` mode, mobile camera/files, and paste; both boxes auto-grow (default OFF, SolutionChecker byte-identical). The `"question"` mode threaded through SIX sites incl. the server allow-list `qrUploadChannel.cjs` — a 6th the frozen five-site map missed. Gate 92/92, qr channel 47/47. NO OTHER surface moved this PR. Prior: **#466–#470 (the convergence arc, trunk `2c59dd2`, owner LIVE-VERIFIED at 360/768/820/1024/1440) — C&I is now ONE fluid responsive component at every width; the mobile twin `pages/app/CheckImprove.tsx` is DELETED.** The route ternary on `isDesktop` is gone; layout is container-relative [`CARD_BASIS`/`HEADER_TITLE_BASIS`, flex-wrap], the 13 `matchMedia` window-media sites removed [F13], so the surface survives an 820px overlay panel — the prerequisite for hosting C&I inside the tutor. Header/lede/How-it-works reworked [#470]; the CI acceptance gate that had silently never run now runs its MI moat check on every PR [#469]. **Restructure + §2a DEPTH; no completion cell regresses.** Prior: **#416 (C&I PR-2) — the FINAL C&I frontend PR MERGED (`a1eaebc`); the surface is COMPLETE desktop+mobile.** Per-question topic via route A2 (client re-runs the EXISTING `/detect-question` — sacred grader untouched) → by-topic scorecard lens + counted `N topics` chip (`topicCount` additive-optional, DISPLAY-ONLY; mixed still feeds no single-topic progress); PDF solution upload on both surfaces; **mobile parity** composing the shared services (`ensureCheckImproveSessionCode` retiring the device-local collision counter, `persistCheckImproveSession`, `ResultsScorecard`, `CheckImproveHistoryPanel`) — #437 stub deleted, no forked grader; `DesktopCheckImprovePage` diff ADDITIVE-only. **[FU-CI-PERQUESTION-TOPIC] + [FU-MOBILE-CI-PARITY] CLOSED.** **#420 (C&I PR-3, `cc84ae5`) closed the arc: [FU-CI-SOLUTION-CACHE] DONE** — scheme-first cached grading (Gate-2a quality gate on every write, Gate-2b `ADMIN_FIREBASE_UIDS` eviction, student-agnostic by construction); grader hooks-only diff, byte-review "textbook-clean". **NOTHING remains on C&I.** Owner live-verify pending on the live path (per-Q topic + chip; PDF-solution + can't-read fallback; 360px; cache-hit speed + garbled-not-persisted + eviction + 503-without-env; [FU-ADMIN-UIDS-DEPLOY-ENV] deploy step). Prior: **#395** first-class `SessionSurface` (durable record + `topicSource` + 5th scorecard variant + history overlay; [FU-CI-SCORECARD-VARIANT] + [FU-CI-DEVICE-LOCAL-SEQUENCE] CLOSED). `bank-matched` topicSource RESERVED-not-emitted. **Equation infra (#429/#431):** the C&I "type" tab AND the shared `SolutionChecker` now use `<EquationInput>` (readable math palette — x^2/sqrt{}/frac{a}{b}/a_1, never raw `\(…\)` — + live KaTeX preview); the 4 graded echo fields render via `<EquationRender>` (= MathText re-export). `\(…\)`/`\[…\]` + readable shorthand is the app's ONE canonical math grammar; grader untouched, harness-proven. Print docs already rendered via MathText; since SolutionChecker is shared, Quick Practice / HPQ / Topic Hub inherit the type-answer widget. |
| **Quick Practice** | Settling | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | **#509 (trunk `41277c1`, owner LIVE-VERIFIED) - "BUILD A FRESH SET" IS ACTUALLY FRESH: the scorecard CTA returned the SAME questions because neither selection input moved - the rotation seed cannot advance in-session (`sessionStartedAt` is mount-once) and the seen-set was never POPULATED (its loader has no `regenerationKey` dep). Fixed additively in `PracticePage` via a `freshSetNonce` on `rotationOffset` (+0 on every existing path) + `buildFreshSet()` carrying the just-displayed ids into `seenQuestionIds`; scarcity now rotates instead of repeating. [FU-PRACTICE-FRESH-SET-NOT-FRESH] resolved; [FU-PRACTICE-CONTROLS-REFRESH-STALE] logged (the "Refresh set" button still carries the same staleness - owner ruled it a separate fast-follow with its own trace + test).** **#501 (trunk `7979a89`, owner LIVE-VERIFIED) - SCORECARD DENOMINATOR FIXED: the "of N" now counts the DISPLAYED set (`filteredQuestions.length`), not the over-fetched pool - a 5-MCQ set read "5 of 75", now "5 of 5". Feed-only; forbidden `ResultsScorecard`/`scorecardVariants` untouched. [FU-QP-SCORECARD-ATTEMPTS-WIPED] resolved; [FU-QP-WRITTEN-BINARY-CHECK] logged.** **#493 + #495 (trunk `273cfe8`, owner LIVE-VERIFIED) — NOW ALSO HOSTED AS AN IN-TUTOR OVERLAY (the C&I twin).** `PracticePage` gained an additive, `overlay`-gated host prop: the tutor opens the REAL page in an in-tree panel seeded with the round-trip URL (`UNSAFE_RouteContext` reset + `<Routes location>` — NO nested Router, the #490 break), with navigation contained to `onClose` so nothing tears the student out of the thread; #495 named the return (scorecard `returnTicket` + close-bar label). A direct/hub `/practice/:grade/:subject` visit is **BYTE-IDENTICAL** (every hunk overlay-gated; the scorecard ticket passed only in overlay mode). Engine/fetch-filter/persist/grader zero-diff. Prior: **#488 (trunk `9edb939`, owner LIVE-VERIFIED both Maths & Science) — MULTI-TOPIC PRESETS LIVE (shape 3c).** A `≥2 topics` hub selection no longer collapses to `topic=<first>`: the QP nav builder emits `topics=a,b,c` [mirrors HPQ], `PracticePage` reads it into a resolved multi-topic list, and the fetch fans out ONE unchanged `buildPracticeQuestionsWithAiTopup` per topic → merged, POOLED-AND-SHUFFLED, ~50% competency floor HARD [`COMPETENCY_FLOOR=0.5`, wins every conflict; topic split SOFT — proportional-to-availability, ≥2 floor, shortfall→richer, never fabricated, via the swappable `topicShare()` (v1=availability)]. Composition core = NEW pure module `multiTopicPractice.ts` [no React/engine/IO]. **Single-topic BYTE-IDENTICAL** [ternary-wrapped, gated `≥2 topics`; only fetch-conditional change is `if(committedMarks==="all")`→`else if`, zero body removals]; **ZERO engine-service edits** [reuses `buildPracticeQuestionsWithAiTopup`/`generatePracticeSet`/`getLikelyQuestionsForConcept`/`selectInRangeFromPool` per-topic]; **`sessionRecords.ts` byte-identical** [a forbidden-file touch was caught via the three-dot-guard false-pass-on-uncommitted, reverted; `topicKeys` moved to the non-forbidden QP service via a post-build override]. Pure 18/18 + router-mount nav 4/4 LOCAL-only (`[FU-CI-GATE-VITEST]`). No completion cell regresses — additive multi-topic branch. Owner-ratified design calls: competency-HARD/topic-SOFT with per-topic spreading; section proportions NOT re-enforced post-merge (`[FU-QP-MULTITOPIC-SECTION-SHARES]`). Un-parks `[FU-PRACTICEHUB-MULTITOPIC-CONTEXT-DROPPED]`; logs `[FU-QP-MULTITOPIC-EXAM-WEIGHT]`. Prior: **#483 → #486 (trunk `889ab6d`, all four owner LIVE-VERIFIED) — THE #481 QP A1 ENTRY IS REACHABLE + NAVIGABLE.** The preset entry (four cards + Customise + optional timer + mobile carousel) is now reachable from the hub [#483 — entry gate re-keyed on `source`: `source=practice`→presets, `source=tutor`/`targeted=1`→auto-build; the fix was the INVERSE of the spec's literal `source==="tutor"` rule, which would have regressed ~21 topic-bearing entrypoints], and escapable back to the chooser both by browser-back [#485 — a real `built=1` search-param history entry; #484's state-only same-URL navigate was a replace/no-op (RR #5362) that shipped broken through all gates + CI + 14/14 (the test never mounted a router), caught by owner live-verify] and the breadcrumb "Back" CTA [#486 — `isBuilt`-aware, label "Back to quick practice"]. The rotation/`seenQuestionIds` engine, `QuickPracticePresets.tsx`, `persistQuickPracticeSession`, grader, MI, the filter wiring, the timer and the hub (`DesktopPracticePage`) are BYTE-IDENTICAL; tutor/targeted auto-build byte-identical. Router-mounted `createMemoryRouter` tests (17/17) but LOCAL-only (`[FU-CI-GATE-VITEST]` — CI doesn't run vitest). No completion cell regresses — restructure of the entry gate only. Prior: none — mature (finish-session scorecard, exact mark-band). |
| **Practice Hub** | Locked | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | **#492 → #494 → #496 (trunk `6d991c0`, owner LIVE-VERIFIED) — REBUILT TO THE LOCKED v6 DESIGN, ROUTING 100% FROZEN.** `/practice-hub` is now a two-step "pick what, then pick how" flow: a scope card (subject · stream · scope, topic selection as an INLINE DROPDOWN for BOTH scopes — single-select radio / multi-select checklist + removable pills) feeding four accent-coloured mode cards (Quick / Worksheet / Predicted-HPQ / Full Test), with a **navy Mistake-Intelligence rail** and a NEW **desktop-only trend sparkline** reading the SAME `getMistakeLogs` history (null — no card — below two days of data; never a fabricated line). ONE responsive component: desktop 2×2 grid, mobile a full-width snap carousel with page dots. **The dropdown is a new INPUT for the EXISTING state** (`topicSlug` / `selectedTopicSlugs`) — which is why every URL could stay identical. **★ ROUTING PROOF:** one capture harness run against trunk AND the redesign, then diffed — **25 URLs (5 scope scenarios × 5 CTA families), diff EMPTY**; pinned in `routingParity.test.tsx`. 22 inbound callers, the route mount and the `/mock-builder` redirect untouched. **★ One owner-approved relaxation:** the timer toggle adds `timed=1` to the SAME scoped builder rather than reusing trunk's `timedDrillPath`, which dropped multi-topic + focus — byte-identical with the timer OFF, scope-preserving when ON. Cut from the UI (routes kept, render removed): blueprint preview, predicted-questions tabs, More-options accordion, quick links, topic-reference panel, and ALL developer language. #494 removed a whole-card `opacity: 0.65` that dimmed every descendant; #496 added accent-tinted resting borders + the **mutation-verified aliveness guard**; #498 deletes the dead `MockBuilder.tsx` and the stale header comment. **⚠ OPEN:** `[FU-HUB-DROPDOWN-ZINDEX]` (avatar menu occluded — fix known, blocked on a FORBIDDEN-list decision) and `[FU-QP-SCORECARD-ATTEMPTS-WIPED]` (QP scorecard "0 of N" — diagnosed, mechanism reproduced, trigger open). Neither regresses a completion cell. |
| **Exam Trends** | Locked | ✅ | ✅ | ✅ | ✅ | — | ✅ | **#416 (C&I PR-2 item E) brought `/exam-trends` under the one-header treatment** ([FU-MOBILE-OLD-HEADER-TRENDS-PRACTICE] CLOSED); **Verified 🟡→✅ — the #423 mobile-parity sweep owner live-verify (360px) confirmed the one-header treatment on the live link.** **#521 - design uplift (presentation-only): rows→cards on the shipped MODE_ACCENT band palette, no grey band, `⋯` popover anchored to its own button, `Open`→`Learn`. Owner-signed BAND_BY_SLUG byte-frozen (owner re-verified: 70 lines / 2,359 bytes / sha `f3b72f1ce8e3549d1d507bcd`). Owner live-verified. No cell moved - the surface was already ✅ across the board.** |
| **HPQ / Predicted** | Locked | ✅ | ✅ | ✅ | ✅ | — | ✅ | **#423 (final mobile-parity sweep) brought `/highly-probable`(+`/:grade/:subject`) under the one-header treatment** — route-level `<MobileSelfChrome>` applies the shared MobileShell avatar header at mobile width, wrapped AROUND `RequirePremium` so the premium-upsell state carries the header too; desktop chrome unchanged; BottomNav preserved. **Mobile 🟡→✅ + Verified 🟡→✅ (owner live-verified at 360px).** · **#505 removed the HPQ "Mock basket" sub-feature (state/panel/"Add to mock" buttons - defunct, fed only the deleted mock-builder); the HPQ surface itself is unchanged.** **WAVE 5D (#608): the evidence-base figure corrected.** HPQ said `drawn from 4 years of papers` while Home said ten; it now reads the new authoritative constant `src/config/evidenceBase.ts`. The brief's three named drift sites were **all wrong**: Welcome's `last 5` was already fixed and pinned, and there is no `3-5` anywhere in `src/config/`. The real drifts were HPQ's `4 years` and Intent's `9 years` - and `pages/app/Intent.tsx` turned out to be route-registered but **unreachable** (`/pricing` 1 inbound nav, `/intent` 0). |
| **Topic Hub** | Settling | ✅ | ✅ | ✅ | ✅ | — | ✅ | **#512 (trunk `e19b2d1`, owner LIVE-VERIFIED) — THE OLD "TEACH ME" DRAWER IS GONE FROM THE CONCEPT ROWS.** Each row now carries exactly one tutor entry — **"Stuck? Ask" → `/tutor`** with the concept pre-loaded — plus "Practise". Until #512 the row rendered an **unconditional** "Teach me" button beside it (opening the old `ConceptTeachDrawer` → `TeachFlow` → `/api/mentor concept_teach`), so students were offered **two** competing tutors; the new `/tutor` supersedes it. Four surgical hunks in `ConceptSpine.tsx` (import · `teachConcept` state · button · mount) — **every new-tutor entry byte-identical**, plus removal of the dead `.lt-spine__btn--teach` CSS and doc-comments still claiming "Teach me stays LIVE as the fallback". The replacement test guard asserts the **absent** case (gone as text, `button` AND `link`) *together with* the **positive** case (every row still has its `/tutor` link) and is **mutation-tested** — re-injecting the button turns it RED. **No completion cell changes; the surface is otherwise untouched.** ⚠ `pages/TopicHub.tsx` (the dead, unrouted premium lesson flow) is **still on disk** — its deletion is forced-but-deferred to **PR-2** because it imports `ConceptTeachDrawer`. _Prior:_ PR-F content (Notes + Examiner's tips) → PR-G delete dead old surfaces |
| **Worksheet** | Settling | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | **BUILDER COMPLETE** (#360): context-aware entry (#357) · **derived scope** (#360: selectedTopics[]+allTopics single source of truth, unified topic picker, honest label) · scope-relative MI with within-topic section skew (#353/#357) · MI 2c copy done · history overlay · pending banner · code-bearing PDF filenames · 360px verified. All builder FUs closed, owner live-verified. **Remaining is NOT a builder defect:** the cross-surface **[FU-TOPICKEY-UNIVERSAL] (P0)** raw-slug topic match still returns zero for 4 Science chapters (≈1,180 Qs) here + latent in Chapter Test/Full Mock — dispatched separately (resolve-everywhere read+write + data migration + CI guard, NOT piecemeal). |
| **Chapter Test** | Settling | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | **BUILT to the locked spec (#374, `e54ab8c`), owner live-verified.** Two-phase grading (Section A objective 0-or-full on submit, PR-348; B–D subjective via the shared worksheet upload path, grader byte-unchanged); Universal `<ResultsScorecard>` chapter-test variant **flipped live** from `deferred:true` (config, not re-architecture); native canonical sourcing (D1, no adapter); `sessionRecords` surface `"chapter-test"` + `CT-{S}-{TOPIC}-{NN}`/#NN; navigator (4 states) / timer / topic-scoped history / pre-submit confirm / downloads. **Behind `MockViewGate` (owner flips at launch).** **#380 shipped the two pre-flip fast-follows:** the scorecard is now **section + concept + four-type** ([FU-CT-CONCEPT-LENS] — by-subtopic marks-lost lens, derived at render / honest-unknown / `sectionBreakdown` null, ALL resolved concepts sorted by loss), and the test surface is **chrome-less full-screen at both widths** ([FU-CT-HEADER-UNIFORMITY] — `isBareFullScreenRoute` suppresses the legacy navbar + mobile BottomNav on `/chapter-test`; `DesktopShell` byte-unchanged, it was the NON-shell navbar). **#397 wired the balanced PYQ+fresh SOURCING ([FU-CT-BALANCED-MIX] CLOSED):** each section (A–D) now draws through the SHARED `drawBalancedSet` (the FT helper, reused verbatim), so a CT paper deliberately mixes real PYQs with fresh authored/extracted questions (~50% PYQ, honest fallback for thin/zero-PYQ topics). Sourcing-only — paper shape/marks/grading/scorecard/numbering + `MIN_TEST_QUESTIONS` gate byte-identical; seed in-blueprint so `ChapterTestPage.tsx` untouched. **CT is ready to flip live at `MockViewGate` at the owner's discretion** (owner live-verify on the visible mix pending). Scope stays Settling until the gate flips; new open [FU-RETIRE-OLD-GLOBAL-HEADER] (product-wide legacy-header retirement — deliberate later) + the cross-surface [FU-BANK-UNRESOLVABLE-MCQ-KEYS] (34 unscorable Section-A MCQ keys, bank lane — see §2a). |
| **Full Mock** | Settling | ✅ | ✅ | ✅ | ✅ | ✅ | ⬜ | **BUILT to the locked spec (#387, `f6522d0`), owner byte-reviewed.** Reads the DUAL-SOURCE union (canonical + predicted) via `drawBalancedSet`; gated by `MockViewGate`. Two-phase grading (A objective 0-or-full at submit; B–E via the shared upload grader, byte-unchanged); Universal `<ResultsScorecard>` full-mock variant flipped live from `deferred:true` — section lens (exact) + CHAPTER lens (derived, `sectionBreakdown` null) + honest delta + §8b focus line; NO board-readiness. §8a persisted wall-clock + whole-paper resume; §8b focus aggregates. `FM-{S}-{NN}` records + perQuestion + mistakeLog. ONE responsive component (360px in the mockup grammar). **#391 FINALIZED the surface (2026-07-13):** hub "Full Test" card + Home tiles + MI link LIVE (plain nav, MockViewGate the ONLY gate; every old-engine entry retired) → **[FU-FM-HUB-ENTRY] CLOSED**; cross-device upload-later CLOSED via the text-only `fullMockPaperStore` snapshot (rules byte-untouched) → **[FU-FM-CROSS-DEVICE-UPLOAD] CLOSED**; executable RTL nav proof in-repo. **Verified = owner live-verify on the STABLE production link pending** (checklist §7, `report-ftfinalize-build-2026-07-13.md`) — the cell flips on that pass. [FU-CT-BALANCED-MIX] reuse follow-up remains (CT lane). |
| **Me / Progress** | Settling | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | **arc PR-4 SHIPPED (#408, `25c3cd7`) + PR-B-v2 ENGINE FIXES SHIPPED (#412, `1228c95`) — owner LIVE-VERIFIED on the stable link → Verified ⬜→✅. Launch-domino #3 closed: the arc shows REAL data across all four surfaces.** arc PR-4: `MobileMePage` retires the legacy Streak/XP hero; shared responsive `ProgressWindowArc` renders every rung honest-or-silent on both widths; careless carried; BottomNav preserved; trial badge dropped. **PR-B-v2 (#412) fixed the engine beneath it:** [FU-PROG-TOPIC-KEY-MISMATCH] CLOSED (resolveCanonicalSlug both sides — 5/26 topics could never key-match; legacy label attempts re-bucket), [FU-PROG-DATA-COMPLETENESS] CLOSED (unified graded stream: attempts ∪ record payload marks, deterministic ws:/ct:/fm: id dedup — CT/FM objective Section-A now counts; C&I dual write counts once by construction; pre-#403 record-only history healed), [FU-PROG-WINDOW-MODEL] CLOSED (activity-median split — wider ⊇ narrower; spanDays + isShortSpan → the honest amber short-term label on Me arc + Topic Hub), + the Topic Hub running-accuracy sparkline via cross-device `getTopicTrendFromCloud` (real scores from 2 points). Live-verify evidence: Polynomials hub 33.9%→46.9% + sparkline + honest label; Trigonometry honestly empty. **Scope stays Settling:** Home nudge PR-5 + the deferred presentation pass **[FU-PROGRESS-PRESENTATION-REDESIGN]** (fold per-topic trend into the topic HERO card; graphical Me with subject toggle + topic dropdown — owner-recorded, LATER PR, presentation-only, NOT a defect) + [FU-PROG-PRE403-QP-BACKFILL] (deferred). |
| **Tutor / Learn** | Settling | ✅ | ✅ | ✅ | ✅ | 🟡 | ✅ | **#516 (trunk `a86feda`, owner byte-reviewed the full deletion set) - THE OLD TUTOR IS NOW GONE FROM THE REPO, NOT JUST FROM THE PRODUCT.** PR-2 deleted the whole dead cluster (`ConceptTeachDrawer`, `TeachFlow`, `TutorDrawerV2`, `MentorPanel`, `TutorMessageRenderer`, `tutorStructuredExtract`, `pages/TopicHub.tsx`), the unreachable `MentorSolveDrawer` + `mentorDrawerLogic` and their PracticePage wiring, the **`/api/mentor` endpoint** with its 13 dedicated smoke/acceptance suites, and the persona/test-bot cluster - **62 files, +34/-15,239**. **NO cell moves: no student-visible surface changed** (that happened at #512); this is the dead-code half. `[FU-TUTOR-LEGACY-RETIRE]` **CLOSED end-to-end**. ★★ **CORRECTION to the post-#512 entry below, which claimed `/api/mentor` and `MentorSolveDrawer` were LIVE and must survive the deletion pass - they were NOT.** The drawer was imported, mounted and state-gated, but the prop that would set that state landed in an unused `_onOpenMentorBoard` binding three levels down, and its `?journeyMentor=` auto-open had no producer anywhere in the product. **An import proves a file compiles; a mount proves it renders; only a TRIGGER proves a student can reach it.** ★★ **Two briefed deletions were REVERSED after tracing:** `types/mentor.ts` is a shared TYPE module feeding `aiClient` (33 importers) and the live grading stack - **KEPT**, misleading filename notwithstanding; and `server/routes/mentor.cjs` re-exported `ensureDiagramFields` + `buildMoreLikeThisUserPrompt` into **live** consumers (`/api/more-like-this` is live via `practiceQuestionBuilder` and HPQ), so `index.cjs` now requires them from their real homes in `server/prompts/`. `MI` stays 🟡 (independent Stage-3 gap). **`topicHubMastery` UNTOUCHED** - owner still holding; `[FU-MASTERY-WRITE-ORPHAN]` is now definite but harmless. Prior: **#512 (trunk `e19b2d1`, owner LIVE-VERIFIED) — ★★ THE OLD TUTOR IS RETIRED FROM THE LIVE PRODUCT; `Redesigned` 🟡→✅.** The 🟡 below was held *explicitly* because `ConceptTeachDrawer` was still live on Topic Hub — an **unconditional** "Teach me" button on every concept row, beside the new "Stuck? Ask". #512 removed it, so the new `/tutor` is now the **sole** concept-level tutor entry and the surface a student meets is genuinely the redesigned one. **A product removal, not a cleanup.** `MI` stays 🟡 — an independent Stage-3 gap, untouched here. **The old-stack FILES are still on disk** (`ConceptTeachDrawer`, `TeachFlow`, `TutorDrawerV2`, `MentorPanel`, `TutorMessageRenderer`, `tutorStructuredExtract`, `pages/TopicHub.tsx`), now imported **only by each other** — zero live importers, tsc-clean, no ops script broken yet. **Deleting them + sweeping the ~27 coupled ops scripts is PR-2 and has NOT started**, so `[FU-TUTOR-LEGACY-RETIRE]` is **HALF resolved**. **KEPT because they are live-shared: `/api/mentor` + `types/mentor.ts` + `MentorSolveDrawer` (LIVE in `PracticePage`)** — the old/new boundary is the *endpoint* (`/api/mentor` shared, `/api/tutor` new), so only the `mode:"concept_teach"` client emitters died. `topicHubMastery` **UNTOUCHED** (owner holding); the old tutor's mastery *write* is now unreachable ⇒ `[FU-MASTERY-WRITE-ORPHAN]`, informational only — readers already tolerate empty. Prior: **#493 + #495 (trunk `273cfe8`, owner LIVE-VERIFIED) — NOW ALSO HOSTS QUICK PRACTICE AS AN IN-TUTOR OVERLAY (the C&I overlay's twin, completing the pair).** "Practise this" opens the real `PracticePage` over the dimmed tutor — scoped to the microconcept — instead of navigating away; the graded set is read back on close via the shipped `composePracticeRecordReturnOpener` storage round-trip (NOT rebuilt), and #495 named the return in both the scorecard and the close-bar. The practice navigate/poll leg is retired (`routeToPractice`/`routeOut` gone). Prior: **#476 (trunk `cca0a5d`, owner byte-verified) — NOW HOSTS CHECK & IMPROVE AS AN IN-TUTOR OVERLAY: "Get my attempt marked" opens the real C&I over the dimmed light tutor (right-slide desktop/tablet, full-screen mobile sheet); the graded record is handed back via the existing `composeReturnOpener` (poll-free, D-TUT-8 — the tutor never grades), and the check-improve navigate/poll/waiting-banner leg is RETIRED (the practice leg still routes out + keeps count-5/banner, next). `tutorRoundTrip.ts`/`composeReturnOpener` byte-identical. NEXT = the QP overlay (the twin). Prior: ★★ #457 + #459 (trunk `27e6ec2`) — THE NCERT PAGE ARC: dormant → live → WINNABLE; owner byte-reviewed ×2 + LIVE-VERIFIED ×3 (maths p.11 · science p.132 · electricity·Ohm's law inline).** The panel could cite an exact page and never show one: the affordance was **BUILT AND DORMANT** (gated on `ncertPage`; **zero of 54 rows carried one**). **#457** = data only — **54→73 rows**, **65** with a **PDF-verified** page (folio **AND** section heading required; **never** derived from a spec field — `page_pdf` is 0-based in some specs and 1-based in others, Light's is plain wrong, and the `source_ledger` misplaces electricity), coverage **13→22 topics**, **39 concepts deliberately REFUSED**, `data.ts` `306 added / 0 deleted`. **#459** = the ceiling removed: **in-play exam figure → real figure → NCERT page → interactive → gap** (**15/73 rows change**; a whole-chapter interactive no longer beats an exact page), **fail-closed** to the honest gap when Storage doesn't answer (`ncert` is the ONLY body kind whose asset isn't proven before render), plus `hasNcertPage` plumbed to the prompt builder's doorstep (**prompt text byte-identical — the model still doesn't know pages exist**). **★★ Its prerequisite was the whole ballgame:** `catalogueFiguresForTopic` hid every `best.kind==="none"` row ⇒ the promotion would have been **dead code for the one row it was built for**. **★★ PHOTOSYNTHESIS IS A VOCABULARY GAP, NOT A COVERAGE GAP** — no `boardEssentials` row exists; the CI label gate did its job **by refusing** ⇒ [FU-TUTOR-VOCAB-BRAIN-ROW] (same shape, human brain Fig 6.3). **No cell moves — §2a depth; `Verified` NOT re-claimed (#444 precedent), and Stage 3's own live-verify items remain pending.** New: [FU-CATALOGUE-SYNC-GUARD] · [FU-NCERT-OFFSETS-MATHS-CH9] · [FU-SCOPE-GUARD-HANDOFF-LANE]; **[FU-TUTOR-NCERT-PROACTIVE-MENTION] UNBLOCKED** (field CONFIRMED to survive `normalizeFigures`); **[FU-TOPICHUB-LENSPOWER-ANCHOR] CONFIRMED** (NCERT defines lens power in **Light ch.9 §9.3.8 p.157**, not the eye chapter). _Prior:_ **★ Stage 1 + Stage 2 + STAGE 3 all LIVE behind `/tutor` (premium-gated) — the fresh tutor's 3-stage build is COMPLETE.** **Stage 3 = the explanation panel (#448 `0e42e16`), D-TUT-13 closed:** split desktop / overlay mobile; **exact-match-or-nothing** resolution (D-TUT-15 — unknown concept → nothing, never a wrong figure); `[[figure:<conceptKey>]]` sentinel mirroring `[[offer:…]]`, server-validated against the topic's closed curated set; **interactive = an OFFER, never auto-embedded**; **5/7 hard gaps filled as NCERT-traced committed `.svg`** (3 traced / 4 original, each labelled); **D-TUT-16's cache deliberately NOT built** (mirrors a prod-dead cache ⇒ would regenerate an unreviewed diagram per student); optional `questionId` seam added to `buildTutorPath` (omitted ⇒ byte-identical). ~~**`Redesigned` 🟡 is deliberate — the OLD tutor is STILL LIVE on Topic Hub**~~ **— SUPERSEDED by #512: the old tutor is retired from the live product and `Redesigned` is now ✅.** (Historical: the drawer was reached from `ConceptSpine` **and** the dead `TopicHub.tsx`; `[FU-TUTOR-LEGACY-RETIRE]` was BLOCKED and its "dead files" premise verified WRONG — `mentor.cjs` has 3 live routes and only `TutorDrawerV2` was truly dead. ~~**That caution still governs PR-2: `/api/mentor` and `MentorSolveDrawer` are LIVE and must survive the deletion pass.**~~ **— THIS WAS FALSE, and #516 disproved it: both were dead (mounted-but-unreachable / no product caller) and are deleted. The caution itself was sound - verify before deleting - but its conclusion came from reading a MOUNT instead of tracing a TRIGGER.**) **`Verified` ✅ carries Stage 1+2's own pass and is PARTIALLY STALE: Stage 3's live-verify (atmospheric refraction + the reactions scheme) is PENDING.** Remaining: [FU-TUTOR-LAST-2-GAP-FIGURES] (unblocked) · [FU-TUTOR-INCHAT-QUESTION-UPLOAD] · [FU-TUTOR-READ-QP-RECORD] · `<EquationInput>` into the composer (deferred). _Stage 1+2 record:_ **Stage 1 + Stage 2 LIVE behind `/tutor` (premium-gated), owner live-verified incl. 360px.** Stage 1 = the fresh `/api/tutor` chat shell (#425 `2864be9` + #426 `d3c7be2`); **Stage 2 = the round-trip (#428 `7ef4bcf` + the six fixes #432 `65fdf85`), owner-live-verified.** FRESH engine (D-TUT-12): new `/api/tutor` (stateless → honesty-guard structural, tutor reads `practiceInsights`/`sessionRecords` but NEVER writes a grade) + new UI; ZERO old-engine imports; old `TeachFlow`/`ConceptTeachDrawer` KEPT as fallback until the new tutor is fully verified. Durable `tutorSessions/{uid}` session survives close/reopen (real continuity memory). Round-trip: **Practise → Quick Practice concept-filtered (returns via `practiceInsights`)** + **Get-marked → Check & Improve (returns via `sessionRecords`)**, both back into the SAME thread with a reframed opener; ONE intent-driven CTA via the `[[offer:…]]` sentinel (server-stripped); demonstrations pull a VERIFIED bank question. `resolveCanonicalSlug` only; nameless/board-voiced; two-panel scaffold (explanation panel a CLOSED Stage-3 placeholder). **NEXT = Stage 3 (explanation-panel visuals: `conceptVisualCatalogue` D-TUT-14 + matcher fix D-TUT-15 + AI-gen gap-fill D-TUT-16) — SEPARATE dispatch, not started.** [FU-CONTEXTUAL-TUTOR-REBUILD] resolving via the staged build; [FU-TUTOR-LEGACY-RETIRE] after Stage 3. Live FUs: [FU-MATHTEXT-COMMAND-CORRUPTION], [FU-QUICK-PRACTICE-DURABLE-SURFACE], [FU-RETURN-TICKET-CONTRACT], [FU-PRACTICE-COUNT-PASSTHROUGH], [FU-TUTOR-INCHAT-QUESTION-UPLOAD]. **#435 (`fd57db1`) fixed the MathText command corruption that was visible in tutor turns — cos²A/sin²A now render as real maths (owner live-verified). NO tracker cell moved: this was shared-renderer infra, not a tutor-surface change; the Tutor row was already ✅ where applicable. Stage 3 (explanation-panel visuals) remains the open Tutor work — Redesigned 🟡 / MI 🟡 are Stage-3 gaps, untouched by #435.** |
| **Home** | Settling | ✅ | ✅ | ✅ | ✅ | 🟡 | ✅ | **#520 + #522 (trunk `2865432`, owner byte-reviewed both, LIVE-VERIFIED #522) — REDESIGNED END-TO-END ON BOTH VARIANTS.** The duplicated Worksheets hero is retired (it and Practice carried the *identical* destination `/practice-hub?source=home&returnTo=%2F`); a Tutor card takes the slot and opens a SHARED firebase-free `TutorPickerModal` honouring the §3 login/return contract (signed-out → `/login` carrying the chapter in `?redirect`, **never** `/tutor`, so `RequirePremium` is never bypassed — asserted across all 26 topics × both subjects, mutation-verified). MI rebuilt on the verbatim `MistakeIntelligencePanel` buckets, **no tutor CTA** (no single honest destination). Mobile: shipped scroll-snap carousel + dots, MI near the top, collapsed quick links, and the **signed-out SAMPLE MI panel restored** in #522 after #520 over-read spec §4. Cards carry the practice-hub side-and-edge accent (#520 had declared the spine and never painted it); **geometry byte-identical**, owner-re-verified. Mobile Home now carries the account avatar (**new capability** — `/browse` never had one). **NEXT: PR-B — the `DesktopShell` rail Tutor entry, now unblocked** (#519 lifted the ban; the picker was built firebase-free precisely so the shell can mount it). **MI is 🟡 deliberately**: DESKTOP reads real `getMistakeLogs(uid,7)`, but MOBILE renders the honest empty state (signed-in) / labelled SAMPLE (signed-out) — real mobile MI is an open product call. FUs: `[FU-HOME-BOARD-COUNTDOWN]` · `[FU-HOME-MEMORY-STRIP-VS-MI]` · `[FU-MOBILE-MI-REAL-DATA]` (re-scoped — bundle argument void). |
| **Landing** | Settling | ✅ | ✅ | ✅ | ✅ | — | 🟡 | confirm both widths. *(Pricing was split out to its own row 2026-07-27 — see below. Landing's scope returns to `Locked`.)* **WAVE 5D (#608 + #609): the signed-out landing now reaches the privacy policy.** COPY-1's audit found legal reachable from six surfaces but NOT from `/welcome` mobile, `/pricing`, or `/` + `/welcome` desktop - `isPublicLandingRoute` suppresses the global navbar and none of those pages imported shared chrome. #608 fixed MobileWelcome; #609 fixed desktop `Welcome.tsx` under a narrow owner-authorised exception to the CLAUDE.md sec4 ban, scoped to the footer mount alone. **Mounting it as a sibling would have been a SILENT NO-OP** - at >=1180px the landing is `height:100vh`/`overflow:hidden` with `.lt-landing-stage display:contents`, so a footer outside the stage is clipped with no scrollbar: in the DOM, green on every presence test, invisible to every student. Proven live by a `footer[aria-label=Legal]` count of 1 plus an `elementFromPoint` hit-test at 390/1024/1280/1440, signed out. **Scope Locked -> Settling: `[FU-LEGAL-CONSOLIDATE-UNDER-ONE-ROOF]` - the owner judges this footer harmful to a designed page and intends to relocate legal under one roof. Verified stays amber pending that.** |
| **Pricing** | Settling | ✅ | ✅ | ✅ | ✅ | — | 🟡 | **NEW ROW 2026-07-27, owner-ruled.** `/pricing` publishes the company's prices to the public **and to Google via JSON-LD**, and it now carries its own guard test — it was previously tracked as a line inside `Landing` reading "confirm both widths", which is not a proportionate account of a surface that sets revenue. **#539 (trunk `ff5cb527`) — ₹599/month + ₹4,999/board year published from a single `src/config/pricing.ts`.** The saving is **derived** (`599×12−4999`), never stored. ★★ Scope is `Settling` because the price proved to be a **cross-surface invariant, not a page**: five literals across four files, two of them live and route-mounted (`PracticeLimitGate:90` → `/practice`, `MockViewGate:189` → `/mock-paper`·`/chapter-test`·`/full-mock`). Shipping the page alone would have shown a student ₹599 on `/pricing` and ₹149 at the moment of upgrade intent. Guarded by `pricing.guard.test.ts`, which walks **all of `src/`** for **two shapes** — `/₹\s*\d/` and bare numeric in `price:` position — because the most dangerous instance (`Home.tsx:242`, `price: "149"` in JSON-LD) carries no rupee sign at all. Cross-surface test asserts the rendered block and the JSON-LD agree **with each other** as well as with the constant. Still owed: **live-verify ₹599 renders on `/practice` and the mock gates in production**, and confirm both widths. Residuals: `[FU-HOME-FABRICATED-SOCIAL-PROOF]`, `[FU-JSONLD-OFFER-SHAPE]`, `[FU-HOME-TSX-DEAD-FILE]`. **WAVE 5D (#608): `/pricing` now carries the legal footer** (new `PublicLegalFooter`; the two pre-existing footers are signed-in avatar dropdowns and could not be reused on a signed-out page). Same `[FU-LEGAL-CONSOLIDATE-UNDER-ONE-ROOF]` relocation pending. Also `[FU-LEGAL-FOOTER-REST-CONTRAST]`: the component default measures **4.49:1 rest against a 4.5:1 AA threshold**, and hover/`:focus-visible` measured **1.42:1** - the link vanishes exactly when a keyboard user selects it. #609 overrode it per-host; **the fix belongs in the COMPONENT** (`#969ea9` = 6.59:1 rest / 17.84:1 hover), a Wave 5E lane that makes #609's override redundant. |
| **Login** | Locked | ✅ | ✅ | ✅ | ✅ | — | ✅ | **#538 (trunk `694c81b3`, owner LIVE-VERIFIED end to end) — FORGOT-PASSWORD RECOVERY, ENUMERATION-SAFE.** Before this, zero `sendPasswordReset` / `forgot.?password` hits existed anywhere in `src/` or `server/`: a student who forgot their password lost the account, the progress and the Mistake Intelligence with it. `AuthContext` gains `sendPasswordReset` (purely additive, zero `-` lines); `Login` gains an **inline** reset state in the email pane, not a route, so `App.tsx` stays byte-identical. ★★ The defence is an **allowlist**, not the denylist the spec asked for: only request-shaped failures may surface (`invalid-email`, `missing-email`, `too-many-requests`, `network-request-failed`), and `user-not-found` / `user-disabled` / `invalid-credential` / **any future code** fall through to one neutral notice. It fails SAFE by construction — a denylist protects only the cases someone thought of. Load-bearing test asserts **byte identity** on `textContent`, not `contains`. Phone-only students have no password, so the entry renders in the email pane only and tab-switching clears it. Residual on this surface: **`[FU-SIGNIN-DISABLED-ACCOUNT-ENUMERATION]`** — `describeAuthError:911-912` still gives `auth/user-disabled` its own message on the SIGN-IN path while correctly grouping `user-not-found` with `wrong-password` at `:913-916`; same leak class, fix pattern already present in the same file. · **#512 (trunk `e19b2d1`, owner LIVE-VERIFIED) — POST-AUTH LANDING RE-POINTED OFF THE RETIRED `/onboarding` TO `/`.** All **four** live inbounds cut: `Login.tsx:876`, `SignUpPage.tsx:52`, `DesktopHome.tsx:294` and `MobileHome.tsx:448` (the two start-trial CTAs). **★ Wider than "new signups":** the `hasProfile` gate it replaces was **permanently `false`** — it read the **bare** key `lazytopper.profile.v2` while `studentCloudStore.ts:23` only ever writes the uid-suffixed `lazytopper.profile.v2:<uid>` — so **every** login, returning students included, was already being routed to the off-brand dark onboarding screen. Gate deleted (not repointed); `[FU-LOGIN-HASPROFILE-DEAD-KEY]` resolved. Referral crediting also moved **into** the auth-success effects here and on `/sign-up`, keyed on the real Firebase `uid` — both pages, because `AuthContext` exposes no new-user signal and a Google-first signup can complete on either; this also fixed a latent **double-credit** (the old fresh-timestamp id could never match `addReferralToCode`'s dedup). `?redirect=` / `state.from` stay `isSafeInternalPath`-guarded. ⚠ **`[FU-SIGNUP-UNSAFE-REDIRECT]` OPEN** — `SignUpPage.tsx:52` still returns `st.from` **without** that guard; pre-existing, deliberately not folded into a retirement PR. ⚠ The `/onboarding` route + `pages/Onboarding.tsx` remain **inert on disk** (App.tsx frozen) ⇒ `[FU-APP-TSX-FROZEN-RESIDUE]`. _Prior:_ none (Firebase live). |
| **Notes** (content track) | Settling | ✅ | — | ✅ | ✅ | — | ✅ | **v1.3 MERGED (#356, `629457e`) — template COMPLETE, owner live-verified.** v1.3 made the mindmap read as a **VISIBLE TREE** by default (per-branch `--mm-accent` rail + connector elbows + root › branch › leaf weight; open-state PRESERVED — the brief's "collapsed" premise was wrong: all specs are depth-2, already fully expanded at `useState(depth <= 1)`) and the note modal **near-full-screen** (92vw×92vh capped 1280px; mobile full-screen); kept the v1.2 ≤380px no-overlap win. Owner live-verified the tree + full-screen modal + **360px on a real device** → **Mobile + Verified flipped ✅**. v1.2 (#345): collapsible mindmap + per-step marks (schema 1.2, validator Rule 10) + note-as-popup + clickable NCERT page refs, proven across physics/maths/biology (light/quadratic/life-processes). **NOTES FAN-OUT COMPLETE (2026-07-12) — all 26 canonical topics specced + independently audited** (batches #365/#368/#370/#371/#372; Chemical Reactions = LOCKED chemistry exemplar; syllabus trims held — Heredity evolution-trimmed, Magnetic Effects motor/generator-trimmed). Clickable NCERT page-cites are **LIVE + owner-verified** (#375 `ncertPdfOffsets.ts` + owner-hosted `ncert/{subject}/ch{N}.pdf` + public-read rule + CORS; Trig p.114 / Heredity p.129 land exactly). Source-ledger `p.N` cites made clickable too via [FU-LEDGER-CLICKABLE-CITES] (PR #376, in review). **Surface ✅ COMPLETE.** |

## 2a · DISCOVERED-SCOPE LOG (scope that emerged AFTER a surface was first planned)

### 2026-08-06 - Wave 5E (#611-#617) - the batch-grading arc built, and NOTHING a student can reach yet

**Surfaces moved:** **Quick Practice** -- **Scope Locked -> Settling.** Nothing a student sees has
changed **yet**: `#611` (the batch caller) and `#617` (the graded answer sheet) are both merged and
**DORMANT**. Also **the public root** -- robots, sitemap, llms.txt and favicon now serve at the root
(`#612`/`#613`, owner-authored); **Google had never fetched the sitemap.**

**Scope discovered after planning - and it is why Quick Practice is no longer Locked:**

1. **Quick Practice is becoming EXAM-SHAPED.** Owner-ruled: nothing grades per question; **one
   batched call at Finish**; then a scorecard across the set plus per-answer board-style depth.
   That is a change to the **learning loop**, not a feature addition, and it was settled only after a
   lane returned BLOCKED for encoding the product decision implicitly.
2. **The graded sheet is a ROUTE, not a modal.** ~1,877px of content inside a 540px card at 1024px --
   measured. **And a graded paper is something a student comes back to**: a modal cannot be linked,
   bookmarked or reached from history. ⇒ **`App.tsx` and `SurfaceHistory` are now in this surface's
   scope**, which they were not when it was marked Locked.
3. **Batching covers PHOTO working ONLY.** `WorksheetGradeQuestionInput` has no `textAnswer` field, so
   typed working returns `typed-no-channel`. **Do not record batching as complete.**
   `[FU-BATCH-TYPED-ANSWER-NO-CHANNEL]`
4. ** THE SURFACE IS BUILT AND UNREACHABLE UNTIL `WIRE-2`. ** `#578`, `#611` and `#617` are all
   merged and called by nothing. **`WIRE-2` is the single lane that ends all three.**
5. **`[FU-SCORECARD-DESKTOP-SCROLL-CEILING]` affects TRUNK**, not just this surface: `max-height`
   below 1024px against a `position:fixed` dim leaves a long variant's head and footer -- **return
   ticket included -- unreachable with nothing to scroll.**

### 2026-08-05 - Wave 5D (#606-#609) - a legal gap on the front door, two briefs whose headline premises were already shipped, and a stacked PR that deadlocked with its own base

**Surfaces moved:** **Landing** - the signed-out desktop front door (`/` signed out, `/welcome` at
>=1024px) now reaches the privacy policy (`#609`); **Landing (mobile)** and **Pricing** likewise
(`#608`). **HPQ / Predicted** - the evidence-base figure now reads one authoritative constant instead
of a literal (`#608`). **Quick Practice protection** - blanket ban replaced by persist contract tests
(`#606`). **The `/api` proxy** - its privileged-header strip now has executable coverage (`#607`).

**Scope discovered after planning - and Landing goes Locked -> Settling because of it:**

1. **Landing -> Settling. `[FU-LEGAL-CONSOLIDATE-UNDER-ONE-ROOF]`.** The audit's real finding was that
   legal was ALREADY reachable from six surfaces and missing from three, not the wholesale gap the
   brief described. Fixing it exposed a design question the owner then ruled on: he judges a public
   footer harmful to a designed landing page and intends to relocate legal under one roof, reached
   from the avatar and the door. **Merged as-is; he verifies live, then relocates.** The compliance
   point is the COLLECTION point - the sign-up door, which already links and is pinned by a test.
   The landing and pricing footers are belt-and-braces. Whatever the design becomes, **the policy
   must stay reachable from the door before the button is pressed.**
2. **`Welcome.tsx` required a narrow exception to the CLAUDE.md sec4 global ban.** COPY-1 stopped
   rather than touch it, which was correct; the owner authorised the footer mount alone. Scope
   discovered: the mount is not a one-liner. At >=1180px the landing is `height:100vh` /
   `overflow:hidden` with `.lt-landing-stage display:contents`, so a footer mounted outside the stage
   is clipped **with no scrollbar** - present in the DOM, green on every presence test, invisible to
   every student.
3. **`[FU-LEGAL-FOOTER-REST-CONTRAST]` - the new component ships below AA.** 4.49:1 rest against a
   4.5:1 threshold, and 1.42:1 on hover/`:focus-visible`, so the link vanishes exactly when a keyboard
   user selects it. `#609` overrode it per-host; **the fix belongs in the component** and is a 5E lane.
4. **`pages/app/Intent.tsx` is route-registered but unreachable** (`/pricing` 1 inbound nav,
   `/intent` 0). A dead-surface decision, not a copy fix. `[FU-EVIDENCE-BASE-INTENT-SURFACE-DEAD]`
5. **`BATCH-2` is still blocked.** `#606` lifted only the Quick Practice service ban;
   `ResultsScorecard.tsx` remains banned by BOTH C&I gates and needs two amendments in one PR plus a
   from-scratch component suite - it has no component test today.

### 2026-08-04 — Wave 5C (#601–#604) — a question bank that was live when everyone believed it inert, and three spec errors that belonged to the spec

**Surfaces moved:** **Check & Improve / SolutionChecker** — the paid CTA now shows its lock *before*
the tap, completing the explained → enforced → **visible** trio (`#602`). **Science question bank
(Heredity, Magnetic Effects)** — 40 defective Section A items removed from student-reachable packs
(`#603`). **CI itself** — a docs-only fast path, and SEC-1's two previously ungated server suites
wired in (`#604`). **`App.tsx` protection** — blanket ban → targeted tests (`#601`).

**Scope discovered after planning — all four lanes, and Scope is Settling on none of them:**
1. **★★ The two `pack1` files were ALREADY IMPORTED and live to students.** Both the brief and the
   owner's ruling assumed they were inert. They were consumed via `canonicalQuestionBank.ts` by 28
   modules. ⇒ **the remedy changed from "remove a landmine" to "stop live harm"**, and the owner's
   whole-file ruling was **withdrawn** once the lane proved 48 of 79 sound and the defect confined to
   Section A. **The cofounder's importer grep had a `-v` filter that excluded the path substring, so
   it could not have found them, and there was no control on the absence claim.**
2. **GATE-3's four named parents were not render sites at all** — they are `useSubscription`
   consumers. **There are two render sites and none of the four is one.**
3. **`scope:guard` cannot live in a CI fast path** — it reads the working tree, so a clean CI
   checkout measures `inspected=0` and passes forever.
4. **`lane_overlap.mjs` `GATED_FILES` survives the `App.tsx` lift**, so ME-PROGRESS still draws an
   owner review even though the zero-diff ban is gone.

★★ **The wave's subject: EVERY LANE DISPROVED PART OF ITS OWN SPEC — and all three spec errors were
the cofounder's, not the lanes'.** The corrective is not "brief harder"; it is that **a brief's
factual claims are hypotheses a lane must test with a control**, and *"IF THIS SPEC IS WRONG, YOUR
VERIFIED FINDING WINS"* is what made each correction reachable — including one that overturned the
owner's own ruling.

### 2026-08-03 — Wave 5A (#579–#582) — the paywall sells retired surfaces, a blocking gate depends on domain reputation, and MERGED turned out not to mean DEPLOYED

**No Scope cell moves.** Pricing and Check & Improve are **already `Settling`**; Login is `Locked` and nothing here is a new necessary piece for **its own** definition-of-done — the paywall copy belongs to **Pricing**. The rest is tooling, infrastructure and owner/DNS work. Logged because each changed the *shape* of the remaining work.

- **★★ The paywall's CONTENT is scope nobody had counted, and it is launch-blocking.** The "Choose a Plan" modal lists **Smart Study Planner, Daily Focus Mix, Full Analytics Dashboard** and Chapter Hub. **Study Plan, Daily Mix and Dashboard are RETIRED SURFACES** — the standing rule is that a reference to them is evidence of **deadness, not liveness**. ⇒ **the product is selling four things it no longer ships, to a student being asked ₹599.** *This is the anti-fabrication doctrine applied to commerce, and no gate in this project can see it.* The modal also offers **no "Keep using Basic" exit** — it tells a student the product is over rather than what they still have — and renders correct copy in an **error-red box**, because *a locked feature is not a mistake the student made.* ⇒ **the fix belongs inside GATE-2's spec, which replaces this modal; a separate lane would collide with it on the same file and land the wrong one second.** → `[FU-UPGRADE-MODAL-SELLS-RETIRED-FEATURES]` · `[FU-UPGRADE-MODAL-NO-BASIC-EXIT]` · `[FU-GATE-COPY-STILL-READS-AS-ERROR]`
- **★★ A BLOCKING gate can be defeated by something outside the repo entirely.** The email-verification gate shipped correct and works — and the mail **lands in Spam**, because Gmail has prior spam reports against `lazzyy-topper.firebaseapp.com`. **A new email/password student cannot enter until they click a link in a folder they will not open.** ⚠ **Google and phone bypass the gate, so this is invisible in owner testing while affecting every email student.** Fix = a Firebase **custom action-handler domain on `lazytopper.com`** + authenticated SMTP ⇒ **owner/DNS work, not an agent lane.** ★ *The spec predicted the spam risk and required a spam-folder prompt; the prompt shipped and the deliverability cause did not — the mitigation was right and insufficient.* → `[FU-AUTH-VERIFY-EMAIL-DELIVERABILITY]`
- **★★ The DEPLOY layer sits below every gate — and this time it bit, on two targets rather than one.** Wave 3 recorded that nothing in CI can observe what is deployed. Wave 5A shows the sharper version: **Vercel and Railway build independently from the same trunk and DIVERGED for ~2 hours**, so the server enforced a rule the client had no code to explain. ⇒ **a surface is not verified until BOTH deployments are confirmed by asking the running system.** ⚠ **Vercel "Redeploy" rebuilds the ORIGINAL commit, not the branch tip**, and branch protection makes a **Deploy Hook the only manual trigger.** → `[FU-DEPLOY-SPLIT-RAILWAY-VERCEL-DIVERGENCE]` · `[FU-DEPLOY-HOOK-IS-THE-ONLY-MANUAL-TRIGGER]`
- **★ Entitlement is a DERIVATION, and every surface that reads it inherits that.** The effective tier is `applyExpiry(repairInterruptedTrial(...))`, order load-bearing — not the stored `tier` field. Any future surface that gates on tier must not re-derive it differently: **both directions are wrong**, and an unelapsed `{tier:"free", plan:"trial_7day"}` is what the activation defect writes for every new signup. → `[FU-ENTITLEMENT-TIER-DERIVATION-DUPLICATED]`
- **★ Login's verification gate is page-scoped, and app-wide is a different surface's scope.** Making it app-wide needs `RequireAuth` / `App.tsx`, which are out of every current allowlist. **Recorded so the limit is not mistaken for coverage.** → `[FU-AUTH-VERIFY-NOT-APP-WIDE]`
- **★ Two dead things were confirmed LIVE rather than inferred, and both are their own lanes.** `/api/user/progress` **503s on every real session** (production HAR) → PG-1; and `<link rel="canonical" href="https://lazytopper.app/">` is **live in production on a domain never owned** → META-1, **suppressing search presence today.** → `[FU-PG-DEAD-ENDPOINT-LIVE-503]` · `[FU-META-CANONICAL-DEAD-DOMAIN]`


### 2026-07-29 — Wave 3 (#557–#563) — entitlement was never one surface, a guard could not see its own repo, and the deploy layer sits below every gate
**No Scope cell moves.** Check & Improve, Home and Pricing are **already `Settling`**; the other discoveries are tooling/infrastructure. Logged because each changed the *shape* of the remaining work, and because a wave that discovers scope and says nothing is indistinguishable from one that looked at nothing.

- **★★ Entitlement: the goal was not achievable inside one lane, and the routes were miscounted.** "Close the self-granted premium path" was specced as ONE Firestore rules change. It spans `firestore.rules` **and** `lazytopper/src/`, and there turned out to be **three** routes, not two — the third a **forged trial**, `tier:"trial"` being treated exactly as `"premium"` at 40 sites while `trialEndDate` was a client-supplied ISO string. **Merging the first PR and calling premium secured would have been false**, and was explicitly refused as a status. Closed only after **both** PRs merged **and** the deployed rules were read back from the Console.
- **★★ The deploy layer is outside every gate this project owns.** Nothing in CI can observe what is actually deployed — a merged rules file, a Railway env var, a Console setting can all diverge from trunk silently and indefinitely. Discovered the hard way: a deploy from a stale checkout re-shipped six-commit-stale rules and reported success. ⇒ **A lane whose outcome depends on a deploy closes on the DEPLOYED state.** Scope on *process*, not on a surface, and the wave's most transferable finding.
- **★ The grading-server surface's allowlist was incomplete — twice over.** **Two** gates banned the grader, not one, and amending only the allowlisted gate would have left the next PR red on the other **while looking like it had unblocked it**; and shipping a test without the `lazytopper/package.json` wiring would have been a silent no-op — *a test nobody runs guards nothing*. Scope grew by one gate and one manifest, owner-approved and declared in the PR body.
- **★ The ops-gates surface has guards nobody runs.** `agent3_uiux_guard` was **RED (3/7) and unread through three redesigns** because nothing invokes it; `repo_boundary_acceptance.mjs` audited **1 of 13 top-level trees** while keeping a stale private copy of the classifier; `ux_focus_acceptance.mjs` still reads a file that no longer exists. **Impact today: zero, because none of them is wired** — which is precisely the finding. → `[FU-GUARD-1-A]`, `[FU-GUARD-1-B]`, `[FU-GUARD-1-C]`, `[FU-UX-FOCUS-ACCEPTANCE-HOME-FIXTURE]`.
- **★ Home's returning-visitor story is scope, not a feature.** The product has **no returning-visit signal at all**; #557 added a per-feature counter for its own use. Any future "has returned" gating needs a **shared helper**, added once. → `[FU-NO-RETURNING-SESSION-SIGNAL]`. And `landingMemory.hasProfile` is **permanently false** — a uid-suffixed key read without its suffix, the second instance of that class in this codebase — which today gives a returning student the first-time-visitor layout on **both** Home pages. Fixing it is a behaviour change on two live surfaces and needs its own scoped PR with screenshots. → `[FU-LANDINGMEMORY-HASPROFILE-DEAD-KEY]`.

### 2026-07-26 — Pricing (#539) — the price was never one surface, and this board had no row for it
**RESOLVED 2026-07-27: `Pricing` is now its own row (owner ruling), and `Landing` returns to `Locked`.** The lane was scoped to `PricingPage.tsx`. It is now established that the published price is a **cross-surface invariant**, not a page, so the surface as previously scoped was wrong rather than incomplete — and the board's own shape was wrong too: a surface that sets revenue and feeds Google's price display was tracked as a sub-line of `Landing` reading "confirm both widths". Owner ruled **add the row, do not widen `Landing`** — it has its own guard test and its own live-verify debt, so it deserves its own line.

- **★★ The price lived in five places across four files, two of them live and route-mounted.** `PracticeLimitGate.tsx:90` (wraps `/practice`) and `MockViewGate.tsx:189` (wraps `/mock-paper`, `/chapter-test`, `/full-mock`) both published **₹149/month**, alongside `Home.tsx` at `:506` (visible block, plus retired ₹349/3-month and ₹999/year tiers), `:242` (JSON-LD `Offer`) and `:11` (meta description). Shipping the pricing page alone would have told a student **₹599 on `/pricing` and ₹149 at the exact moment of upgrade intent** — the worst possible place for the contradiction.
- **★★ The most dangerous instance carried no rupee sign.** `Home.tsx:242` was `price: "149"` inside JSON-LD. A guard written to the literal instruction ("no rupee price literal in `src/`") would have passed while missing the one value Google indexes and displays. The guard therefore checks **two independent shapes** and mutation-proves each separately; before demonstrating the second, the agent first confirmed the mutated line contained **zero** rupee signs, so the point is evidenced rather than asserted.
- **Cure is structural, not textual.** `src/config/pricing.ts` is the single source of truth, the ₹2,189 saving is **derived** (`599×12−4999`) rather than stored, and the guard walks **all of `src/`** (>200 files asserted) rather than a hand-listed set — a fixed list cannot catch the fifth surface, which is the entire purpose. A cross-surface test asserts the rendered block and the JSON-LD agree **with each other** as well as with the constant.
- **★ THIS BOARD HAS NO `Pricing` ROW.** The nearest surface is `Landing`, whose entire Next-action cell reads *"confirm both widths"*. A surface that publishes the company's prices to the public and to search engines has been tracked as a width check. Flagged for the owner rather than invented into the matrix. **RULED 2026-07-27: add the row, do not widen `Landing`** — `/pricing` publishes to Google via JSON-LD and carries its own guard test, so it gets its own line. `Pricing` is now a first-class surface at `Settling`; `Landing` returns to `Locked`.
- **Residual on this surface:** `[FU-HOME-FABRICATED-SOCIAL-PROOF]` (`Home.tsx` publishes `aggregateRating 4.8 / reviewCount 2340` as JSON-LD plus "12,800+ students" in prose — a §5 doctrine violation and, as structured data, exposed to Google's fabricated-review policy), `[FU-JSONLD-OFFER-SHAPE]`, `[FU-HOME-TSX-DEAD-FILE]`.

### 2026-07-22 — Home (#520 + #522) — a redesign shipped a duplicated slot's cure, then shipped two defects of its own that only live-verify caught
**Scope stays `Settling`** — PR-B (the DesktopShell rail Tutor entry) is the remaining known piece, and it is now unblocked. Logged because it corrects the board and changes how the next lane should verify.

- **★★ The duplicated hero was real and is gone.** The Worksheets card and the Practice card carried the *identical* destination string `/practice-hub?source=home&returnTo=%2F` — a literally duplicated slot on the product's front door. Retired; worksheets stay reachable at the hub + the quick-strip tile. **Mobile carried a second, independent duplicate** (`What scores most` and `What's likely in 2027` both → `/exam-trends`) because `MobileHome` hardcoded its titles inline. Both variants now render one shared inventory, so they cannot drift again.
- **★★ A CONVERSION SURFACE REGRESSED, GREEN, AND THE BOARD WOULD NOT HAVE SHOWN IT.** #520 deleted the signed-out SAMPLE Mistake-Intelligence panel from mobile `/browse` by over-reading spec §4's *"real data only"*. Every gate passed. It was caught only because the removal was flagged prominently in the PR description. **Restored in #522** with the sample label made *structural* (badge and figures in one containment-asserted block). **The doctrine: "real data only" forbids fabricated stats presented as a student's OWN — not a clearly-labelled sample.**
- **★★ A STYLING TOKEN WAS DEFINED AND NEVER CONSUMED — invisible to every gate.** #520 declared the card `::before` accent spine and never gave it a `background`, so `HOME_ACCENTS.spine` never painted. The cards shipped "styled with a no-op". tsc, the linter, the matrices and the tests all pass on that. **Only rendering the surface and measuring the computed style catches it.** Fixed in #522 with the practice-hub "side and edge" treatment; geometry byte-identical and owner-re-verified.
- **★ The mobile chrome record on this board was wrong in the brief, not in the code.** `/browse` is in `isMobileSelfChromedRoute` — it has **no `MobileShell` wrapper and had no account avatar**. The avatar added in #522 is a **NEW capability on this surface, not a restored one**; `MobileAccountMenu` was extracted out of `MobileShell` (character-identical body) so a self-chromed route can mount it without stacking two headers.
- **★ The "firebase-free" boundary recorded for this surface was FALSE.** `MobileHome` already pulled firebase — and `mistakeLogService` itself — via `useSubscription` and `AuthContext`, before #520. `[FU-MOBILE-MI-REAL-DATA]`'s bundle rationale is **void**; it is now purely a product decision. Corrected in-file and pinned by a characterisation test.

### 2026-07-21 — Tutor / Learn (#516, PR-2 of 2) — a recorded "LIVE" surface turned out to be dead, and two briefed deletions turned out to be load-bearing
**No Scope cell moves — Tutor was already Settling.** Logged because it corrects the board and changes how the next lane should verify.

- **★★ The board itself carried a false liveness claim.** `MentorSolveDrawer` was recorded as "LIVE in PracticePage" and used to justify keeping `/api/mentor`. It was **mounted but unreachable**. The error came from reading a mount, an import and a state gate — all normally reliable signals — instead of tracing the trigger, which landed in an unused `_`-prefixed binding three levels deep. **Any future retirement must trace triggers, and the board's own liveness claims are not evidence.**
- **★★ Two briefed deletions were load-bearing and were reversed.** `types/mentor.ts` is a shared type module feeding 33-importer `aiClient` and the live grading stack (misleading filename). `server/routes/mentor.cjs` re-exported two helpers into live surfaces including `/api/more-like-this`. **Check a module's exports and a file's importers before believing its name or its brief.**
- **★ Second-order cascades are real and invisible to import graphs.** Deleting 5 suites broke 8 scripts that *spawned* them; deleting 1 browser journey broke the runner that *imported* it, which would have taken 4 unrelated journeys down. **Grep for who invokes, not only who imports.**
- **★ The ops-sweep plan was too aggressive and reading corrected it.** "Retire ~8 wholly-dead scripts" → only 1 was mostly-dead; all 23 also asserted on surviving files. **Zero retired; all surgically fixed**, keeping surviving AND-conjuncts.
- **★ A third lane has now left residue behind the over-broad `App.tsx` freeze.** MockBuilder, onboarding, and now the tutor cluster's stale comments. The case for narrowing that guard is now three lanes strong.

### 2026-07-21 — Tutor / Learn + Topic Hub (#512, PR-1 of 2) — the retirement's fallout was an order of magnitude larger than audited
**Neither surface's Scope cell moves — both were already Settling.** Logged because the *shape of the remaining work* changed.

- **★★ The prior audit was wrong three times; re-verifying at trunk is what caught it.** (a) ops-script coupling: **2 claimed → 16 hard-`readText`** a to-be-deleted path (~27 files referencing) — **this alone forced the one-PR plan to become two**; (b) `ConceptSpine.test.tsx` asserts "Teach me" in **three** places, not two — the missed one (`:107-110`) would have landed the PR **red**; (c) deleting `pages/TopicHub.tsx` is **forced, not optional** (it imports `ConceptTeachDrawer`), so the ops fallout was never dodgeable. **An audit is a starting hypothesis, not a spec.**
- **★ Retiring "Teach me" was a PRODUCT removal.** The audit's own premise — that the mount was latent — was wrong: the button was unconditional on every concept row. Scope reclassified from janitorial to product, which is why it needed an owner ruling and a live-verify.
- **★ A second lane has now left residue behind the over-broad `App.tsx` freeze** (`[FU-APP-TSX-FROZEN-RESIDUE]`, joining `[FU-APP-TSX-DEADCASE-+-OVERLAY-FREEZE]` from MockBuilder #505). Two independent lanes blocked by one guard is the argument for narrowing it from "App.tsx zero-diff" to "the relevant route elements unchanged".
- **★ `[FU-OPS-SCRIPTS-PATH-COUPLING]` is a standing structural debt**: ~27 `scripts/ops/*.mjs` hard-read product files by path and none are CI-gated, so the coupling **rots invisibly** until a deletion exposes it. PR-2 clears the tutor-adjacent ones; the general hardening pass stays open.
- **Deferred to PR-2 (not started):** delete the 7-file old-tutor cluster; retire the wholly-dead ops scripts and surgically fix the mixed ones. **Explicitly rejected: swallowing ENOENT in `readText`** — vacuous green.
The record of how each surface revealed its true shape. Each entry: what was discovered · why it became necessary · where it went (PR / queued). This is not noise — it is the most reusable artifact here: it teaches us to predict the *next* surface's hidden scope (Chapter Test will discover its own equivalents).
- **Tutor / Learn** (the explanation panel's NCERT page — #457 `084442b` + #459 `27e6ec2`):
  - *The affordance was **built and dormant**, not missing* — `ExplanationPanel` already rendered the NCERT button whenever a row carried `ncertPage`, and **zero of 54 rows had one**. The "missing feature" was **data** → #457 (54→73 rows, 65 pages).
  - *★★ The catalogue is **two files and only one ships*** — `handoff/curation/conceptFigureCatalogue.curated.ts` is imported by **NOBODY** (the app tsconfig includes only `src`); the wired copy is `conceptVisualCatalogue.data.ts`. **A brief scoped to the handoff file alone would have shipped ZERO student-visible change** → escalated per §4; owner granted a narrow, **now-expired** grant. **And the two had ALREADY drifted** (#448's gap-fill rows + the 2026-07-16 label fixes lived only in `data.ts`) ⇒ re-sync **curated ← data** → **[FU-CATALOGUE-SYNC-GUARD]** (nothing enforces it; it rotted in one PR cycle).
  - *★★ The repo's own derived NCERT data is not a source* — `page_pdf` is **0-based in some specs and 1-based in others** (25/62 off by one; **Light's is plain wrong**), the `source_ledger` **misplaces electricity** (series 192→**182**), and the page field is named **three ways**. ⇒ every page verified against the official PDF (**folio AND section heading**) → the method the owner's ×2 clicks then validated on live content.
  - *★★ Coverage was capped by **vocabulary**, not by figures* — the live photosynthesis miss cannot be fixed by a catalogue row: **no such `boardEssentials` row exists**, and CI hard-fails a non-live label. **The gate did its job by refusing.** Same shape: the human brain (Fig 6.3, extracted, board-heavy, no row) → **[FU-TUTOR-VOCAB-BRAIN-ROW]**. *The missing thing is the CONCEPT ROW, not the figure.*
  - *★★ The page was permanently second-class, and its promotion was **dead code without a prerequisite*** — `resolveConceptVisual` stated the ceiling in its own comment; and `catalogueFiguresForTopic` hid every `best.kind==="none"` row, i.e. **the one row whose only visual was the page** → #459 (priority reorder, **15/73 rows**, + the filter fix). *A rationale can be TRUE WHEN WRITTEN and rot — the same species as #451's "third 5 MB constant".*
  - *A body kind whose asset isn't proven before render* — images are disk-gated by CI, but an NCERT page is fetched from Storage **at render time** ⇒ the panel must **fail closed** to the honest gap. *Queued elsewhere:* **[FU-NCERT-OFFSETS-MATHS-CH9]** (no `maths/ch9` offset ⇒ heights-&-distances can never offer a page) · **[FU-SCOPE-GUARD-HANDOFF-LANE]** (repo-root `handoff/` matches no lane; the `_handoff/` near-miss hides it).
- **Worksheet** (originally "a responsive generator"):
  - *Scorecard popup* — graded results needed a summary surface, not a wall of per-question text → PR-A (#295).
  - *Worksheet nomenclature `WS-{S}-{TOPIC}-{NN}`* — durable records and "which worksheet" need a stable identity → PR-A device-local, PR-B durable.
  - *Four-type breakdown on the scorecard* — the MI moat had to be reflected at the worksheet surface → PR-A.
  - *Branded graded PDF* — students wanted to keep/share the graded sheet → PR-A.
  - *Durable per-student record (uniqueness + journey + scorecard persistence)* — device-local couldn't guarantee fresh questions or a Me/Progress journey → PR-B (#321) MERGED, and now genuinely LIVE end-to-end after **#322** (`706cc12`). *Discovered while live-verifying PR-B:* the record was merged but non-functional — Firestore's `getFirestore` init threw on every `undefined`-bearing attempt doc and fire-and-forget `.catch` hid it (see D32). #322's `initializeFirestore(..., { ignoreUndefinedProperties: true })` fixed it; owner live-verified the durable `practiceInsights/{uid}/attempts` write on production. This does NOT flip Me/Progress to ✅ (the visual journey redesign is still pending) — it makes the persistence FOUNDATION the redesign reads from actually work.
  - *Parent/teacher view foundation* — once a durable record existed, seeding the parent/teacher storage became cheap and obvious → PR-B foundation; the VIEW is a later deliberate feature.
  - *Universal topicKey resolution* (discovered #357 live-verify) — the bank stores **51 distinct `topicKey` for ~26 chapters** (25 Title-Case + 26 slug), but the surfaces match RAW slugs, so **4 Science chapters (`chemical-reactions-equations`/`acids-bases-salts`/`metals-non-metals`/`reproduction` ≈ 1,180 Qs) return ZERO**, and `WorksheetGenerator.tsx`'s `q.topicKey === t.key` raw compare silently disables enrichment on Title-Case chapters. This is a **CROSS-SURFACE** defect — **Chapter Test and Full Mock carry it LATENTLY** — so the cure is resolve-everywhere (read AND write) + a CI guard (prior piecemeal fixes reappeared for lack of one), not a worksheet-local patch → **[FU-TOPICKEY-UNIVERSAL] (P0)**, Phase-2 [FU-BANK-TOPICKEY-NORMALISE]/[FU-MI-TOPICKEY-BACKFILL]. Its own scoped PR; do NOT attempt piecemeal. **UPDATE -- SHIPPED (#363, `6ecf15f`), owner live-verified.** C2 migrated all 34 orphan keys losslessly (assembled bank re-verified: **7084 served / 0 dup / 0 orphan / 26 canonical keys**); C3 wired a **dual-style Guard A** + an **authoritative import-based runtime proof** into CI so the disease cannot regenerate. The 4 zeroed Science chapters now return questions. Corrected the diagnosis numbers against the ASSEMBLED bank (true count **7084**, orphan volume **2,563**; the "51 distinct keys" was really 60 raw / 34 orphan). CROSS-SURFACE, so **no single surface completion cell flips** -- Worksheet/Chapter Test/Full Mock now all read correctly, but their redesign cells are unchanged. Discovered scope: the aggregator `canonicalQuestionBank.ts` itself holds 26 inline served questions -> [FU-AGGREGATOR-INLINE-QUESTIONS]; the mastery/schedule storage-key layer is a C1-preserved separate scheme -> [FU-TOPICHUB-MASTERY-STORAGE-KEY].
  - *Session-record DATA LAYER (Progress-Journey ARC · PR-1, #338 `d704b1c`)* — the durable per-COMPLETED-session record (`sessionRecords/{uid}/records/{code}`) + the ONE `progressStore` aggregation reader + durable cross-device `#NN` + `perQuestionRef` (review-my-answers) generalise the PR-B per-ATTEMPT record to the graded-SESSION altitude. This is the connectivity spine the Universal Scorecard (arc PR-2), per-surface histories (PR-3), **Me/Progress** (PR-4), and the Home ungraded nudge (PR-5) all READ — so those surfaces' redesigns now have their data foundation. **No completion cell flips yet** (data plumbing only; the reading surfaces are the later arc PRs). New top-level Firestore collection → owner-deployed rule `dc73360`.
  - *Universal `<ResultsScorecard>` (Progress-Journey ARC · PR-2, #341 `8c4c159`)* — the worksheet scorecard (PR-A discovered scope) was extracted into ONE shared results shell (`components/results/ResultsScorecard.tsx`) + a typed 4-surface variant interface (`scorecardVariants.ts`). Worksheet + Quick Practice variants are LIVE and behaviour-identical (worksheet proven byte-identical = the non-regression gate); Chapter Test + Full Mock are `deferred:true` config seams so those rebuilds (below) FILL config, not re-architect. Presentational — writes nothing (§1a). The old per-surface `WorksheetScorecard.tsx` was deleted (absorbed). No completion cell flips (both live surfaces already ✅); this is the shared chrome the per-surface histories (PR-3), Me/Progress (PR-4), and the CT/FM rebuilds will reuse.
  - *Per-surface HISTORY section (Progress-Journey ARC · PR-3, #344 `a4c3eec`)* — `components/results/SurfaceHistory.tsx` renders the durable session records as a "Your worksheets" list on the WorksheetGenerator BUILD view (rows: code/title/date/score-chip-or-awaiting-pill/four-type dot-strip + honest empty; a `getSubjectProgress` vs-last-time chip honest-or-silent; tap-row → READ-ONLY `<ResultsScorecard>` re-open, no `perQuestionRef` reconstruction). CONSUMES `progressStore` read-only; PR-2 files edited additively. This proves the arc READ-LAYER on the one LIVE graded surface — CT/FM history are deferred `SURFACE_COPY` seams pending those surfaces' rebuilds. No completion cell flips (Worksheet already ✅ Built/Redesigned; discovered-scope depth). C2 note: the vs-last-time chip is a subject-level MONTH trend, not a literal per-worksheet delta (possible fast-follow — OPEN_QUESTIONS).
  - *Progress-memory ENGINE (`getWindowedProgress`, PR-B, #403 `894ef6a`)* — the launch-blocker DATA layer: the ONE cross-device, multi-rung windowed aggregation (subject/topic/concept-bank-matched/section/mistake-type, honest-or-silent per rung) arc PR-4 + scorecards consume, over the durable streams honoring uid. Extends `progressStore` (the `void uid` device-local seam → real cloud read); new pure `progressBankIndex.ts`; additive read-only `getAllSessionPerQuestionFromCloud`. mistake-type = composition share over fully-graded only (adversarial review caught a pending-record rate fabrication); no rollup, `firestore.rules` untouched, sessionRecords WRITE shape byte-unchanged. A minimal desktop before→now arc (`ProgressWindowArc`) reads it. **No completion cell flips** — the visual redesign is arc PR-4, and **Mobile stays 🟡: owner live-verify found the arc renders ONLY in `DesktopMePage` (≥1024px); mobile still shows the legacy `pages/app/Me`** → [FU-MOBILE-ME-PROGRESS-PARITY] (arc PR-4, non-negotiable), plus [FU-PROGRESS-WINDOW-SPLIT-UX] + [FU-TOPICHUB-PROGRESS-ARC]. The engine is correct; these are consumption-surface gaps (OPEN_QUESTIONS).
  - *BUILD view redesign + history overlay + pending banner + unique PDF filename (#349 `b4f2162`)* — the WorksheetGenerator BUILD view was rebuilt to the locked "A · Smart default": a smart-default hero + a NEW **Preview step** (real section bar/counts/marks, computed enrichment callout, 3 real samples, honest bank-shortfall), all prior controls preserved behind **Customise**, MI as one honest toggle. **ONE `candidate` set feeds hero → preview → generate** (shown count == generated count). The PR-3 in-page `<SurfaceHistory>` mount was **moved off the page body** into a header-control OVERLAY (`WorksheetHistoryPanel`) that reuses SurfaceHistory's rows + read-only scorecard re-open unchanged (`embedded`/`pendingOnly` props only). New `WorksheetPendingBanner` surfaces ungraded worksheets (1 → deep-link Upload-now attaching to the EXISTING record via #338 idempotency; ≥2 → panel filtered to pending). PDF filenames now carry the unique `code` on all 3 paths. **No completion cell flips** (Worksheet already ✅ Built/Redesigned; discovered-scope depth). Closes 2 of the 3 owner-found worksheet bugs post-#344 (PDF filename + history placement; grader MCQ was #348). The generated "ready" view / `WorksheetGradePanel` / Practice hub / Topic Hub UNCHANGED. New FUs: [FU-MI-SCOPE-RELATIVE], [FU-MI-ENRICH-WITHIN-TOPIC], [FU-BUILDER-PREVIEW-AFFORDANCE].
  - *Scope-relative MI + within-topic section enrichment + Preview affordance (#353 `f8c1536`)* — the dispatched follow-up to #349, closing the three FUs above. MI is now **SCOPE-RELATIVE** (new pure `worksheetMiSelector.ts`: `scopeHotspot` = weakest in-scope topic vs `globalHotspot`), the single locked box **SPLIT into its true causes** (no data / weak-area-elsewhere + one-tap "Focus on / Add {topic}" / this-topic-IS-the-weak-area / signed-out — a student with MI data is never told to "grade a worksheet first"), and **within-topic SECTION enrichment is LIVE** for single-topic scope (section via the CBSE `totalMarks` band proxy — honest-unknown for non-band values, never a fabricated section; additive `orderPoolBySectionBoost` in `worksheetModel.ts`, capped at real availability, gated on the drawable pool so the toggle is never a no-op; cross-topic `MI_BOOST` byte-unchanged; NO schema change / no new writes). **FIX B** — desktop `position:sticky` Preview footer + drawer-foot Preview. 6 files, all `components/worksheet/`, zero forbidden/gated. **No completion cell flips** — Worksheet MI already ✅; scope-relative DEPTH (§2a), owner live-verified + squash-merged. CI quality-gate GREEN + Vercel PASS; 11-agent adversarial review → 5 low findings all fixed + SHIP. New FUs: [FU-WS-ENTRY-CONTEXT] (builder ignores the entry topic — `WorksheetGenerator()` takes no props, defaults to `topics[0]`), [FU-WS-MULTITOPIC-MI-AGGREGATE] (`weakestTopic()` names one topic → multi-topic should aggregate), [FU-WS-PREVIEW-BUTTONS] (drop the sticky, keep hero+drawer-foot), [FU-WS-MI-SWITCH], [FU-WS-MI-COPY], [FU-MOBILE-VERIFY-GAP] (mobile ≤360px never mockup-designed/live-verified — every future mockup ships a mobile frame + every live-verify includes a 360px check).
- **Topic Hub** (originally "an IA layout"):
  - *Exact mark-band filter* — concept-row Practise leaked wrong-mark questions through coarse buckets → PR-E1.
  - *Two-pool count divergence fix* — the "N available" hint and the displayed set drew from different samples → PR-E1.
  - *Concept rows FULLY SEEDED* — only 14 topics had real authored `boardEssentials`; the other 12 fell back to the generic `buildSampleActionable` "core ideas" rows → #337 authored real CBSE-2026-27 concepts for all 12, so **26/26 topics now render real board-essential concepts** (none on the fallback; `buildSampleActionable` retained as the net). Pedagogy sign-off (selection + mark bands) deferred to student-QC ([FU-TOPICHUB-PEDAGOGY-REVIEW]).
- **Notes** (content track):
  - *Visible-tree vs the responsive win (v1.3, #356 `629457e`)* — v1.2 made the mindmap responsive + collapsible, but it read FLAT. The brief blamed a collapsed default; ground-truth re-derivation showed the OPPOSITE — all three specs are **depth-2** trees already fully expanded at `useState(depth <= 1)` (no branch ever rendered a closed caret; leaves carry no caret). The real defect was **visual legibility** (~24 near-identical full-width cards at 16px indent don't read as a tree). Cure: PRESERVE the open-state, redraw as a real tree (per-branch `--mm-accent` rail + connector elbows + root › branch › leaf weight) while keeping the ≤380px no-overlap win. **Reusable lesson:** verify a brief's "collapsed/broken" claim against the actual rendered data before coding — here the fix was visual, not behavioural (the JS default is unchanged). Also surfaced [FU-PNPM-PACKAGEMANAGER-PIN] (a fresh worktree's `--frozen-lockfile` install fails `ERR_PNPM_LOCKFILE_CONFIG_MISMATCH`).
- **Chapter Test (#374 — BUILT):**
  - *Scorecard depth = section + four-type, not concept* — the locked scorecard lands a by-section (A–D) lens + the four-type MI, but NOT a subtopic-level ("concept") weak-area breakdown. `subtopic` IS on `CanonicalQuestion`, so a concept lens is derivable (FM-parity) → **[FU-CT-CONCEPT-LENS]**, a fast-follow before the `MockViewGate` flips. **UPDATE — SHIPPED (#380, `5bd148c`).** `deriveChapterTestConceptLens(response, questions)` derives the subtopic lens (join `qNumber → paper questionId → canonical subtopic`), rendered between the section lens and the four-type; derived-at-render / honest-unknown / `sectionBreakdown` stays null / ALL resolved concepts sorted by loss. Signature took a 2nd `questions` arg because the grade response carries `qNumber`, not `questionId`.
  - *Full-screen test still shows the DesktopShell global header* — the locked test-taking view is meant to be chrome-less/full-screen, but the global product header persists (restyling it is a forbidden-file `DesktopShell` change). Owner to decide: a global header restyle vs a chrome-less test surface → **[FU-CT-HEADER-UNIFORMITY]**, fast-follow before the gate flips. **UPDATE — SHIPPED (#380, `5bd148c`), route-scoped.** Recon reframe: the chrome on the test was the **NON-shell legacy navbar**, NOT `DesktopShell` — so `DesktopShell.tsx` is byte-unchanged, and `isBareFullScreenRoute` (App.tsx, owner-authorized bare-route exception) suppresses the legacy header + mobile BottomNav on `/chapter-test` at both widths. Owner chose the chrome-less test over a global restyle; the product-wide legacy-header retirement is the new open **[FU-RETIRE-OLD-GLOBAL-HEADER]**.
  - *Reopen-from-history downloads* — the read-only reopen is the PR-3 light re-open (score + four-type + derived lens + Done); graded/solution PDF on reopen needs paper reconstruction from `questionIds` → **[FU-CT-REOPEN-DOWNLOAD]** (deferred).
  - *Balanced PYQ+fresh sourcing exposed 34 unscorable Section-A MCQ keys (#397 `6db7f1d`)* — wiring the CT draw to `drawBalancedSet` was sourcing-only, but writing the test to assert the REAL grader contract (not seed luck) revealed a **CROSS-SURFACE bank defect**: 34 MCQs bank-wide whose `answer` resolves to NO option under the grader's `norm`, so any drawn into **CT or FT** Section A can never be scored correct → **[FU-BANK-UNRESOLVABLE-MCQ-KEYS]** (bank lane, before-launch scoring-correctness fix; full id-list in OPEN_QUESTIONS + the report). Distinct from the repaired [FU-BANK-CORRUPT-KEYS] 13. Lesson: assert the downstream contract a change feeds into (here the grader), not just the change's own shape — the test that "just verifies the wire" is where latent data defects surface. Also surfaced [FU-FM-BLUEPRINT-TEST-SEED-LUCK] (the FT test's identical strict assertion passes only because seed 11 misses the bad keys). No completion cell flips (sourcing DEPTH on the already-built CT surface; [FU-CT-BALANCED-MIX] CLOSED).
  - *Mis-banded 5-mark Section-D rows drained (#504 `b810055`, owner LIVE-VERIFIED)* — a sibling bank-content defect surfacing on **CT/FM Section D**: **44 rows sat at `section:"D"/marks:5` while being objective or short-content questions** (the owner's live `TG3-056` "find cosec 60°" grading as a 5-marker; MCQs surfacing as absurd 5-mark Section-D items). Data-only relabel to true CBSE value — **37 MCQ/AR → A/1** (format unchanged ⇒ stay objective, binary 0-or-1; `test:objective:dedup` PASSED), **7 short VSA → B/2** (each PYQ-grounded at fallback level 1, `solutionSteps` re-authored to the 2-mark scheme). The count was **44, not the spec's ~76** — short-*prompt* genuine-`Long` proofs ("Prove √2 irrational") are correctly banded and excluded (the Class-(a)/(b) boundary). Resolves **[FU-BANK-SCARCE-BAND-MISBANDING] Class (a)** + the co-tracked **[FU-AITIER-MARKS-MISMATCH]** (its shrink-only `PACK_5MK_SHORT_BACKLOG` emptied — the one companion edit outside `data/`, owner-approved). **CLASS (b)** (~178 under-stepped genuine-5-mark rows) STILL OPEN — a different defect, do not conflate. **No completion cell flips** (bank-content correctness on the already-built CT/FM banding). Lesson: measure the rendered kind before relabelling — a concise prompt is not a short question.
- **Full Mock (#391 — FT finalize):**
  - *A third old-engine dead link* — the plan named two Home-side retargets (the per-subject tiles); the build found a THIRD `/mock-builder` dead link (the mistake-strip "Add weak-area to next mock"). Same ratified treatment applied (re-point + honest "Open Full Test" relabel — no weak-area claim), flagged-not-silent, owner approved. Lesson: retiring a surface means grepping for EVERY route-literal referrer, not just the entries a plan names — which also surfaced [FU-RETIRE-EXAM-SIMULATION-LINKS] (6 legacy pages still on the OTHER old engine).
  - *Pre-existing vitest rot* — running the FULL suite for the nav proof exposed 6 failures in 3 untouched files that reproduce on trunk (invisible to CI, which runs the matrices not vitest) → [FU-VITEST-PREEXISTING-FAILURES].
- **Check & Improve (#472 — question-side parity):**
  - *A frozen five-site map was one server site short* — the new `"question"` `QrHandoffMode` also had to be allow-listed server-side in `qrUploadChannel.cjs` (`VARIANTS`), which coerced any unknown mint to `'document'`. The five CLIENT sites alone would have shown a QUESTION student "your answers" on the phone. Found by tracing the round-trip (mint → `peekSlot`), not by the spec; proven with a channel-test `mint→peek` assertion. **A frozen enumeration is a strong prior, not a completeness proof.** C&I Scope stays **Settling** (no new open design question — the discovery was a completed catch, not deferred work).
- **Check & Improve (#466–#470 — the convergence arc):**
  - *13 `matchMedia("(max-width:960px)")` layout sites* in the "desktop" twin drove an `isNarrow` flag through BOTH grids — the twin measured the WINDOW, so an 820px overlay was already broken. The convergence was structurally larger than "delete a twin"; the rule was rewritten from "never `useIsDesktop`" [a hook] to "no window-derived value drives layout" [a behaviour]. → #466.
  - *The CI acceptance gate had never run its MI/FORBIDDEN checks* — `checkout@v5` at default depth 1 hid the base ref, so the script silently skipped on every run [incl. #466’s "fully green"]. A whole class of green-proves-nothing; fixed with `fetch-depth: 0`, a fixed-SHA MI anchor, and a hard-fail-on-missing-ref in CI. → #469.
- **Check & Improve (#395 — C&I PR-1, the SessionSurface plumbing):**
  - *The #348-flagged open design question, DECIDED + BUILT* — [FU-CI-SCORECARD-VARIANT] ("C&I writes no session record / isn't a `SessionSurface`") was resolved by the locked C&I spec 2026-07-10 + the 2026-07-12 dispatch: full session surface, `topicKeys[]` always populated WITH provenance (`topicSource`, mirroring `marksSource`). The "record with optional topic" alternative was rejected — an inferred topic is signal, not absence.
  - *Provenance over re-detection* — the correction UX already existed; PR-1 tagged its OUTPUT (`topicTouched` flag → confirmed/inferred; empty slug → mixed) instead of building a picker. `bank-matched` sits RESERVED in the union until a real matcher exists ([FU-CI-TOPICSOURCE-BANK-MATCHED-RESERVED]).
  - *The honest MIX ceiling* — until per-question topic (arc PR-2), a mixed paper's topic count is UNKNOWABLE: the history chip is a plain "Mixed topics" (the mock's "3 topics" literal would have fabricated a number — the dispatch's own honesty rule overrode its mock), and mixed sessions write `topicKeys: []`, feeding marks + four-type only, said out loud on the scorecard.
  - *Grade-time minting* — the lazy export-only code mint was WHY single-question sessions vanished; owner decision: mint at grade time, every graded session gets a record, `couldNotRead`-only gets none.
  - *One forced type ripple* — expanding the `SessionSurface` union forced a +4-line copy-seam entry in `SurfaceHistory.tsx` (`Record<SessionSurface,…>` exhaustiveness); flagged, owner-ratified. Lesson: a union expansion's ripples are found by tsc, not by the plan — budget for exhaustive-Record consumers.
- **Me-Progress:** *(none yet — Settling; expect discoveries during its redesign; log them here as they surface.)*

## 2b · OPEN DISCOVERED ITEMS NOT YET PLACED
(Discovered-necessary work that hasn't been assigned to a PR yet — the holding pen so nothing falls through.)
- *(none open right now — PR-B holds the current Worksheet discoveries.)*
- **[FU-AGGREGATOR-INLINE-QUESTIONS]** (from #363) — relocate the 26 inline served questions in `canonicalQuestionBank.ts` to a `questionBanks/**` pack so decision 3A ("migrate questionBanks-only") holds literally. Tracked in OPEN_QUESTIONS_AND_FOLLOWUPS.
- **[FU-TOPICHUB-MASTERY-STORAGE-KEY]** (from #363) — reconcile the mastery/schedule storage-key scheme (`normalizeTopicKeyForStorage` in `topicHubMastery.ts` + the dormant SR engine) with the canonical vocabulary; migration-sensitive (C1 preserved the key to avoid orphaning stored mastery). Tracked in OPEN_QUESTIONS_AND_FOLLOWUPS.
- **[FU-BANK-UNRESOLVABLE-MCQ-KEYS]** (from #397) — 34 MCQs bank-wide whose `answer` string-resolves to NO option under the grader's `norm`; unscorable if drawn into CT/FT Section A. **Before-launch scoring-correctness fix, bank lane.** Full 34-id list + failure classes in OPEN_QUESTIONS_AND_FOLLOWUPS + `report-ct-balanced-mix-2026-07-13.md`. 6 are Batch-5 REP2-* AR items → the bank-expansion authoring template must enforce `q.answer` = exact option text. Pairs with [FU-FM-BLUEPRINT-TEST-SEED-LUCK] (relax the FT test's strict key assertion, or fix the keys first).

## 3 · THE UPDATE DISCIPLINE (the culture that keeps this honest)
The tracker stays fresh because it rides the ritual that ALREADY happens, not a new separate process.

**Trigger A — every PR (the agent).** When a PR moves a surface's status, the agent flips the cell(s) here **in the same docs-handoff PR** it already files alongside CURRENT_STATE/ROADMAP. One line added to `handoff/templates/session-update-template.md` and to CLAUDE.md's handoff ritual: *"Update SURFACE_TRACKER.md cells for any surface this PR moved; if none moved, state so."* No flip is silent.

**Trigger B — every session (the cofounder / Claude).** At session START, reconcile the matrix against the repo (same verify-before-asserting rule — confirm a cell marked ✅ is actually true in the code) and flag any drift. At session END, ensure the matrix reflects what moved. The cofounder owns the *accuracy* of this file; the agent owns the *per-PR flip*.

**Trigger C — every live-verify (the owner).** When Chetan live-verifies a surface, the **Verified** cell flips ✅ (he says so; the agent or cofounder records it). A surface is never "done" on static work alone — ⚠️ until the owner verifies.

**Trigger D — the moment scope is DISCOVERED (anyone, mid-work).** When, mid-PR or mid-design, a new piece is decided necessary that wasn't in the plan, treat it as a first-class event, not a silent expansion:
1. **Log the decision** in the repo's `handoff/DECISION_LOG.md` with the rationale ("decided X necessary because …").
2. **Add it to §2a** under that surface (what · why · where it went), and keep the surface's **Scope = `Settling`** until the dust settles.
3. **Decide placement:** small + tightly coupled → fold into the open PR and note it in the report; non-trivial or separable → **spawn a NEW queued PR** (PR-A → PR-B was exactly this) and park it in §2b until assigned. **Default to spawning, not swelling** — a PR that grows mid-flight is the one most likely to slip past review. A surface only flips to `Locked` when no obvious discovered work remains.

**Cadence & milestones.** The matrix is the standing "are we there yet" board. A surface going fully green across all its applicable columns is a milestone — a one-line SESSION_LOG entry. The soft-launch gate is met when every CORE surface row (Check&Improve, Quick Practice, Topic Hub, Worksheet, Chapter Test, Full Mock, Me/Progress, Tutor, Home, Landing, Login) is ✅/✅ across Built · Redesigned · Desktop · Mobile · MI · Verified.

**Anti-rot guarantee.** Because the flip lives inside the existing post-PR handoff (the thing the agent does every single PR), it cannot drift far. The classic failure — a standalone tracker nobody updates — is avoided by *not* making it standalone.

## 4 · READING THE BOARD RIGHT NOW (so the path is unambiguous)
- **Mature / done (5):** Check & Improve, Quick Practice, Topic Hub, Login — plus Worksheet pending only the owner live-verify.
- **The surface-completion critical path (your stated priority):** **Chapter Test redesign → Full Mock rebuild → Me/Progress redesign.** These three are the bulk of "surfaces complete + responsive both + MI through." Chapter Test is next (design first); it establishes the scorecard-and-sheet + upload-grade pattern that Full Mock and Me/Progress reuse.
- **The cross-cutting mobile-drift risk:** several surfaces are 🟡 on Mobile because they still route to an OLD design (the split-component legacy). PR-G's deletion of dead old-mobile surfaces + per-surface convergence to one responsive component is what turns those ✅. Track it per surface here, not as a vague "make it responsive" task.
  - **Mobile account chrome is now app-wide via `MobileShell` (#410, `f662fbe`).** The account avatar-dropdown (desktop-parity: read-only subscription, same manage-subscription URL + logout) lives once in the shared `MobileShell`, so every mobile page that uses it inherits it; the old global mobile brand bar's one-header treatment (`isMobileSelfChromedRoute`) now covers `/browse`, `/welcome`, `/me`, `/check-improve`, `/intent`, `/practice/worksheets/ready`. **No completion cells flipped** — this is cross-cutting chrome, and the surfaces it touched were already ✅. **Discovered gap:** only 4 MobileShell surfaces are actually routed (the "12+ pages" was stale), and **`/exam-trends` + `/practice` still render the OLD global brand bar** (they don't use MobileShell) → **[FU-MOBILE-OLD-HEADER-TRENDS-PRACTICE]** (C&I PR-2 item E). `DesktopShell.tsx` byte-unchanged; shared status derivation now in `utils/accountStatus.ts` (desktop migration = [FU-DESKTOP-ACCOUNT-MENU-SHARE]).
- **MI through-and-through:** ✅ on Check&Improve, Quick Practice, Worksheet. **Gaps:** Chapter Test (MCQ-only today — needs the E2b upload loop for subjective + mistake-type MI) and Full Mock (⬜). Closing those two is "MI through-and-through."
- **Data (parallel, later):** extraction (next folder = Foundation 5-mark LA/proofs) and Notes authoring enrich the banks gradually; they do NOT block the architecture gate.

> **2026-08-04 · WAVE 5B (#595-#598, trunk `1adce673`).** Surfaces moved: **paywall/upgrade sheet** (GATE-2 - the sheet replaces the red box; the *visible* locked-CTA layer did NOT ship, `GATE-3` in Wave 5C) and **SEO/metadata** (META-1 - canonical, title, description, sitemap, `llms.txt`). Server-only lanes PG-1 and SEC-1 moved no student-facing surface. **Scope discovered:** `[FU-AUTH-SIGNUP-ROUTE-UNREACHABLE]` - `/sign-up` is linked from nowhere, so every new account is created with no `displayName`; that surface is **Settling**.
