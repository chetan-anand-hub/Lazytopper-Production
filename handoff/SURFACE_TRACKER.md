# LazyTopper — SURFACE TRACKER (the road to soft launch)
**Home:** this file lives at `handoff/SURFACE_TRACKER.md` in the repo (version-controlled, beside CURRENT_STATE). **Trunk at last sync:** `cc84ae5` (#420 — **C&I PR-3: the model-solution CACHE — the Check & Improve surface is COMPLETE.** Scheme-first grading: keyless SUBJECTIVE questions grade against a student-agnostic solution from the EXISTING `step_solutions` Postgres cache (hash → read → generate-from-question-ONLY on miss → Gate-2a quality gate at EVERY write path → write-if-pass) fed into the grader's EXISTING scheme slot; sacred `checkSolution.cjs` diff = +95/−4 deps-injected hooks only (owner byte-review "textbook-clean"); Gate 2b admin evict/regenerate behind fail-closed `ADMIN_FIREBASE_UIDS` ([FU-ADMIN-UIDS-DEPLOY-ENV] = set the env); CACHE_VERSION now prefixes ALL hashes. **[FU-CI-SOLUTION-CACHE] CLOSED; [FU-MODEL-ANSWER-QUALITY] structurally addressed.** No COMPLETION cell moves — server-side DEPTH on the already-✅ C&I surface (§2a); the C&I Verified pass now also covers cache-hit speed / garbled-not-persisted / eviction). _Prior sync `daf403a`:_ (#399 docs on #397 `6db7f1d` — **CT BALANCED PYQ+FRESH MIX** [FU-CT-BALANCED-MIX] CLOSED. Sourcing-only: the Chapter Test now draws each section (A–D) through the SHARED `drawBalancedSet` (`utils/balancedMockDraw.ts`, reused verbatim — the FT helper, byte-unchanged, not forked), mirroring FT's pass-1 per-cell pattern, so a CT paper deliberately mixes real PYQs with fresh authored/extracted questions (~50% PYQ target; honest fallback for thin/zero-PYQ topics). Byte-identical: paper shape/marks/grading/scorecard/numbering + the `MIN_TEST_QUESTIONS` honest gate (confirmed intact after the draw); seed minted in-blueprint (FT's recipe, optional `seed?` test seam) so `ChapterTestPage.tsx` stays byte-unchanged. Exactly 2 files; no forbidden files. New `chapterTestBlueprint.test.ts` proves the seeded wire + zero/thin-PYQ fallback (vitest 22/22). No surface COMPLETION cell moves — DEPTH on the already-built CT surface (§2a). **Surfaced a cross-surface bank defect [FU-BANK-UNRESOLVABLE-MCQ-KEYS]** (34 MCQs whose key matches no option → never scorable in CT/FT Section A; §2a) + [FU-FM-BLUEPRINT-TEST-SEED-LUCK]. Owner live-verify on the balanced mix pending. _Prior sync `e33b9d3`:_ (#395 — **C&I PR-1: Check & Improve is a first-class SessionSurface.** Every graded C&I session now writes a durable `surface:"check-improve"` sessionRecord (idempotent on the durable cross-device `CI-{S}-{TOK}-{NN}` code — the device-local `lt:ci-multi-seq` RETIRED, no shadow path; `couldNotRead`-only sessions write NOTHING; `questionIds: []` — an external upload's concept is unknowable, never fabricated) + `topicSource` provenance tagged onto the EXISTING correction flow (`confirmed`/`inferred`/`mixed`; **`bank-matched` RESERVED-not-emitted** until a real bank-match path exists — [FU-CI-TOPICSOURCE-BANK-MATCHED-RESERVED]; never backfilled) + the 5th `<ResultsScorecard>` variant pair (live + stored; **shell zero-line-diff**; four-type lens only — by-topic is per-question-topic-gated; NO board-readiness/solution key) + the "Your checked papers" overlay panel (locked CT ScoreRing/DotStrip; MIX = plain "Mixed topics" chip, never a guessed topic or fabricated count). Detection/correction/override-log/MI byte-intact; grader/worksheetGradeService/App.tsx/DesktopShell/firestore.rules byte-clean; one forced +4 copy-seam ripple in `SurfaceHistory.tsx` (Record union expansion). Closes [FU-CI-SCORECARD-VARIANT] + [FU-CI-DEVICE-LOCAL-SEQUENCE]; new [FU-CI-TOPICSOURCE-BANK-MATCHED-RESERVED] + [FU-CI-PERQUESTION-TOPIC] (arc PR-2) → then [FU-CI-SOLUTION-CACHE] (arc PR-3/4, owner 3-gate sign-off). **PR-B is now unblocked to aggregate check-improve** — the critical-path successor). _Prior sync `25257c0`:_ (#391 — **FT FINALIZE: Full Test LINKED + cross-device upload-later.** Hub "Full Test" card (locked copy, cta "Open Full Test") + DesktopHome per-subject tiles + MI-panel link all navigate PLAINLY to `/full-mock/:grade/:subject` — MockViewGate the ONLY gate; old-engine entries RETIRED (the /exam-simulation card, the circular un-routed /mock-builder "Practice Paper" card, + a third discovered dead link on Home's mistake-strip — all "weak-area" wording dropped, the board paper has no weak-area targeting). Cross-device upload-later CLOSED: `services/fullMockPaperStore.ts` text-only paper snapshot (paper + frozen objective + §8b focus — never the answer image/typed answers) at `sessionRecords/{uid}/fullMockPapers/{code}` under the EXISTING recursive rule → firestore.rules byte-untouched; fetch + re-seed on pending re-open (also cures localStorage eviction); verbatim honest fallback for true misses. Plus [FU-TOPIC-DISPLAY-TITLECASE] (connectives lowercase in the display fallback) + [FU-SCORECARD-STALE-HEADER-COMMENTS] (comment-only). Executable RTL nav proof `DesktopPracticePage.fullTestNav.test.tsx`. Resolves all four FT FUs; new [FU-RETIRE-EXAM-SIMULATION-LINKS] + [FU-VITEST-PREEXISTING-FAILURES]. Owner live-verify on the stable link pending → the FM Verified cell flips on that pass). _Prior sync `99b1d2a`:_ (#388 — bank-expansion Batch 4 our-environment +80, data-enrichment parallel track: assembled bank → **7,342**, our-environment 169→249; both scarce bands honest-stop; owner byte-review added the **two-direction syllabus boundary** (deleted-chapter + Class-11/12 adjacency) as standard, and the scarce floor is now **≥75 distinct**. NEXT: reproduce/heredity. ~22 topics remain). _Prior sync `f6522d0`:_ (#387 — Full Mock) (#387 — **FULL TEST (Full Mock) BUILT + LIVE**: per-subject 3-hour board mock, 38Q/80mk A–E, DUAL-SOURCE union (canonicalQuestionBank + the live predicted banks) through the new shared `drawBalancedSet` (utils/, seeded, honest PYQ/fresh re-weighting), persisted wall-clock timer + whole-paper resume (§8a), §8b focus aggregates (additive optional `focus?`), scorecard = section lens (exact) + CHAPTER lens (derived; `sectionBreakdown` null), FM-{S}-{NN} records + MI via the SHARED grader. Gated by `MockViewGate`; route LIVE but UNLINKED [FU-FM-HUB-ENTRY]; cross-device upload-later honestly limited [FU-FM-CROSS-DEVICE-UPLOAD]; CT can now reuse the helper [FU-CT-BALANCED-MIX]. Resolves [FU-FM-RESOURCE-PREDICTED]). _Prior sync `ce34b3e`:_ (#384 + #385 — **bank-expansion lane IN PROGRESS**, data-enrichment parallel track: Batch 2 real-numbers corrective +12 (exhaustive re-sweep + D/E exhaustion audit proving the ~24-distinct-method ceiling) and Batch 3 life-processes +136 (first SCIENCE batch — extract-max 75 A/B/C, scarce D→53/E→54 clearing the ≥50 distinct floor). Assembled bank → **7,262**. Exhaustive per-source-sweep discipline confirmed working (75 vs 23 = reservoir depth). New FIGURE-PENDING safeguard doctrine. Manifest `docs/bank-expansion-review-queue.md`; surfaces GATED until student QA. NEXT: another Science topic). _Prior sync `3866a94`:_ (#381 — **bank-expansion lane IN PROGRESS** (data-enrichment parallel track, NOT an architecture-gate surface): Batch 1 real-numbers +30 lands the assembled bank at **7,114** under the owner-set **band-scarcity floor** (A/B/C extract-max; scarce E/D/proofs ≥50 genuinely-distinct + honest-stop). Manifest `docs/bank-expansion-review-queue.md`; surfaces that serve these stay GATED until trusted-student QA. Correction for future batches: A/B/C extract-max must EXHAUSTIVELY sweep all sources with a per-source table [FU-BANK-EXPANSION-SOURCE-SWEEP]). _Prior sync `5bd148c`:_ (#380 — **CT concept-lens + bare full-screen**: the CT full scorecard now derives a by-CONCEPT (subtopic) lens between the A–D section lens and the four-type (derive-don't-persist, `sectionBreakdown` null, honest-unknown, ALL resolved concepts sorted by loss), and `isBareFullScreenRoute` makes CT chrome-less full-screen (legacy header + mobile BottomNav suppressed on `/chapter-test`, both widths; `DesktopShell` byte-unchanged — it was the NON-shell navbar); **[FU-CT-CONCEPT-LENS] + [FU-CT-HEADER-UNIFORMITY] CLOSED** — CT is ready to flip live at `MockViewGate` at the owner's discretion. New open [FU-RETIRE-OLD-GLOBAL-HEADER]). _Prior sync `e54ab8c`:_ #374 — **Chapter Test BUILT to the locked spec**: two-phase grading, native canonical sourcing, Universal `<ResultsScorecard>` chapter-test variant live, `CT-{S}-{TOPIC}-{NN}`, behind `MockViewGate`; scorecard is section+four-type today — CONCEPT lens + header-uniformity are the pending fast-follow before the gate flips). _Prior sync `8fb1ad6`:_ post-**notes-fan-out-COMPLETE** (all 26 chapters specced + independently audited; #365/#368/#370/#371/#372) + **NCERT page-cites LIVE** (#375 offset map + owner-hosted PDFs + public-read rule + CORS) + **coordination automation LIVE** (#366: lane-overlap REQUIRED check + state-board ledger + CODEOWNERS + trunk ruleset). [FU-LEDGER-CLICKABLE-CITES] PR #376 (source-ledger `p.N` clickable) in review. _Prior sync `aa7e778`:_ (#357 — the worksheet builder is now **CONTEXT-AWARE**: it reads scope/subject/topic(s) from the URL (single source of truth; extends the `buildDesktopWorksheetPath` idiom), DELETES the `topics[0]` guess, and redirects ONCE to the hub when no valid topic — **App.tsx untouched** (the hub already passed the params; the builder was ignoring them). **Multi-topic MI now aggregates** across all in-scope weak topics (`rankedInScopeWeakTopics` + `allocateMiCounts`: proportional marks-lost split + FLOOR [a chosen topic is never dropped] + CAP [≈50% at N≥3, 60/40 at 2] + availability gate + per-topic level-2 section skew); `allocateCounts`/`MI_BOOST` byte-unchanged; enrichment count + named topics **drawn-gated**. Sticky bar removed (mobile Preview restored); accessible `role="switch"`; 360px reflow; Home Worksheets card → hub. 8 files, App.tsx + all forbidden untouched; CI quality-gate GREEN; 6-dim review 5 clean + 1 honesty finding fixed. **No COMPLETION cell moved** — entry-flow + MI DEPTH on the existing Worksheet surface (§2a), owner live-verified. Closes [FU-WS-ENTRY-CONTEXT]/[FU-WS-MULTITOPIC-MI-AGGREGATE]/[FU-WS-PREVIEW-BUTTONS]/[FU-WS-MI-SWITCH]; [FU-WS-MI-COPY] HALF-landed; new [FU-WS-SCOPE-DERIVE] (Customise tick never derives scope) + **[FU-TOPICKEY-UNIVERSAL] (P0)** — raw-slug topic matching → 4 Science chapters (chemical-reactions-equations/acids-bases-salts/metals-non-metals/reproduction ≈1,180 Qs) return ZERO; Chapter Test + Full Mock carry the same LATENT defect (see §2a)) (#353 — the worksheet builder's Mistake-Intelligence is now **SCOPE-RELATIVE** (weakness resolved WITHIN the chosen scope via the new pure `worksheetMiSelector.ts` — `scopeHotspot` vs `globalHotspot`; the single locked box SPLIT into its true causes so a student WITH MI data sees the real weak topic NAMED + a one-tap remedy, never the false "grade a worksheet first") and **within-topic section enrichment is LIVE** for single-topic scope (section derived from each mistake's `totalMarks` via the CBSE band proxy — honest-unknown for non-band values, never a fabricated section; additive `orderPoolBySectionBoost` in `worksheetModel.ts`, capped at real availability, gated on the drawable pool so the toggle is never a no-op; cross-topic `MI_BOOST` byte-unchanged; NO schema change / no new writes). Desktop `position:sticky` Preview footer + drawer-foot Preview. 6 files, all `components/worksheet/`, zero forbidden; CI quality-gate GREEN + Vercel PASS; 11-agent review → 5 low findings all fixed + SHIP. **No COMPLETION cell moved** — Worksheet MI was already ✅; this is scope-relative DEPTH on an existing surface (§2a), owner live-verified. Closes [FU-MI-SCOPE-RELATIVE] + [FU-MI-ENRICH-WITHIN-TOPIC] + [FU-BUILDER-PREVIEW-AFFORDANCE]; new FUs [FU-WS-ENTRY-CONTEXT], [FU-WS-MULTITOPIC-MI-AGGREGATE], [FU-WS-PREVIEW-BUTTONS], [FU-WS-MI-SWITCH], [FU-WS-MI-COPY], [FU-MOBILE-VERIFY-GAP]) (#352 — objective **answer keys** repaired: the bank's objective rows are now **DETERMINISTICALLY gradable except 13 rows** pending a real-paper lookup (`docs/objective-answer-key-review-queue.md`). 89 defects re-derived via an AST scanner using the grader's OWN `normaliseOption`/`resolveOptionIndex`/`isObjectiveType` — 76 fixed (61 corrupt MCQ keys set to the exact correct EXISTING option + 15 Assertion-Reason rows given the 4 standard CBSE `options[]`), 13 honestly manifested (corrupted/duplicated options + figure-dependent → grader defers to model). `correctOption` never introduced — the key stays `q.answer`; grader `server/routes/*` byte-untouched; 43 bank files + 1 manifest; only `q.answer` + AR `options[]` changed. **No COMPLETION cell moved** — a bank data-correctness fix completing #348's guarantee. [FU-BANK-CORRUPT-KEYS] CLOSED except the 13; new FUs [FU-BANK-KEY-REVIEW-QUEUE], [FU-SECTION-A-VSA-HALFMARK] (99 Section-A written-answer rows all `marks:1` → 0/1, no live hole), [FU-BANK-GARBLED-DISPLAY-TEXT]) (#349 — the worksheet BUILD view redesigned to the locked "A · Smart default": a smart-default hero (`{mode} · {topic}` + real exact chip line) → primary **Preview worksheet →**; ALL prior controls preserved behind **Customise** (progressive disclosure, nothing removed); MI as ONE honest toggle; a new **Preview step** (real section bar/counts/marks, computed enrichment callout, 3 real samples, honest bank-shortfall) — one `candidate` set feeds hero→preview→generate so the shown count IS the generated count. **History moved OFF the page body to a header-control OVERLAY** (`WorksheetHistoryPanel` reuses SurfaceHistory rows + read-only `<ResultsScorecard>` re-open; `embedded`/`pendingOnly` props only) — the bottom in-page history section is GONE. **Pending uploads surface via `WorksheetPendingBanner`** (1 → `Upload now` deep-links + attaches to the EXISTING record, #338 idempotency, no dup; ≥2 → panel filtered to pending). PDF filenames now carry the unique code. The generated "worksheet is ready" view / `WorksheetGradePanel` / Practice hub / Topic Hub were **NOT touched**. **No COMPLETION cell moved** — Worksheet was already ✅ Built/Redesigned; this is discovered-scope DEPTH on an existing surface (like #344), logged §2a. Closes 2 of the 3 owner-found worksheet bugs post-#344 (PDF filename + history placement; grader MCQ = #348). New follow-ups: [FU-MI-SCOPE-RELATIVE], [FU-MI-ENRICH-WITHIN-TOPIC], [FU-BUILDER-PREVIEW-AFFORDANCE]) (#348 — the grader now enforces the **OBJECTIVE-scoring invariant** uniformly: an MCQ / Assertion-Reason / Section-A question scores **0 or FULL — never fractional, never step-distributed**; working is analysed ONLY to classify the mistake type. One shared `server/routes/objectiveScoring.cjs` (+ parity-pinned client twin) is called by BOTH grader functions → byte-aligned. **TWO paths, and they do NOT carry the same guarantee — be explicit:** **(a) bank-sourced** grading (the Worksheet surface, and bank-sourced Check & Improve, which forward `q.answer` = the option text + `q.options`) is **DETERMINISTIC** — a normalised key compare that OVERRIDES the model; **(b) keyless C&I uploads** (a student's own question, no bank key) are **MODEL-CLASSIFIED-then-CLAMPED** — a detect-step `objective` flag routes to the model's BINARY verdict clamped to 0/full, gated by a **≤1-mark safety rail** so a multi-mark subjective is never clamped on a model guess. Corrupt bank keys / no-`options` AR rows also fall to (b) [FU-BANK-CORRUPT-KEYS]. **No COMPLETION cell moved** — Check & Improve / Quick Practice / Worksheet were already ✅; this is a grader-correctness fix across them, owner live-verified on both paths. `objectiveScoring.cjs` is the single source of truth Chapter Test / Full Mock will consume when their MCQs move server-side. New follow-ups: [FU-OBJECTIVE-COST-SKIP], [FU-BANK-CORRUPT-KEYS], [FU-CI-SCORECARD-VARIANT] (C&I writes no session record / isn't a `SessionSurface` — an OPEN design question, not yet decided-necessary, so C&I stays Locked)) (#344 Progress-Journey ARC · PR-3 — the **per-surface Worksheet HISTORY** section (`components/results/SurfaceHistory.tsx`) on the WorksheetGenerator BUILD view: a "Your worksheets" list (code/title/date/score-chip-or-awaiting-pill/four-type dot-strip + honest empty) with an honest-or-silent `getSubjectProgress` vs-last-time chip, each row re-opening its STORED record as a READ-ONLY `<ResultsScorecard>` (no `perQuestionRef` reconstruction). **CONSUMES the store read-only** (no mutation). The **Worksheet surface now carries its history section**; the arc READ-LAYER (histories reading `sessionRecords` via `progressStore`) is proven on the one LIVE graded surface — CT/FM history are deferred `SURFACE_COPY` seams pending those rebuilds. No COMPLETION cell flips (Worksheet was already ✅ Built/Redesigned; this is discovered-scope depth, logged §2a). PR-2 files edited additively (no live-scorecard regression)) (#341 Progress-Journey ARC · PR-2 — the **Universal `<ResultsScorecard>`** extracted from `WorksheetScorecard`: one shared results SHELL + a typed 4-surface variant interface (`components/results/`). **Worksheet + Quick Practice variants LIVE**; Chapter Test + Full Mock = `deferred:true` config seams pending those surfaces' rebuilds. Presentational — writes nothing (§1a: Quick Practice writes no session record; worksheet write stays in `gradeWorksheetAndRecord`). **No COMPLETION cell moved** — both live surfaces were already ✅ and the worksheet scorecard is byte-identical (the non-regression gate); this consolidates shared chrome the later arc surfaces (histories/Me/CT/FM) will reuse) (#338 Progress-Journey ARC · PR-1 — the session-record **DATA LAYER**: `sessionRecords/{uid}/records/{code}` store + `progressStore` aggregation reader + durable cross-device `#NN` + `perQuestionRef`; the connectivity spine the scorecard/histories/Me/Home arc reads — **no surface COMPLETION cell moved** (data plumbing; the surfaces that read it are later arc PRs); new Firestore collection → rule deployed `dc73360`) (#330 Light extraction — CONTENT-ONLY: question-bank supply enriched 326→767 with bound figures; **no surface cells moved**) (post-#297 Z3 figure-binding — Quick Practice now renders bound source figures as `<img>` for Z3 case-based questions). **Maintainer ritual:** see §3 — it is updated in the SAME post-PR handoff the agent already does, reconciled by the cofounder each session, and the Verified column flipped by the owner.

This is the **glanceable "are we there yet" view** that the per-PR CURRENT_STATE/ROADMAP can't give. CURRENT_STATE answers "what did the last PR do"; THIS answers "how complete is each surface, and what's left to soft launch."

---
## 1 · THE NORTH STAR (definition of soft-launch-ready)
Soft launch = **every CORE student surface is Built + Redesigned (to the new grammar) + responsive on Desktop AND Mobile + MI-connected (where applicable) + owner Live-verified.**
Goal ORDER (owner-set): **(1)** finish & converge the surfaces + make everything responsive on both widths + wire MI through-and-through (the *architecture & plumbing*) → **(2)** then enrich data gradually (extraction, notes authoring). Data enrichment is a PARALLEL track and is NOT part of the architecture gate — a surface can be "done" structurally with a thin-but-honest bank.

**Legend:** ✅ done · 🟡 partial / in progress · ⬜ not started · ⚠️ done-but-owner-live-verify-pending · — not applicable
**Mobile column rule:** ✅ only if the surface renders the CURRENT design on mobile (one responsive component or verified mobile build). 🟡 if it still routes to an OLD/dead mobile surface — that drift is the known launch risk and must reach ✅.

**⚠️ THE SCOPE-IS-NOT-FIXED PRINCIPLE (read before trusting any row).** A surface's definition-of-done is NOT known in full up front. Building a surface *discovers* necessary pieces — the worksheet generator revealed the scorecard, the nomenclature, the durable record, the parent/teacher foundation; none was in the original plan, all proved necessary. This is scope *discovery*, not scope creep. So every row carries a **Scope** status, and the completion cells (✅/🟡) are read **against the surface's CURRENTLY-KNOWN scope** — which can grow.
- **Scope = `Settling`** — still discovering necessary detailing; the known scope is probably not the whole scope. Do NOT read a row of ✅s as "finished" while Scope is Settling.
- **Scope = `Locked`** — the shape has stabilized; nothing obvious remains; ✅s can be trusted as truly done. A `Locked` surface that later sprouts a new necessary piece is a useful signal (the lock was premature) — reopen it to `Settling`, log the discovery (§2a + the repo's DECISION_LOG), and usually spawn a NEW PR rather than swell an open one.

## 2 · THE MATRIX
Cells are read against each surface's CURRENTLY-KNOWN scope (see the Scope column + §4 discovered-scope log).
| Surface | Scope | Built | Redesigned | Desktop | Mobile | MI | Verified | Next action |
|---|---|---|---|---|---|---|---|---|
| **Check & Improve** | Settling | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | **#416 (C&I PR-2) — the FINAL C&I frontend PR MERGED (`a1eaebc`); the surface is COMPLETE desktop+mobile.** Per-question topic via route A2 (client re-runs the EXISTING `/detect-question` — sacred grader untouched) → by-topic scorecard lens + counted `N topics` chip (`topicCount` additive-optional, DISPLAY-ONLY; mixed still feeds no single-topic progress); PDF solution upload on both surfaces; **mobile parity** composing the shared services (`ensureCheckImproveSessionCode` retiring the device-local collision counter, `persistCheckImproveSession`, `ResultsScorecard`, `CheckImproveHistoryPanel`) — #437 stub deleted, no forked grader; `DesktopCheckImprovePage` diff ADDITIVE-only. **[FU-CI-PERQUESTION-TOPIC] + [FU-MOBILE-CI-PARITY] CLOSED.** **#420 (C&I PR-3, `cc84ae5`) closed the arc: [FU-CI-SOLUTION-CACHE] DONE** — scheme-first cached grading (Gate-2a quality gate on every write, Gate-2b `ADMIN_FIREBASE_UIDS` eviction, student-agnostic by construction); grader hooks-only diff, byte-review "textbook-clean". **NOTHING remains on C&I.** Owner live-verify pending on the live path (per-Q topic + chip; PDF-solution + can't-read fallback; 360px; cache-hit speed + garbled-not-persisted + eviction + 503-without-env; [FU-ADMIN-UIDS-DEPLOY-ENV] deploy step). Prior: **#395** first-class `SessionSurface` (durable record + `topicSource` + 5th scorecard variant + history overlay; [FU-CI-SCORECARD-VARIANT] + [FU-CI-DEVICE-LOCAL-SEQUENCE] CLOSED). `bank-matched` topicSource RESERVED-not-emitted. |
| **Quick Practice** | Locked | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | none — mature (finish-session scorecard, exact mark-band) |
| **Exam Trends** | Locked | ✅ | ✅ | ✅ | ✅ | — | 🟡 | **#416 (C&I PR-2 item E) brought `/exam-trends` under the one-header treatment** (added to `isMobileSelfChromedRoute` + a `!isDesktop` `MobileShell` header with the shared avatar-dropdown; old global brand bar retired; desktop chrome unchanged; BottomNav preserved) → **[FU-MOBILE-OLD-HEADER-TRENDS-PRACTICE] CLOSED**. Mobile 🟡→✅. Verified 🟡 = owner mobile live-verify of the new header pending. |
| **HPQ / Predicted** | Locked | ✅ | ✅ | ✅ | 🟡 | — | 🟡 | confirm mobile parity |
| **Topic Hub** | Settling | ✅ | ✅ | ✅ | ✅ | — | ✅ | PR-F content (Notes + Examiner's tips) → PR-G delete dead old surfaces |
| **Worksheet** | Settling | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | **BUILDER COMPLETE** (#360): context-aware entry (#357) · **derived scope** (#360: selectedTopics[]+allTopics single source of truth, unified topic picker, honest label) · scope-relative MI with within-topic section skew (#353/#357) · MI 2c copy done · history overlay · pending banner · code-bearing PDF filenames · 360px verified. All builder FUs closed, owner live-verified. **Remaining is NOT a builder defect:** the cross-surface **[FU-TOPICKEY-UNIVERSAL] (P0)** raw-slug topic match still returns zero for 4 Science chapters (≈1,180 Qs) here + latent in Chapter Test/Full Mock — dispatched separately (resolve-everywhere read+write + data migration + CI guard, NOT piecemeal). |
| **Chapter Test** | Settling | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | **BUILT to the locked spec (#374, `e54ab8c`), owner live-verified.** Two-phase grading (Section A objective 0-or-full on submit, PR-348; B–D subjective via the shared worksheet upload path, grader byte-unchanged); Universal `<ResultsScorecard>` chapter-test variant **flipped live** from `deferred:true` (config, not re-architecture); native canonical sourcing (D1, no adapter); `sessionRecords` surface `"chapter-test"` + `CT-{S}-{TOPIC}-{NN}`/#NN; navigator (4 states) / timer / topic-scoped history / pre-submit confirm / downloads. **Behind `MockViewGate` (owner flips at launch).** **#380 shipped the two pre-flip fast-follows:** the scorecard is now **section + concept + four-type** ([FU-CT-CONCEPT-LENS] — by-subtopic marks-lost lens, derived at render / honest-unknown / `sectionBreakdown` null, ALL resolved concepts sorted by loss), and the test surface is **chrome-less full-screen at both widths** ([FU-CT-HEADER-UNIFORMITY] — `isBareFullScreenRoute` suppresses the legacy navbar + mobile BottomNav on `/chapter-test`; `DesktopShell` byte-unchanged, it was the NON-shell navbar). **#397 wired the balanced PYQ+fresh SOURCING ([FU-CT-BALANCED-MIX] CLOSED):** each section (A–D) now draws through the SHARED `drawBalancedSet` (the FT helper, reused verbatim), so a CT paper deliberately mixes real PYQs with fresh authored/extracted questions (~50% PYQ, honest fallback for thin/zero-PYQ topics). Sourcing-only — paper shape/marks/grading/scorecard/numbering + `MIN_TEST_QUESTIONS` gate byte-identical; seed in-blueprint so `ChapterTestPage.tsx` untouched. **CT is ready to flip live at `MockViewGate` at the owner's discretion** (owner live-verify on the visible mix pending). Scope stays Settling until the gate flips; new open [FU-RETIRE-OLD-GLOBAL-HEADER] (product-wide legacy-header retirement — deliberate later) + the cross-surface [FU-BANK-UNRESOLVABLE-MCQ-KEYS] (34 unscorable Section-A MCQ keys, bank lane — see §2a). |
| **Full Mock** | Settling | ✅ | ✅ | ✅ | ✅ | ✅ | ⬜ | **BUILT to the locked spec (#387, `f6522d0`), owner byte-reviewed.** Reads the DUAL-SOURCE union (canonical + predicted) via `drawBalancedSet`; gated by `MockViewGate`. Two-phase grading (A objective 0-or-full at submit; B–E via the shared upload grader, byte-unchanged); Universal `<ResultsScorecard>` full-mock variant flipped live from `deferred:true` — section lens (exact) + CHAPTER lens (derived, `sectionBreakdown` null) + honest delta + §8b focus line; NO board-readiness. §8a persisted wall-clock + whole-paper resume; §8b focus aggregates. `FM-{S}-{NN}` records + perQuestion + mistakeLog. ONE responsive component (360px in the mockup grammar). **#391 FINALIZED the surface (2026-07-13):** hub "Full Test" card + Home tiles + MI link LIVE (plain nav, MockViewGate the ONLY gate; every old-engine entry retired) → **[FU-FM-HUB-ENTRY] CLOSED**; cross-device upload-later CLOSED via the text-only `fullMockPaperStore` snapshot (rules byte-untouched) → **[FU-FM-CROSS-DEVICE-UPLOAD] CLOSED**; executable RTL nav proof in-repo. **Verified = owner live-verify on the STABLE production link pending** (checklist §7, `report-ftfinalize-build-2026-07-13.md`) — the cell flips on that pass. [FU-CT-BALANCED-MIX] reuse follow-up remains (CT lane). |
| **Me / Progress** | Settling | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | **arc PR-4 SHIPPED (#408, `25c3cd7`) + PR-B-v2 ENGINE FIXES SHIPPED (#412, `1228c95`) — owner LIVE-VERIFIED on the stable link → Verified ⬜→✅. Launch-domino #3 closed: the arc shows REAL data across all four surfaces.** arc PR-4: `MobileMePage` retires the legacy Streak/XP hero; shared responsive `ProgressWindowArc` renders every rung honest-or-silent on both widths; careless carried; BottomNav preserved; trial badge dropped. **PR-B-v2 (#412) fixed the engine beneath it:** [FU-PROG-TOPIC-KEY-MISMATCH] CLOSED (resolveCanonicalSlug both sides — 5/26 topics could never key-match; legacy label attempts re-bucket), [FU-PROG-DATA-COMPLETENESS] CLOSED (unified graded stream: attempts ∪ record payload marks, deterministic ws:/ct:/fm: id dedup — CT/FM objective Section-A now counts; C&I dual write counts once by construction; pre-#403 record-only history healed), [FU-PROG-WINDOW-MODEL] CLOSED (activity-median split — wider ⊇ narrower; spanDays + isShortSpan → the honest amber short-term label on Me arc + Topic Hub), + the Topic Hub running-accuracy sparkline via cross-device `getTopicTrendFromCloud` (real scores from 2 points). Live-verify evidence: Polynomials hub 33.9%→46.9% + sparkline + honest label; Trigonometry honestly empty. **Scope stays Settling:** Home nudge PR-5 + the deferred presentation pass **[FU-PROGRESS-PRESENTATION-REDESIGN]** (fold per-topic trend into the topic HERO card; graphical Me with subject toggle + topic dropdown — owner-recorded, LATER PR, presentation-only, NOT a defect) + [FU-PROG-PRE403-QP-BACKFILL] (deferred). |
| **Tutor / Learn (TeachFlow)** | Settling | ✅ | 🟡 | ✅ | 🟡 | — | 🟡 | PR-D.1 mobile tutor toggle; [FU-CONTEXTUAL-TUTOR-REBUILD] |
| **Home** | Settling | ✅ | 🟡 | 🟡 | ✅ | — | 🟡 | converge to orient-first; real-insights wiring; confirm desktop |
| **Landing** | Locked | ✅ | ✅ | ✅ | ✅ | — | 🟡 | confirm both widths |
| **Login** | Locked | ✅ | ✅ | ✅ | ✅ | — | ✅ | none (Firebase live) |
| **Notes** (content track) | Settling | ✅ | — | ✅ | ✅ | — | ✅ | **v1.3 MERGED (#356, `629457e`) — template COMPLETE, owner live-verified.** v1.3 made the mindmap read as a **VISIBLE TREE** by default (per-branch `--mm-accent` rail + connector elbows + root › branch › leaf weight; open-state PRESERVED — the brief's "collapsed" premise was wrong: all specs are depth-2, already fully expanded at `useState(depth <= 1)`) and the note modal **near-full-screen** (92vw×92vh capped 1280px; mobile full-screen); kept the v1.2 ≤380px no-overlap win. Owner live-verified the tree + full-screen modal + **360px on a real device** → **Mobile + Verified flipped ✅**. v1.2 (#345): collapsible mindmap + per-step marks (schema 1.2, validator Rule 10) + note-as-popup + clickable NCERT page refs, proven across physics/maths/biology (light/quadratic/life-processes). **NOTES FAN-OUT COMPLETE (2026-07-12) — all 26 canonical topics specced + independently audited** (batches #365/#368/#370/#371/#372; Chemical Reactions = LOCKED chemistry exemplar; syllabus trims held — Heredity evolution-trimmed, Magnetic Effects motor/generator-trimmed). Clickable NCERT page-cites are **LIVE + owner-verified** (#375 `ncertPdfOffsets.ts` + owner-hosted `ncert/{subject}/ch{N}.pdf` + public-read rule + CORS; Trig p.114 / Heredity p.129 land exactly). Source-ledger `p.N` cites made clickable too via [FU-LEDGER-CLICKABLE-CITES] (PR #376, in review). **Surface ✅ COMPLETE.** |

## 2a · DISCOVERED-SCOPE LOG (scope that emerged AFTER a surface was first planned)
The record of how each surface revealed its true shape. Each entry: what was discovered · why it became necessary · where it went (PR / queued). This is not noise — it is the most reusable artifact here: it teaches us to predict the *next* surface's hidden scope (Chapter Test will discover its own equivalents).
- **Worksheet** (originally "a responsive generator"):
  - *Scorecard popup* — graded results needed a summary surface, not a wall of per-question text → PR-A (#295).
  - *Worksheet nomenclature `WS-{S}-{TOPIC}-{NN}`* — durable records and "which worksheet" need a stable identity → PR-A device-local, PR-B durable.
  - *Four-type breakdown on the scorecard* — the MI moat had to be reflected at the worksheet surface → PR-A.
  - *Branded graded PDF* — students wanted to keep/share the graded sheet → PR-A.
  - *Durable per-student record (uniqueness + journey + scorecard persistence)* — device-local couldn't guarantee fresh questions or a Me/Progress journey → PR-B (#321) MERGED, and now genuinely LIVE end-to-end after **#322** (`706cc12`). *Discovered while live-verifying PR-B:* the record was merged but non-functional — Firestore's `getFirestore` init threw on every `undefined`-bearing attempt doc and fire-and-forget `.catch` hid it (see D32). #322's `initializeFirestore(..., { ignoreUndefinedProperties: true })` fixed it; owner live-verified the durable `practiceInsights/{uid}/attempts` write on production. This does NOT flip Me/Progress to ✅ (the visual journey redesign is still pending) — it makes the persistence FOUNDATION the redesign reads from actually work.
  - *Parent/teacher view foundation* — once a durable record existed, seeding the parent/teacher storage became cheap and obvious → PR-B foundation; the VIEW is a later deliberate feature.
  - *Universal topicKey resolution* (discovered #357 live-verify) — the bank stores **51 distinct `topicKey` for ~26 chapters** (25 Title-Case + 26 slug), but the surfaces match RAW slugs, so **4 Science chapters (`chemical-reactions-equations`/`acids-bases-salts`/`metals-non-metals`/`reproduction` ≈ 1,180 Qs) return ZERO**, and `WorksheetGenerator.tsx`'s `q.topicKey === t.key` raw compare silently disables enrichment on Title-Case chapters. This is a **CROSS-SURFACE** defect — **Chapter Test and Full Mock carry it LATENTLY** — so the cure is resolve-everywhere (read AND write) + a CI guard (prior piecemeal fixes reappeared for lack of one), not a worksheet-local patch → **[FU-TOPICKEY-UNIVERSAL] (P0)**, Phase-2 [FU-BANK-TOPICKEY-NORMALISE]/[FU-MI-TOPICKEY-BACKFILL]. Its own scoped PR; do NOT attempt piecemeal. **UPDATE -- SHIPPED (#363, `6ecf15f`), owner live-verified.** C2 migrated all 34 orphan keys losslessly (assembled bank re-verified: **7084 served / 0 dup / 0 orphan / 26 canonical keys**); C3 wired a **dual-style Guard A** + an **authoritative import-based runtime proof** into CI so the disease cannot regenerate. The 4 zeroed Science chapters now return questions. Corrected the diagnosis numbers against the ASSEMBLED bank (true count **7084**, orphan volume **2,563**; the "51 distinct keys" was really 60 raw / 34 orphan). CROSS-SURFACE, so **no single surface completion cell flips** -- Worksheet/Chapter Test/Full Mock now all read correctly, but their redesign cells are unchanged. Discovered scope: the aggregator `canonicalQuestionBank.ts` itself holds 26 inline served questions -> [FU-AGGREGATOR-INLINE-QUESTIONS]; the mastery/schedule storage-key layer is a C1-preserved separate scheme -> [FU-TOPICHUB-MASTERY-STORAGE-KEY].
  - *Session-record DATA LAYER (Progress-Journey ARC · PR-1, #338 `d704b1c`)* — the durable per-COMPLETED-session record (`sessionRecords/{uid}/records/{code}`) + the ONE `progressStore` aggregation reader + durable cross-device `#NN` + `perQuestionRef` (review-my-answers) generalise the PR-B per-ATTEMPT record to the graded-SESSION altitude. This is the connectivity spine the Universal Scorecard (arc PR-2), per-surface histories (PR-3), **Me/Progress** (PR-4), and the Home ungraded nudge (PR-5) all READ — so those surfaces' redesigns now have their data foundation. **No completion cell flips yet** (data plumbing only; the reading surfaces are the later arc PRs). New top-level Firestore collection → owner-deployed rule `dc73360`.
  - *Universal `<ResultsScorecard>` (Progress-Journey ARC · PR-2, #341 `8c4c159`)* — the worksheet scorecard (PR-A discovered scope) was extracted into ONE shared results shell (`components/results/ResultsScorecard.tsx`) + a typed 4-surface variant interface (`scorecardVariants.ts`). Worksheet + Quick Practice variants are LIVE and behaviour-identical (worksheet proven byte-identical = the non-regression gate); Chapter Test + Full Mock are `deferred:true` config seams so those rebuilds (below) FILL config, not re-architect. Presentational — writes nothing (§1a). The old per-surface `WorksheetScorecard.tsx` was deleted (absorbed). No completion cell flips (both live surfaces already ✅); this is the shared chrome the per-surface histories (PR-3), Me/Progress (PR-4), and the CT/FM rebuilds will reuse.
  - *Per-surface HISTORY section (Progress-Journey ARC · PR-3, #344 `a4c3eec`)* — `components/results/SurfaceHistory.tsx` renders the durable session records as a "Your worksheets" list on the WorksheetGenerator BUILD view (rows: code/title/date/score-chip-or-awaiting-pill/four-type dot-strip + honest empty; a `getSubjectProgress` vs-last-time chip honest-or-silent; tap-row → READ-ONLY `<ResultsScorecard>` re-open, no `perQuestionRef` reconstruction). CONSUMES `progressStore` read-only; PR-2 files edited additively. This proves the arc READ-LAYER on the one LIVE graded surface — CT/FM history are deferred `SURFACE_COPY` seams pending those surfaces' rebuilds. No completion cell flips (Worksheet already ✅ Built/Redesigned; discovered-scope depth). C2 note: the vs-last-time chip is a subject-level MONTH trend, not a literal per-worksheet delta (possible fast-follow — OPEN_QUESTIONS).
  - *Progress-memory ENGINE (`getWindowedProgress`, PR-B, #403 `894ef6a`)* — the launch-blocker DATA layer: the ONE cross-device, multi-rung windowed aggregation (subject/topic/concept-bank-matched/section/mistake-type, honest-or-silent per rung) arc PR-4 + scorecards consume, over the durable streams honoring uid. Extends `progressStore` (the `void uid` device-local seam → real cloud read); new pure `progressBankIndex.ts`; additive read-only `getAllSessionPerQuestionFromCloud`. mistake-type = composition share over fully-graded only (adversarial review caught a pending-record rate fabrication); no rollup, `firestore.rules` untouched, sessionRecords WRITE shape byte-unchanged. A minimal desktop before→now arc (`ProgressWindowArc`) reads it. **No completion cell flips** — the visual redesign is arc PR-4, and **Mobile stays 🟡: owner live-verify found the arc renders ONLY in `DesktopMePage` (≥1024px); mobile still shows the legacy `pages/app/Me`** → [FU-MOBILE-ME-PROGRESS-PARITY] (arc PR-4, non-negotiable), plus [FU-PROGRESS-WINDOW-SPLIT-UX] + [FU-TOPICHUB-PROGRESS-ARC]. The engine is correct; these are consumption-surface gaps (OPEN_QUESTIONS).
  - *BUILD view redesign + history overlay + pending banner + unique PDF filename (#349 `b4f2162`)* — the WorksheetGenerator BUILD view was rebuilt to the locked "A · Smart default": a smart-default hero + a NEW **Preview step** (real section bar/counts/marks, computed enrichment callout, 3 real samples, honest bank-shortfall), all prior controls preserved behind **Customise**, MI as one honest toggle. **ONE `candidate` set feeds hero → preview → generate** (shown count == generated count). The PR-3 in-page `<SurfaceHistory>` mount was **moved off the page body** into a header-control OVERLAY (`WorksheetHistoryPanel`) that reuses SurfaceHistory's rows + read-only scorecard re-open unchanged (`embedded`/`pendingOnly` props only). New `WorksheetPendingBanner` surfaces ungraded worksheets (1 → deep-link Upload-now attaching to the EXISTING record via #338 idempotency; ≥2 → panel filtered to pending). PDF filenames now carry the unique `code` on all 3 paths. **No completion cell flips** (Worksheet already ✅ Built/Redesigned; discovered-scope depth). Closes 2 of the 3 owner-found worksheet bugs post-#344 (PDF filename + history placement; grader MCQ was #348). The generated "ready" view / `WorksheetGradePanel` / Practice hub / Topic Hub UNCHANGED. New FUs: [FU-MI-SCOPE-RELATIVE], [FU-MI-ENRICH-WITHIN-TOPIC], [FU-BUILDER-PREVIEW-AFFORDANCE].
  - *Scope-relative MI + within-topic section enrichment + Preview affordance (#353 `f8c1536`)* — the dispatched follow-up to #349, closing the three FUs above. MI is now **SCOPE-RELATIVE** (new pure `worksheetMiSelector.ts`: `scopeHotspot` = weakest in-scope topic vs `globalHotspot`), the single locked box **SPLIT into its true causes** (no data / weak-area-elsewhere + one-tap "Focus on / Add {topic}" / this-topic-IS-the-weak-area / signed-out — a student with MI data is never told to "grade a worksheet first"), and **within-topic SECTION enrichment is LIVE** for single-topic scope (section via the CBSE `totalMarks` band proxy — honest-unknown for non-band values, never a fabricated section; additive `orderPoolBySectionBoost` in `worksheetModel.ts`, capped at real availability, gated on the drawable pool so the toggle is never a no-op; cross-topic `MI_BOOST` byte-unchanged; NO schema change / no new writes). **FIX B** — desktop `position:sticky` Preview footer + drawer-foot Preview. 6 files, all `components/worksheet/`, zero forbidden/gated. **No completion cell flips** — Worksheet MI already ✅; scope-relative DEPTH (§2a), owner live-verified + squash-merged. CI quality-gate GREEN + Vercel PASS; 11-agent adversarial review → 5 low findings all fixed + SHIP. New FUs: [FU-WS-ENTRY-CONTEXT] (builder ignores the entry topic — `WorksheetGenerator()` takes no props, defaults to `topics[0]`), [FU-WS-MULTITOPIC-MI-AGGREGATE] (`weakestTopic()` names one topic → multi-topic should aggregate), [FU-WS-PREVIEW-BUTTONS] (drop the sticky, keep hero+drawer-foot), [FU-WS-MI-SWITCH], [FU-WS-MI-COPY], [FU-MOBILE-VERIFY-GAP] (mobile ≤360px never mockup-designed/live-verified — every future mockup ships a mobile frame + every live-verify includes a 360px check).
- **Topic Hub** (originally "an IA layout"):
  - *Exact mark-band filter* — concept-row Practise leaked wrong-mark questions through coarse buckets → PR-E1.
  - *Two-pool count divergence fix* — the "N available" hint and the displayed set drew from different samples → PR-E1.
  - *Concept rows FULLY SEEDED* — only 14 topics had real authored `boardEssentials`; the other 12 fell back to the generic `buildSampleActionable` "core ideas" rows → #337 authored real CBSE-2026-27 concepts for all 12, so **26/26 topics now render real board-essential concepts** (none on the fallback; `buildSampleActionable` retained as the net). Pedagogy sign-off (selection + mark bands) deferred to student-QC ([FU-TOPICHUB-PEDAGOGY-REVIEW]).
- **Notes** (content track):
  - *Visible-tree vs the responsive win (v1.3, #356 `629457e`)* — v1.2 made the mindmap responsive + collapsible, but it read FLAT. The brief blamed a collapsed default; ground-truth re-derivation showed the OPPOSITE — all three specs are **depth-2** trees already fully expanded at `useState(depth <= 1)` (no branch ever rendered a closed caret; leaves carry no caret). The real defect was **visual legibility** (~24 near-identical full-width cards at 16px indent don't read as a tree). Cure: PRESERVE the open-state, redraw as a real tree (per-branch `--mm-accent` rail + connector elbows + root › branch › leaf weight) while keeping the ≤380px no-overlap win. **Reusable lesson:** verify a brief's "collapsed/broken" claim against the actual rendered data before coding — here the fix was visual, not behavioural (the JS default is unchanged). Also surfaced [FU-PNPM-PACKAGEMANAGER-PIN] (a fresh worktree's `--frozen-lockfile` install fails `ERR_PNPM_LOCKFILE_CONFIG_MISMATCH`).
- **Chapter Test (#374 — BUILT):**
  - *Scorecard depth = section + four-type, not concept* — the locked scorecard lands a by-section (A–D) lens + the four-type MI, but NOT a subtopic-level ("concept") weak-area breakdown. `subtopic` IS on `CanonicalQuestion`, so a concept lens is derivable (FM-parity) → **[FU-CT-CONCEPT-LENS]**, a fast-follow before the `MockViewGate` flips. **UPDATE — SHIPPED (#380, `5bd148c`).** `deriveChapterTestConceptLens(response, questions)` derives the subtopic lens (join `qNumber → paper questionId → canonical subtopic`), rendered between the section lens and the four-type; derived-at-render / honest-unknown / `sectionBreakdown` stays null / ALL resolved concepts sorted by loss. Signature took a 2nd `questions` arg because the grade response carries `qNumber`, not `questionId`.
  - *Full-screen test still shows the DesktopShell global header* — the locked test-taking view is meant to be chrome-less/full-screen, but the global product header persists (restyling it is a forbidden-file `DesktopShell` change). Owner to decide: a global header restyle vs a chrome-less test surface → **[FU-CT-HEADER-UNIFORMITY]**, fast-follow before the gate flips. **UPDATE — SHIPPED (#380, `5bd148c`), route-scoped.** Recon reframe: the chrome on the test was the **NON-shell legacy navbar**, NOT `DesktopShell` — so `DesktopShell.tsx` is byte-unchanged, and `isBareFullScreenRoute` (App.tsx, owner-authorized bare-route exception) suppresses the legacy header + mobile BottomNav on `/chapter-test` at both widths. Owner chose the chrome-less test over a global restyle; the product-wide legacy-header retirement is the new open **[FU-RETIRE-OLD-GLOBAL-HEADER]**.
  - *Reopen-from-history downloads* — the read-only reopen is the PR-3 light re-open (score + four-type + derived lens + Done); graded/solution PDF on reopen needs paper reconstruction from `questionIds` → **[FU-CT-REOPEN-DOWNLOAD]** (deferred).
  - *Balanced PYQ+fresh sourcing exposed 34 unscorable Section-A MCQ keys (#397 `6db7f1d`)* — wiring the CT draw to `drawBalancedSet` was sourcing-only, but writing the test to assert the REAL grader contract (not seed luck) revealed a **CROSS-SURFACE bank defect**: 34 MCQs bank-wide whose `answer` resolves to NO option under the grader's `norm`, so any drawn into **CT or FT** Section A can never be scored correct → **[FU-BANK-UNRESOLVABLE-MCQ-KEYS]** (bank lane, before-launch scoring-correctness fix; full id-list in OPEN_QUESTIONS + the report). Distinct from the repaired [FU-BANK-CORRUPT-KEYS] 13. Lesson: assert the downstream contract a change feeds into (here the grader), not just the change's own shape — the test that "just verifies the wire" is where latent data defects surface. Also surfaced [FU-FM-BLUEPRINT-TEST-SEED-LUCK] (the FT test's identical strict assertion passes only because seed 11 misses the bad keys). No completion cell flips (sourcing DEPTH on the already-built CT surface; [FU-CT-BALANCED-MIX] CLOSED).
- **Full Mock (#391 — FT finalize):**
  - *A third old-engine dead link* — the plan named two Home-side retargets (the per-subject tiles); the build found a THIRD `/mock-builder` dead link (the mistake-strip "Add weak-area to next mock"). Same ratified treatment applied (re-point + honest "Open Full Test" relabel — no weak-area claim), flagged-not-silent, owner approved. Lesson: retiring a surface means grepping for EVERY route-literal referrer, not just the entries a plan names — which also surfaced [FU-RETIRE-EXAM-SIMULATION-LINKS] (6 legacy pages still on the OTHER old engine).
  - *Pre-existing vitest rot* — running the FULL suite for the nav proof exposed 6 failures in 3 untouched files that reproduce on trunk (invisible to CI, which runs the matrices not vitest) → [FU-VITEST-PREEXISTING-FAILURES].
- **Check & Improve (#395 — C&I PR-1, the SessionSurface plumbing):**
  - *The #348-flagged open design question, DECIDED + BUILT* — [FU-CI-SCORECARD-VARIANT] ("C&I writes no session record / isn't a `SessionSurface`") was resolved by the locked C&I spec 2026-07-10 + the 2026-07-12 dispatch: full session surface, `topicKeys[]` always populated WITH provenance (`topicSource`, mirroring `marksSource`). The "record with optional topic" alternative was rejected — an inferred topic is signal, not absence.
  - *Provenance over re-detection* — the correction UX already existed; PR-1 tagged its OUTPUT (`topicTouched` flag → confirmed/inferred; empty slug → mixed) instead of building a picker. `bank-matched` sits RESERVED in the union until a real matcher exists ([FU-CI-TOPICSOURCE-BANK-MATCHED-RESERVED]).
  - *The honest MIX ceiling* — until per-question topic (arc PR-2), a mixed paper's topic count is UNKNOWABLE: the history chip is a plain "Mixed topics" (the mock's "3 topics" literal would have fabricated a number — the dispatch's own honesty rule overrode its mock), and mixed sessions write `topicKeys: []`, feeding marks + four-type only, said out loud on the scorecard.
  - *Grade-time minting* — the lazy export-only code mint was WHY single-question sessions vanished; owner decision: mint at grade time, every graded session gets a record, `couldNotRead`-only gets none.
  - *One forced type ripple* — expanding the `SessionSurface` union forced a +4-line copy-seam entry in `SurfaceHistory.tsx` (`Record<SessionSurface,…>` exhaustiveness); flagged, owner-ratified. Lesson: a union expansion's ripples are found by tsc, not by the plan — budget for exhaustive-Record consumers.
- **Me-Progress:** *(none yet — Settling; expect discoveries during its redesign; log them here as they surface.)*

## 2b · OPEN DISCOVERED ITEMS NOT YET PLACED
(Discovered-necessary work that hasn't been assigned to a PR yet — the holding pen so nothing falls through.)
- *(none open right now — PR-B holds the current Worksheet discoveries.)*
- **[FU-AGGREGATOR-INLINE-QUESTIONS]** (from #363) — relocate the 26 inline served questions in `canonicalQuestionBank.ts` to a `questionBanks/**` pack so decision 3A ("migrate questionBanks-only") holds literally. Tracked in OPEN_QUESTIONS_AND_FOLLOWUPS.
- **[FU-TOPICHUB-MASTERY-STORAGE-KEY]** (from #363) — reconcile the mastery/schedule storage-key scheme (`normalizeTopicKeyForStorage` in `topicHubMastery.ts` + the dormant SR engine) with the canonical vocabulary; migration-sensitive (C1 preserved the key to avoid orphaning stored mastery). Tracked in OPEN_QUESTIONS_AND_FOLLOWUPS.
- **[FU-BANK-UNRESOLVABLE-MCQ-KEYS]** (from #397) — 34 MCQs bank-wide whose `answer` string-resolves to NO option under the grader's `norm`; unscorable if drawn into CT/FT Section A. **Before-launch scoring-correctness fix, bank lane.** Full 34-id list + failure classes in OPEN_QUESTIONS_AND_FOLLOWUPS + `report-ct-balanced-mix-2026-07-13.md`. 6 are Batch-5 REP2-* AR items → the bank-expansion authoring template must enforce `q.answer` = exact option text. Pairs with [FU-FM-BLUEPRINT-TEST-SEED-LUCK] (relax the FT test's strict key assertion, or fix the keys first).

## 3 · THE UPDATE DISCIPLINE (the culture that keeps this honest)
The tracker stays fresh because it rides the ritual that ALREADY happens, not a new separate process.

**Trigger A — every PR (the agent).** When a PR moves a surface's status, the agent flips the cell(s) here **in the same docs-handoff PR** it already files alongside CURRENT_STATE/ROADMAP. One line added to `handoff/templates/session-update-template.md` and to CLAUDE.md's handoff ritual: *"Update SURFACE_TRACKER.md cells for any surface this PR moved; if none moved, state so."* No flip is silent.

**Trigger B — every session (the cofounder / Claude).** At session START, reconcile the matrix against the repo (same verify-before-asserting rule — confirm a cell marked ✅ is actually true in the code) and flag any drift. At session END, ensure the matrix reflects what moved. The cofounder owns the *accuracy* of this file; the agent owns the *per-PR flip*.

**Trigger C — every live-verify (the owner).** When Chetan live-verifies a surface, the **Verified** cell flips ✅ (he says so; the agent or cofounder records it). A surface is never "done" on static work alone — ⚠️ until the owner verifies.

**Trigger D — the moment scope is DISCOVERED (anyone, mid-work).** When, mid-PR or mid-design, a new piece is decided necessary that wasn't in the plan, treat it as a first-class event, not a silent expansion:
1. **Log the decision** in the repo's `handoff/DECISION_LOG.md` with the rationale ("decided X necessary because …").
2. **Add it to §2a** under that surface (what · why · where it went), and keep the surface's **Scope = `Settling`** until the dust settles.
3. **Decide placement:** small + tightly coupled → fold into the open PR and note it in the report; non-trivial or separable → **spawn a NEW queued PR** (PR-A → PR-B was exactly this) and park it in §2b until assigned. **Default to spawning, not swelling** — a PR that grows mid-flight is the one most likely to slip past review. A surface only flips to `Locked` when no obvious discovered work remains.

**Cadence & milestones.** The matrix is the standing "are we there yet" board. A surface going fully green across all its applicable columns is a milestone — a one-line SESSION_LOG entry. The soft-launch gate is met when every CORE surface row (Check&Improve, Quick Practice, Topic Hub, Worksheet, Chapter Test, Full Mock, Me/Progress, Tutor, Home, Landing, Login) is ✅/✅ across Built · Redesigned · Desktop · Mobile · MI · Verified.

**Anti-rot guarantee.** Because the flip lives inside the existing post-PR handoff (the thing the agent does every single PR), it cannot drift far. The classic failure — a standalone tracker nobody updates — is avoided by *not* making it standalone.

## 4 · READING THE BOARD RIGHT NOW (so the path is unambiguous)
- **Mature / done (5):** Check & Improve, Quick Practice, Topic Hub, Login — plus Worksheet pending only the owner live-verify.
- **The surface-completion critical path (your stated priority):** **Chapter Test redesign → Full Mock rebuild → Me/Progress redesign.** These three are the bulk of "surfaces complete + responsive both + MI through." Chapter Test is next (design first); it establishes the scorecard-and-sheet + upload-grade pattern that Full Mock and Me/Progress reuse.
- **The cross-cutting mobile-drift risk:** several surfaces are 🟡 on Mobile because they still route to an OLD design (the split-component legacy). PR-G's deletion of dead old-mobile surfaces + per-surface convergence to one responsive component is what turns those ✅. Track it per surface here, not as a vague "make it responsive" task.
  - **Mobile account chrome is now app-wide via `MobileShell` (#410, `f662fbe`).** The account avatar-dropdown (desktop-parity: read-only subscription, same manage-subscription URL + logout) lives once in the shared `MobileShell`, so every mobile page that uses it inherits it; the old global mobile brand bar's one-header treatment (`isMobileSelfChromedRoute`) now covers `/browse`, `/welcome`, `/me`, `/check-improve`, `/intent`, `/practice/worksheets/ready`. **No completion cells flipped** — this is cross-cutting chrome, and the surfaces it touched were already ✅. **Discovered gap:** only 4 MobileShell surfaces are actually routed (the "12+ pages" was stale), and **`/exam-trends` + `/practice` still render the OLD global brand bar** (they don't use MobileShell) → **[FU-MOBILE-OLD-HEADER-TRENDS-PRACTICE]** (C&I PR-2 item E). `DesktopShell.tsx` byte-unchanged; shared status derivation now in `utils/accountStatus.ts` (desktop migration = [FU-DESKTOP-ACCOUNT-MENU-SHARE]).
- **MI through-and-through:** ✅ on Check&Improve, Quick Practice, Worksheet. **Gaps:** Chapter Test (MCQ-only today — needs the E2b upload loop for subjective + mistake-type MI) and Full Mock (⬜). Closing those two is "MI through-and-through."
- **Data (parallel, later):** extraction (next folder = Foundation 5-mark LA/proofs) and Notes authoring enrich the banks gradually; they do NOT block the architecture gate.
